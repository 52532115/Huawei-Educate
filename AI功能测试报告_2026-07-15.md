# SafeTAcademy AI 功能测试报告

- 测试日期：2026-07-15
- 项目：Education_Framework_Code_V1
- 平台：HarmonyOS 6.0 / API 20 / ArkTS
- 测试对象：`features/aiagent` 及其与 `entry`、`mine`、`train` 的集成
- 需求基线：《AI智能助教功能_需求规格说明书》V1.0

## 1. 结论

本轮按相同测试方案重新执行后，项目仍可完成全量非增量编译；错题智能归因、学习数据分析和自适应练习的离线测试表现稳定，但安全、网络和展示层仍有 7 项缺陷，因此尚不建议作为正式 AI 功能上线。

离线自动化共执行 33 项，26 项通过、7 项失败，通过率为 **78.8%**，与优化后的首次回归一致。该通过率不包含真实大模型回答准确率、OCR 准确率、真机性能、弱网和 UI 帧率，因此不能解释为整体产品质量得分。

### 第二次完整复测（2026-07-15）

两个已优化测试域共 15 项，复测结果仍为 **15/15 全部通过**：

- 错题智能归因：8/8，通过计算错误分类及“计算机网络”名称隔离测试。
- 学习数据分析：7/7，通过真实考试历史、真实课程来源、学习时长、确定性和无数据测试。
- 自适应练习：8/8，通过出题、去重、提交、错题同步和计分测试。
- 全量非增量 ArkTS 构建通过，Hvigor 报告用时 17.94 秒，命令总耗时 19.78 秒。

剩余 7 项失败属于本轮未要求修改的安全、SSE/重试、Markdown 与图片处理问题，仍需按下文优先级继续处理。

上线阻断问题主要有：

1. AI API 密钥硬编码在客户端，并已进入编译产物和未签名 HAP。
2. 用户学习数据直接发送给外部模型，未发现脱敏、用户授权和隐私告知流程。
3. 图片搜题后端使用硬编码 HTTP 地址，未满足需求中的 HTTPS 要求，且本次复测后端仍不可达。
4. 模型请求仍为非流式且没有实际重试，Markdown 与图片类型/资源释放也未符合要求。

## 2. 测试执行结果

| 测试域 | 结果 | 说明 |
|---|---:|---|
| 全量非增量构建 | 通过 | `assembleHap` 成功，Hvigor 用时 17.94 秒；存在 ArkTS 警告，且因未配置签名生成未签名 HAP |
| 离线自动化 | 26/33 | 通过率 78.8%，7 项失败与优化后首次回归一致 |
| 错题智能归因 | 8/8 | 概念、公式、方法、计算、粗心、选项差异、知识点名称隔离和信息不足均通过 |
| 学习数据分析 | 7/7 | 真实成绩、错题、课程、学习时长、确定性和无数据语义均通过 |
| 自适应练习 | 8/8 | 6题生成、ID去重、提交、错题同步去重和计分均通过 |
| 模型配置静态检查 | 部分通过 | HTTPS、模型名与官方接口格式有效；客户端密钥、SSE、重试不合格 |
| 安装包安全扫描 | 未通过 | 1 个源文件及 5 个构建产物检出密钥，包含 `modules.abc` 和未签名 HAP |
| 图片搜题后端 | 未通过 | `127.0.0.1:8000/api/test` 仍不可达；项目地址仅适用于模拟器访问宿主机 |
| 真实大模型调用 | 未执行 | 不使用已泄露的客户端密钥发起外部请求；未计入 33 项通过率 |
| OCR/图片识别准确率 | 未执行 | 图片后端未启动，没有可用 OCR 服务和标注图片集 |
| 真机 UI/性能/弱网 | 未执行 | 当前环境没有可控的已连接真机/模拟器会话及性能采样数据 |

## 3. 自动化明细

### 3.1 错题智能归因

以下 8 项全部通过：

- 概念关键词 → 概念理解偏差
- 公式关键词 → 公式/规则使用错误
- 方法关键词 → 解题方法不完整
- 计算/算错关键词 → 计算错误
- 粗心关键词 → 审题/细节失误
- 只有错误/正确选项差异 → 选项辨析能力不足
- “计算机网络”等知识点名称不参与原因判断
- 信息不足 → unknown

### 3.2 学习数据分析

以下 7 项全部通过：

- 从考试历史读取真实分数。
- 根据真实考试历史正确计算平均分。
- 正确解析错题的用户答案与正确答案。
- 课程进度来自真实课程数据，不再追加固定模拟课程。
- 学习时长来自真实记录或考试时长推导，不再返回固定值。
- 同一份输入得到确定的掌握度结果。
- 用户没有成绩时返回空历史，不伪造考试成绩。

### 3.3 自适应练习

以下 8 项全部通过：

- 默认生成 6 题。
- 同一轮题目 ID 不重复。
- 无用户画像时建立 3 个默认知识画像。
- 正确答案提交状态正确，且不生成错误归因。
- 错误答案生成归因并写入错题本。
- 同题重复提交不会重复写入错题本。
- 6 题中 3 题正确时汇总正确数为 3。
- 6 题中 3 题正确时得分为 50。

### 3.4 接口、安全与图片处理

通过：

- 模型接口使用 HTTPS。
- `deepseek-v4-flash` 是测试日期官方文档支持的模型名。
- 请求取消会销毁 HTTP 连接。
- 普通问答失败时显示友好提示。

失败：

- API 密钥硬编码在 `AiConstants.ets`，并可在 `modules.abc` 和未签名 HAP 中检出。
- 图片后端使用 `http://10.0.2.2:8000` 明文固定地址。
- Chat Completions 请求设置为 `stream=false`，回调只在完整响应后调用一次，不是 SSE 流式响应。
- 虽定义最多重试 3 次，但 `AiService` 没有重试循环。
- AI 消息直接用 `Text` 显示，没有 Markdown 解析与代码块渲染。
- 选择任意图片后均以 `image.jpg` 和 `image/jpeg` 上传，PNG、WebP 等类型会被错误声明。
- 打开的图片文件描述符没有关闭，重复选图可能造成资源泄漏。

## 4. 需求符合性检查

| 需求 | 状态 | 证据/说明 |
|---|---|---|
| F-AI-01 自动采集真实学习数据 | 部分符合 | 已接入考试历史、错题、课程进度和学习时长；培训记录等数据源仍未完整接入 |
| 知识掌握度评估 | 部分符合 | 已改为确定性计算且无数据不伪造；仍是启发式公式，尚无标注集验证准确率 |
| 历次考试趋势 | 符合基础要求 | 最多持久化 50 条真实考试历史并用于分析；尚未做大数据量与跨版本迁移测试 |
| F-AI-02 多轮上下文 | 符合基础要求 | 最多保留 20 条用户/AI成功消息作为上下文 |
| SSE 流式响应、首字延迟 <2s | 不符合/未测 | 实现为非流式完整响应，无法测真实 TTFT |
| Markdown 渲染 | 不符合 | 直接使用普通 Text 组件 |
| 新建对话 | 符合基础要求 | 内存中可创建会话 |
| 历史对话查看、离线缓存 | 不符合 | 会话只在内存中，未发现 Repository/持久化与历史选择 UI |
| AI 异常友好降级 | 部分符合 | 普通对话有失败提示，学习分析有本地报告降级；没有超时重试 |
| 数据脱敏后上传 | 不符合 | 未发现脱敏、最小化或字段过滤流程 |
| 传输全部使用 HTTPS | 不符合 | 模型 HTTPS，但图片搜题/历史接口为 HTTP |
| 60fps 与 LazyForEach | 不符合/未测 | 聊天列表使用 `Scroll + ForEach`，不是需求建议的 `List + LazyForEach`；未做帧率实测 |
| API 20+ | 符合 | 构建配置为 API 20，完整构建通过 |
| 手机、平板、2in1 | 部分符合 | aiagent HAR 声明三类设备，但 entry 仅声明 phone；未做真机布局测试 |
| 深色模式 | 不符合 | AI 页面大量硬编码浅色颜色，未发现深色资源/主题切换 |
| 中英文国际化 | 不符合 | AI 页面文案大多硬编码中文，资源目录只有 base |
| F-AI-03 学习路径建议 | 部分符合 | 能基于已采集真实数据生成建议和自适应练习；模型质量与推荐效果尚未在线评测 |
| 复习提醒/遗忘曲线 | 不符合 | 未发现提醒调度或遗忘曲线实现 |
| API 限流、缓存、用量监控 | 不符合 | 未发现相关实现 |
| AI 免责声明/人工审核机制 | 不符合 | 未发现相关 UI 或流程 |

## 5. 缺陷与优先级

### P0：上线阻断

#### AI-SEC-001 客户端密钥泄露

- 现象：源文件、ArkTS 编译产物及 HAP 均包含可识别 API 密钥。
- 影响：安装包可被反编译提取密钥，造成盗刷、费用和数据安全风险。
- 建议：立即吊销并轮换当前密钥；客户端改调自有后端，由后端保存模型密钥并执行认证、限流、审计。

#### AI-DATA-001 学习报告使用模拟数据并伪装为真实数据（已修复并回归）

- 现象：无成绩时生成固定考试历史；课程进度和学习时长始终固定。
- 影响：AI 会基于不存在的用户行为生成“个性化”建议，报告事实不可信。
- 建议：生产模式禁止模拟数据；无数据时明确显示“数据不足”；接入真实历史记录和课程进度接口。

#### AI-DATA-002 同一输入得到不同掌握度（已修复并回归）

- 现象：掌握度公式使用 `Math.random()`；50 次重复分析得到 67、75、80。
- 影响：弱项、优势与推荐可能在刷新后变化，无法复现和验收。
- 建议：使用确定性公式；分母必须来自真实答题数量，建立固定标注样本验证 Precision/Recall/F1。

#### AI-PRIV-001 学习数据上传无脱敏与授权

- 现象：成绩、错题、课程等直接拼入提示词发送至外部模型。
- 影响：不满足需求文档的数据隐私要求。
- 建议：增加显式授权、隐私政策、字段最小化、标识符去除、服务端审计和数据保留期限。

### P1：核心功能不符合需求

- AI-NET-001：请求不是 SSE，无法满足流式体验和首字延迟指标。
- AI-NET-002：重试常量未被使用，429/5xx/超时没有指数退避。
- AI-PHOTO-001：图片后端明文 HTTP、硬编码模拟器地址且本次不可达。
- AI-CHAT-001：Markdown 未解析，代码块、列表、加粗不能按需求渲染。
- AI-CHAT-002：对话历史没有持久化和查看入口，断网无法查看最近对话。
- AI-I18N-001：AI 功能没有中英文资源化。
- AI-THEME-001：未适配 Dark Mode。

### P2：质量与资源问题

- AI-ATTR-001：计算错误被归为粗心，分类体系与实现不一致。（已修复并回归）
- AI-PHOTO-002：所有图片强制声明为 JPEG。
- AI-PHOTO-003：图片文件描述符未关闭。
- AI-PERF-001：聊天消息使用 `Scroll + ForEach`；长会话存在一次性渲染风险。
- AI-BUILD-001：构建存在多个异常处理和废弃 API 警告，HAP 未签名。

## 6. 尚需执行的在线/真机测试

以下指标必须在完成密钥后端化、启动图片后端并准备设备后执行：

1. 文本问答：100～300 道教师标注题，统计最终答案准确率、步骤正确率、幻觉率和教学评分。
2. 多轮上下文：至少 30 组 5～10 轮对话，统计上下文保持率和指代理解准确率。
3. OCR：清晰、倾斜、反光、模糊、手写、公式、多题同框，统计 CER、公式准确率和端到端正确率。
4. 性能：记录 TTFT、完整响应 P50/P95/P99、失败率、超时率、内存、CPU 和聊天列表 FPS。
5. 稳定性：400/401/429/500/503、非法 JSON、空 choices、断网、网络切换、取消、连续点击和后台恢复。
6. 安全：Prompt Injection、系统提示泄漏、用户数据隔离、恶意/超大图片、日志敏感信息、接口重放和限流。

在没有完成这些测试前，文本答案准确率、OCR 准确率、P95 延迟和 60fps 均应标记为 **N/A**，不能填写为通过。

## 7. 复现方式

离线测试脚本：`_test_artifacts/ai_offline_test_harness.cjs`

运行命令：

```powershell
& 'C:\Users\Messi\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' `
  '_test_artifacts\ai_offline_test_harness.cjs'
```

脚本以非零退出码表示至少有一项失败，这是当前预期行为。

仅回归本轮优化的两个目标域，可追加 `--targeted`；目标域全部通过时返回退出码 0。

全量构建命令：

```powershell
$env:DEVECO_SDK_HOME='D:\devecostudio-windows-6.0.2.650\DevEco Studio\sdk'
& 'D:\devecostudio-windows-6.0.2.650\DevEco Studio\tools\hvigor\bin\hvigorw.bat' `
  --mode module -p module=entry@default -p product=default -p buildMode=debug `
  assembleHap --no-daemon --no-incremental
```

## 8. 外部配置核验

本轮复查 [DeepSeek 官方 API 文档](https://api-docs.deepseek.com/)：`https://api.deepseek.com` 支持 OpenAI 兼容接口，`deepseek-v4-flash` 是当前有效模型名。该检查只证明配置格式有效，不证明项目密钥、余额、限额和实际响应可用。
