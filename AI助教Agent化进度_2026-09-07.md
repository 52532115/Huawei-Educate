# AI 助教 Agent 化改造进度(更新于 2026-09-08)

> 目标:把 AI 助教从"聊天框(chatbox)"升级为"可自主调用本地能力的学习 agent"。
> 本文档记录改造进度、已修复问题、验证状态与下一步计划,供后续开发接续。

## 0. 2026-09-08 更新:P2 学习者档案(已完成并离线测试)

**新增文件 `service/LearnerProfileStore.ets`**:`LearnerProfile` 模型(weakPoints≤8 / strengths≤5 / focusTopics≤6 / examCount / avgScore / totalStudyTime / analysisSummaries≤3×200字,全部源自真实本地数据或助教自己的分析结论,零额外 API 调用);AppStorage + JSON 文件双持久化(`filesDir/learnerProfile.json`,沿用 SessionStore 模式);`updateFromAnalysis`(数据派生字段整体回写)、`recordFocusTopics`(去重合并,上限 6)、`recordAnalysisSummary`(仅存 ≥20 字结论,截断 200 字,保留最近 3 条)、`buildProfilePrompt`(空档案返回 '',非空生成中文档案文本)。

**接入点**:
- `AgentTools.ets`:新增第 7 个工具 `get_learner_profile`(读档案)+ 步骤文案;`get_learning_stats`/`analyze_error_book` 执行后用 `analyzeData` 结果回写档案;`generate_practice` 执行后 `recordFocusTopics(题目)`。
- `ChatViewModel.ets`:`buildContextMessages` 把档案 prompt 作为第二条 system 消息注入(CHAT 快路径与 agent 路径共用);agent system prompt 增加工具 7 规则与"结合历史档案"规则;ANALYTICS agent 回答完成后 `recordAnalysisSummary(回答内容)`。

**离线测试**:新增 `_test_artifacts/ai_agent_p2_test.cjs`(21 用例:prompt 构建/截断/去重/摘要保留/文件往返/损坏数据/工具回写);p0 更新为 7 工具断言。全部通过:p2 21/21、p0 7/7、p1 22/22、旧回归 26/33(7 个 FAIL 仍为已知旧遗留)。

**真机验证(待用户)**:①Bug #4 复验 ②发"分析我的学习情况"→ 档案生成 → 新建会话发"还记得我之前哪些知识点薄弱吗"看是否跨会话记忆 ③档案注入后普通 CHAT 是否仍正常。

**踩坑记录(新增)**:离线测试中 `.ets` 源码在 `new Function` 里执行,只能看到 global 作用域 —— prelude 里的 `const fileIo = {...}` 对被测模块不可见,必须挂 `global.fileIo`。

### 0.1 2026-09-08 第二次更新:P2.5 打磨(已完成并离线测试)

**1. 会话重启后练习按钮可点击**:`SessionStore` 的 `PersistedMessageRecord` 新增 `action?: string`,落盘/恢复时保留 `action`(如 `start_practice`)。修复了"重启后 AI 回答还在但 🚀 开始自适应练习 按钮丢失"的问题。

**2. PRACTICE 本地路径也回写档案**:`ChatViewModel.replyPracticeReady`(本地生成练习、不走工具的路径)与 agent 的 `generate_practice` 行为对齐,生成后 `recordFocusTopics(题目)`。

**3. 档案可视化页面「AI 眼中的你」**:
- 新文件 `components/LearnerProfileView.ets`:NavDestination 页面,展示学习概况(考试次数/平均分/累计学习,含最后更新时间)、薄弱点/优势/近期练习(标签卡片,Flex wrap)、AI 的分析记忆(编号列表);空档案显示引导态("去和 AI 助教聊聊")+ 返回按钮;右上角「重置」按钮(AlertDialog 确认后 `clearProfile()`,新加的 store 方法);底部隐私说明(仅本机保存)。
- 聊天页头部中间右侧新增 👤 按钮(`AiChatView.ets`)→ `pushPathByName('LearnerProfileView')`。
- `route_map.json` 注册 `LearnerProfileView` / `LearnerProfileViewBuilder`;`Index.ets` 导出。
- 注意:`LearnerProfileView` 是 UI 文件,离线 transpile 无法检查,需 DevEco 编译验证。

**离线测试**:`ai_agent_p2_test.cjs` 扩展至 24 用例(新增:clearProfile、SessionStore action 往返、STEP 不落盘)。全部通过:p2 24/24、p0 7/7、p1 22/22、旧回归 26/33(7 个 FAIL 仍为已知旧遗留)。

**真机验证(2026-09-08 已全部通过)**:①DevEco 编译通过 ②聊天页 👤 →「AI 眼中的你」正常展示(有档案/空档案两态)③重置按钮生效 ④重启后历史会话的练习按钮可点击。

## 1. 今日完成:P0(Agent 地基)+ P1(意图路由/练习交接/历史摘要)

### 1.1 P0 — Agent 地基(已完成并验证)

**架构变化**

```
之前: UI按钮 → 写死流程 → 拼prompt → 一次调用 → 假流式(一次回调)
现在: 用户输入 → IntentRouter 分流
        ├─ CHAT       → 直接流式(不带工具,快路径)
        ├─ ANALYTICS  → AgentService ReAct 循环 ──带tools调用──▶ DeepSeek
        │                ▲                                   │
        │                │                             返回 tool_calls?
        │                │                        ┌─是─▶ ToolRegistry 执行本地服务
        │                │                        │      (DataCollect/归因/练习)
        │                └──── tool 结果回填消息列表 ──┘
        │                              └─否─▶ SSE 流式(失败降级非流式)
        └─ PRACTICE   → 本地生成练习会话(0 API 调用)→ 动作卡片 → 练习页
```

**新增文件**

| 文件 | 职责 |
|---|---|
| `features/aiagent/src/main/ets/service/AgentService.ets` | ReAct 循环:最多 5 轮工具调用;总超时看门狗 120s(`AgentTimeoutError`);同一工具单次运行只执行一次(去重防循环);工具轮 max_tokens=1024;`AgentLimitError`(超轮数,用户友好提示) |
| `features/aiagent/src/main/ets/service/AgentTools.ets` | 6 个工具(get_learning_stats / get_exam_history / get_course_progress / get_error_questions / analyze_error_book / generate_practice)+ `buildToolDefinitions()`(真数组,非 JSON 字符串!)+ `getToolStepLabel()` 步骤文案 + `ToolErrorResult` 类 |
| `features/aiagent/src/main/ets/service/SessionStore.ets` | 会话持久化到 `filesDir/aiChatSessions.json`(AppStorage + 文件双写,沿用错题本模式);STEP 消息不落盘;恢复时 role/status 显式转换 |
| `features/aiagent/src/main/ets/service/IntentRouter.ets` | 本地意图分类(纯关键词,零成本):PRACTICE > ANALYTICS > CHAT |
| `features/aiagent/src/main/ets/service/ContextCompressor.ets` | 超窗口历史压缩为确定性本地摘要(用户问题要点,最多 10 条 × 30 字),0 API 调用 |
| `features/aiagent/src/main/ets/service/PracticeSessionStore.ets` | 练习会话交接:agent/路由生成的 `AdaptivePracticeSession` 存 AppStorage,练习页一次性消费 |
| `_test_artifacts/ai_agent_p0_test.cjs` | P0 离线冒烟测试(7 用例) |
| `_test_artifacts/ai_agent_p1_test.cjs` | P1 离线测试(22 用例) |

**修改文件**

| 文件 | 改动要点 |
|---|---|
| `service/AiService.ets` | 拆为 `chat`(非流式,解析 tool_calls,可传 `ApiToolSchema[]`)+ `chatStream`(SSE,dataReceive 事件 + 跨包行缓冲,事件不触发时从 response.result 兜底解析)+ `chatWithFallback`(流式失败/0 字符自动降级非流式,返回交付字符数);删除了旧 `sendChatMessage` |
| `viewmodel/ChatModels.ets` | `AiMessage` + `tool_calls`/`tool_call_id`;新增 `AiToolCall`/`AiResponseResult`/`ApiToolSchema` 等;`MessageRole` + `STEP`;`ChatMessage` + `action`/`stepDone` |
| `viewmodel/ChatViewModel.ets` | `sendMessage` 按 IntentRouter 分流;agent 版 system prompt(工具使用规则 + `{"action":"start_practice"}` 输出协议);`extractAction` 剥离动作 JSON;STEP 消息原地更新(activeStepId);会话恢复/保存;`buildContextMessages(prompt)` + 历史摘要注入 |
| `components/ChatBubble.ets` | STEP 消息渲染(spinner → 完成变绿 ✓,透明背景无边框无时间戳) |
| `components/AiChatView.ets` | 渲染 STEP 消息;AI 消息 `action==='start_practice'` 时显示「🚀 开始自适应练习」按钮跳练习页;ForEach key 含 `stepDone` |
| `components/PhotoSearchView.ets` | `sendChatMessage` → `chatStream`(累计回调) |
| `viewmodel/AdaptivePracticeViewModel.ets` | 构造时优先消费 PracticeSessionStore 的待练习会话,否则本地生成 |

### 1.2 期间修复的 Bug(按时间序,含根因,供回查)

| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | 编译错误 16 个 | ArkTS 严格限制:对象字面量需显式类型、throw 只允许 Error、await 需 async 函数、Color≠string、索引式访问 | payload 全部显式接口化;`applyToolCalls` 改 async;`parseRole/parseStatus` switch 转换;`'transparent'`;`getToolStepLabel()` 函数 |
| 2 | 对话提示"请检查网络连接后重试" | `TOOL_DEFINITIONS_JSON` 是 JSON 字符串,赋给 `tools` 字段序列化后是 `"tools":"[...]"`(字符串),API 期望真数组 → 400 | `tools` 改为 `ApiToolSchema[]`,`buildToolDefinitions()` 逐条构建 |
| 3 | 步骤消息变绿后"分析数据"仍转圈 | 完成回调创建了第二条消息(标绿),原消息永远 `stepDone=false` | ChatViewModel 用 `activeStepId` 原地更新同一条消息;ForEach key 加入 `stepDone` |
| 4 | "什么是导数"(CHAT 快路径)报"AI 未返回有效回答" | 设备上 HarmonyOS http 的 `dataReceive` 事件不触发,流式 0 字符;agent 路径有非流式兜底,CHAT 快路径没有 | `chatStream` 增加 `receivedViaEvent` 检测,事件不触发时从 `response.result` 解析完整 SSE 体;抽 `chatWithFallback` 供 CHAT 与 agent 两条路径共用 |

## 2. 验证状态

### 2.1 离线测试(全部通过)

```bash
node _test_artifacts/ai_agent_p0_test.cjs   # 7/7   SSE解析/工具Schema(7个)/工具执行/兜底/步骤文案
node _test_artifacts/ai_agent_p1_test.cjs   # 22/22 意图路由×10/摘要×4/练习会话往返×8
node _test_artifacts/ai_agent_p2_test.cjs   # 24/24 档案prompt/截断/去重/摘要/往返/工具回写/clearProfile/会话action往返
node _test_artifacts/ai_offline_test_harness.cjs  # 旧回归:26/33(7个FAIL为测试报告已知遗留问题,与本次无关)
```

### 2.2 真机验证(用户执行)

- ✅ 编译通过、可运行
- ✅ "帮我出几道练习题" → PRACTICE 路径正常(动作卡片 + 练习页同一组题)
- ✅ "分析我的学习情况" → agent 路径正常(步骤消息 → 回答)
- ⏳ "什么是导数" → 曾报"AI 未返回有效回答",**已修复(Bug #4),待重新验证**
- ✅ P2 档案(2026-09-08):编译通过;跨会话记忆生效("还记得我之前哪些知识点薄弱吗"正确回忆);普通 CHAT 正常
- ✅ P2.5 打磨(2026-09-08):编译通过;👤 →「AI 眼中的你」有档案/空档案两态正常;重置按钮生效;重启后历史会话练习按钮可点击

### 2.3 遗留提示

- 若日志显示 `[AiService] stream done: N chars (event=false)`,说明该设备不支持事件式 SSE,回答走整段解析/非流式兜底,功能正常但无逐字流式效果。
- 已知旧遗留(与本次无关,见 `AI功能测试报告_2026-07-15.md`):Markdown 未渲染(Text 直出)、图片搜题 MIME 固定 JPEG、imageFd 未关闭、API Key 硬编码。

## 3. 下一步计划(按优先级)

| 阶段 | 内容 | 说明 |
|---|---|---|
| **P3** | 体验优化(下一步) | ①取消按钮(接线 `AiService.cancelRequest()`,现在只是就绪未接 UI)②会话历史管理 UI(现在 sessionCount 只显示数字,无列表/删除/切换入口;SessionStore 与 ChatViewModel.switchSession/deleteSession 已就绪,只差 UI)③agent 轨迹日志(工具名/耗时/轮次写文件) |
| **P3.5** | 真流式 | 若想要逐字流式,`chatStream` 换 `http.requestInStream` 手动 read 循环(当前设备 event=false 时是整段返回) |
| **P4** | 工程化 | ①API Key 移到服务端代理(客户端硬编码可被反编译,测试报告已点名)②修复旧测试遗留 FAIL(Markdown 渲染/图片 MIME/fd 关闭) |

## 4. 关键经验(ArkTS 踩坑速查)

- 对象字面量必须对应显式接口/类(`arkts-no-untyped-obj-literals`)
- `throw` 只接受 Error 实例(`arkts-limited-throw`)
- 回调含 `await` 时,回调所在方法必须 `async`
- `.backgroundColor()` 传 `'transparent'` 字符串,不能传 `Color.Transparent`(返回类型为 string 时)
- OpenAI tools 参数必须是**真数组**,不能是 JSON 字符串(序列化会变 `"tools":"[...]"`)
- ForEach key 必须包含所有会变化的渲染字段(如 `stepDone`),否则原地更新不刷新
- 持久化反序列化后 string→enum 需显式 switch 转换(ArkTS 无隐式枚举转换)
- 离线测试模式:Node + `ts.transpileModule` + prelude 注入 stub(见 `_test_artifacts/*.cjs`)
- 离线测试 prelude 里的 stub(`fileIo` 等)必须挂 `global.*`——被测模块在 `new Function` 里执行,看不到测试脚本的词法作用域

## 5. 文件索引

- 核心服务:`features/aiagent/src/main/ets/service/` 下 AiService / AgentService / AgentTools / SessionStore / IntentRouter / ContextCompressor / PracticeSessionStore / LearnerProfileStore(P2 新增)
- 数据模型:`features/aiagent/src/main/ets/viewmodel/ChatModels.ets`、`AdaptivePracticeModels.ets`
- UI:`features/aiagent/src/main/ets/components/` 下 AiChatView / ChatBubble / AdaptivePracticeView / LearnerProfileView(P2.5 新增,👤 入口在 AiChatView 头部)
- 配置:`features/aiagent/src/main/ets/utils/AiConstants.ets`(API URL/Key/模型/超时,MAX_HISTORY_MESSAGES=20)
- 离线测试:`_test_artifacts/` 下 ai_agent_p0/p1/p2_test.cjs + ai_offline_test_harness.cjs
