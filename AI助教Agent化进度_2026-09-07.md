# AI 助教 Agent 化改造进度(更新于 2026-09-19)

> 目标:把 AI 助教从"聊天框(chatbox)"升级为"可自主调用本地能力的学习 agent"。
> 本文档记录改造进度、已修复问题、验证状态与下一步计划,供后续开发接续。

## 0. 2026-09-19 更新:P8 RAG + LLM 现场出题(已完成并跑通真机通路)

### 0.0 解决了什么

内置题库只有 **12 道硬编码题**,且与课程讲义脱节:冷启动时三个默认知识点各只对应一道题,
点两次「换一组」就取尽。P7 已经把讲义正文做成了可检索的本地索引,但那批语料**只被用来说话**(问答),
没被用来**出题**。P8 就是把它接上:检索最弱知识点的讲义片段 → 让后端 LLM 依片段写 3 道单选题
(题干/4 选项/答案下标/解析/建议)→ 落缓存池 → 下一轮练习排在最前。

### 0.1 新增两个文件 + 一处接线

- **`service/PracticeQuestionSynthesizer.ets`(纯逻辑)**:`buildPrompt` / `parseResponse` / `citationOf` /
  `isMaterialSufficient`。无 IO、无时钟、无随机 —— 整个「提示词契约 + 解析容错」都能离线断言。
- **`service/GeneratedQuestionPool.ets`(缓存池)**:按知识点存题,每知识点上限 8、知识点上限 24,
  淘汰按 `seq`(**不是数组下标**);**双键失效**(语料指纹 + `SYNTHESIS_CONTRACT_VERSION`);
  `filesDir/generatedQuestions.json`;所有 IO 失败静默降级为纯内存。
- **`AdaptivePracticeService.ets`**:新增**生成题层**,插在画像层**之前**;
  四层顺序填充 → **五层**:错题本 → **生成题** → 画像 → 题库 → 提示卡。

### 0.2 契约是量出来的,不是假设的(决定性证据)

用一个真调脚本(`_test_artifacts/_probe_question_gen.cjs`)打本机后端,量到:
返回**裸 JSON 数组**、每题恰好 **4 选项**、`correctIndex` **0-based**、选项**不带 `A.` 前缀**、
无代码块无解释文字 —— 契约 100% 符合。
**但 `sourceIndex` 被完全忽略(连测三次都忽略,尽管系统提示里逐字写明了这个字段)。**
→ 后果是设计性的:**「出处」不可能是「本题出自第几段」,只能是「写这题时参考了哪几段」**
(`citationOf`:去重 + 上限 2),UI 文案用「参考讲义」而非「本题出自」。
解析器仍然保留剥代码块/外壳的容错,因为换一个模型就未必这么听话。

### 0.3 两个「放错位置就静默变慢」的决定

- `setCorpusFingerprint(corpusFingerprint())` 被放在**异步 `runSynthesis` 的开头**,不在同步路径上:
  `corpusFingerprint()` 会解析 `KnowledgeStore` 单例,留在同步路径上就是「练习页首帧去建检索索引」。
- 后台预取只在**会话已经渲染完之后**触发(`PRACTICE_PREPARE_DELAY_MS = 1200`),
  `prepareQuestions` **单飞**;失败按**后端身份**粘住(`synthesisBlockedKey`),
  成功按知识点计 `COVERED_QUESTION_COUNT = 3` 达到覆盖就不再付费。
  **定价结论:每次出题约 1100 token。**

### 0.4 验证

| 项 | 结果 |
| --- | --- |
| 离线全套 `_run_all.cjs` | **629/629**(新增 p8 65) |
| p8 变异 `_mutate_generation.cjs` | **20/20 全红** |
| 服务端 `cd server && node --test` | **181/181** |
| 真机 `assembleHap --no-incremental` | **BUILD SUCCESSFUL 37 s,0 error / 60 warning(= 基线)** |
| 端到端真调 `_e2e_generation_real.cjs` | **21/21**(真调后端 200 / 3 题入池 / 下一组排第一 / 零嵌入请求 / 落盘可恢复) |

### 0.5 本轮最重要的两次「差点漏掉」

1. **离线全绿 ≠ 能编译。** `ts.transpileModule` 只转译、**不做类型检查**。629 全绿之后
   真机直接 `COMPILE RESULT:FAIL {ERROR:3 WARN:61}`,两条都出在后加的松散读取器上:
   `10605008 arkts-no-any-unknown`(`GeneratedQuestionPool.ets:427`)与
   `10505001` 非法 `as` 转换(`:356`)。
   修法见第 4 节新增条目。**结论:改完 .ets 必须跑一次真机编译,没有任何离线手段能替代。**
2. **解析器里两处真 bug 是「变异 + 手工构造坏输入」找出来的**,不是跑绿测出来的:
   ① `parseResponse` 遇 `null` 元素直接崩 —— `as` 是编译期断言,运行时什么都不检查,
      `buildQuestion` 开头必须显式判 `null / 非对象 / 数组`;
   ② 选项里有一个乱码时原来用 `continue` 跳过 → **它后面所有选项下标前移,`correctIndex` 指错,
      判分会把对的判成错的**。改成全有或全无(有坏选项就整题作废)。

### 0.6 顺手修好一个「把失败报成通过」的护栏

`_test_artifacts/_build_report.cjs` 用 `readFileSync(file, 'utf8')` 读 **UTF-16LE** 的 hvigor 日志,
于是每个 `indexOf` 返回 -1 —— 上面那次 **BUILD FAILED 被它报成「0 error / (no summary line)」且退出码 0**。
现在:嗅探 BOM 解码 → 剥 ANSI → **把换行删掉再匹配**(hvigor 在 ~100 列处硬换行,甚至会从路径中间断开,
实际文本是 `Learne`+换行+`rProfileView.ets`)→ 抓完整 `At File: …:\d+:\d+` → **构建失败 exit 1**。
已用那份已知失败的日志验证过:正确输出 `RESULT: FAILURE` 与两条真实错误。
**这是本项目第三次「校验工具自己误报通过」** —— 判「通过/失败」的判据必须拿一份**已知失败**的样本验一次。

### 0.7 下一步

- 真实嵌入模型上线以真正启用向量侧(换掉 embedder 即可,RRF 融合层不用动)。
- 其余未动方向:难度参与出题、掌握度细化、复习排程、出题配额、LLM 归因、会话内自适应。

## 0. 2026-09-19 更新:P7 向量侧(实测后才决定接,且只接对的那部分)

### 0.0 检索现状(边界清晰,别重复造)

- **入口只有一个**:`service/KnowledgeStore.ets` 的 `search()` / `searchBest()` / `buildContextBlock()` /
  `citationOf()`。**不要在别处再 `new KnowledgeChunker()` 或 `new KnowledgeIndex()`** ——
  分块与检索只应通过它访问。
- 语料 = **讲义正文 + 课程目录 + 题库**(**刻意只索引公开材料**,不索引错题本/成绩/档案;
  个人数据仍只走各自工具)。当前块数 **265**。讲义在 `KnowledgeNotes*.ets`(16 个内容文件按学科分),
  统一经 `KnowledgeNotesSupport.makeLecture()` 构造,**每门课恰好一篇,`courseId` 必须与
  `KnowledgeCourseCatalog` 对齐**(p7 有断言),正文以 `## <课程名>` 开头(这决定 citation 里有没有课程名)。
  **讲义里不许出现反引号**(模板字符串转义会吃掉正文)。
- `KnowledgeStore` 是**懒构建 + 进程内单例**;构建失败静默降级为空索引,绝不把异常抛给会话。
  启动预热入口**只有** `KnowledgeStore.scheduleWarmUp()`(在 `EntryAbility.onWindowStageCreate` 调用),
  它是经 `features/aiagent/Index.ets` 这个 barrel 导出的**唯一非 UI 导出**;
  **不要在启动路径上直接调 `warmUp()`**,要调延迟就用 `KNOWLEDGE_WARMUP_DELAY_MS`(400 ms)。
  **`filesDir` 必须在 `loadContent` 之前发布** —— 晚发布不报错,只会让 `KnowledgeIndexStore` 认定
  持久化不可用,于是每次启动全量重建,日志毫无异常。预热**不联网**。

### 0.1 向量侧:量完之后只接了「对的那一段」

- **`search()` 默认仍是纯词法 BM25。`searchHybrid()` 要显式传查询向量才启用,
  不要自动接线进 App**:实测本地哈希嵌入器权重 1.0 时 **Recall@3 从 0.8485 掉到 0.7879** ——
  它没有语义能力,当等权伙伴只会挤掉 BM25 的正确结果。p7 已把这个结论固化成断言。
- 嵌入**只有走后端代理**(`KnowledgeEmbeddingProxy`)才有意义:客户端只发 `{model, inputs}`,
  **不持任何厂商 embedding 密钥**(守住 P0 安全基线)。接上真嵌入模型后**换 embedder 即可,融合层(RRF)不用动**。
- 索引快照落 `filesDir/knowledgeIndex.json`(`KnowledgeIndexStore`),**绝不写 AppStorage**。
  **必须连倒排 postings 一起序列化**:只存 chunks 等于白存(倒排构建才是大头)。
  失效是**双键**:`fingerprintChunks()`(语料变)+ `buildSignature()`(分词器/索引结构变);
  只留一条会静默吃旧索引。`importState()` 任一校验不过就**整体拒绝**、索引保持原样。
- 评测集 `KnowledgeEvalSet`(33 例:21 direct + 12 paraphrase)+ `KnowledgeRetrievalMetrics`
  (Recall@K / MRR / 分桶)。**纯 BM25 基线:Recall@3 0.8485、MRR 0.7273、direct 0.9048、paraphrase 0.75。**
  接上远端向量后 0.9697。**改分词/加权/语料后必须复跑,掉分就是回归。**

### 0.2 端到端真调(留证:`_e2e_dense_real.cjs` / `_e2e_dense_run.log`)

真语料 → 真 `KnowledgeIndexStore` → 真代理 → 真 http → 本机后端 `/embed` → 百炼。
四段证据:①冷启动首问走 BM25 且**不等待**(后台同时真的开始嵌 265 块);②第二问走融合,
批次形状正确;③33 条评测用例经**生产** `searchBest` 复现基线;④快照落盘后新实例从缓存恢复向量、
**不再嵌入**、排序逐位不变 —— 这条用**真实 1024 维远端向量**验证了 int8+base64 编解码。

## 0. 2026-09-16 更新:P6 方案 2 — 掌握度累积模型 + 练习历史

### 0.0 解决了什么

改造前 `KnowledgePracticeProfile.mastery` 是**每次 `generateSession()` 从零重算**的启发式(错题 tag 起步 68、每多一道错题 −8 下限 20;课程进度用 `Math.min` 压顶),而且**只用于展示** —— 真正决定出什么题的是另一个字段 `priority`。结果是:练了十次的知识点和第一次看见的显示同一个数字,答对没有任何累积,停练一个月也不会重新浮上来。**"自适应"没有记忆。**

现在:掌握度**跨会话累积、随时间衰减**并持久化,练习历史可见,低分知识点回流到学习者档案。

> ⚠ 本文档同日的"气泡溢出第四修"见下一节。

### 0.1 新文件 `service/PracticeHistoryStore.ets`

持久化模板照抄 `LearnerProfileStore`:AppStorage key `PracticeHistory` + `<filesDir>/practiceHistory.json` 双写;读序 AppStorage → 文件 → 空;写序 AppStorage 后 `openSync(0o2|0o100|0o1000)`/`writeSync`/`closeSync`,失败静默不打断练习;`getHistoryFilePath()` 每次读 `AppStorage.get('filesDir')` 不缓存。**不 import Logger**(否则每个加载它的 prelude 都要补 Logger 桩)。

**规则(全部是确定数值,离线可断言)**

| 项 | 值 |
|---|---|
| 存储值钳制 | [0, 100];非有限值(NaN/±Inf)按 **0** 丢弃(不是按 100 —— 否则一条坏记录就能伪造"已掌握") |
| 答对 / 答错 | **+6 / −8** |
| 冷启动种子 | `DEFAULT_SEED_MASTERY = 68`(等于旧启发式的首次错题值) |
| 弱项阈值 | `WEAK_MASTERY_THRESHOLD = 60`,**严格小于**才算弱 |
| 衰减 | 复用错题本的 `reviewAgeLevel` 档位:0 / <1天 / <3天 / <7天 / <14天 → **−0 / −4 / −8 / −12 / −16**;超过 14 天也是 −16 |
| 有效掌握度 | `clamp(存储值 − 衰减, 0, 100)` —— **只在读取时算,永不回写存储值** |
| 条目上限 | 60,超出按 `lastPracticeTime` 淘汰最旧,**绝不淘汰刚写入的那条** |

纯函数层(`clampMastery` / `decayPenaltyForAgeLevel` / `masteryDecay` / `effectiveMasteryOf` / `nextStoredMastery` / `buildPracticeHistoryLine` / `buildHistorySummaryLine`)全部无 I/O,可离线裸测。类内**不读时钟**,`now` 全程由调用方注入。

**合计值不落盘**,由 entries 现算 —— 少一个可能失同步的真值来源。

### 0.2 三条产品决策(用户已确认,勿回改)

1. **每题提交即写**:掌握度写在 `submitAnswer` 里、**错题本回写之前**。顺序是有意的 —— 中途退出也必须保住,而且错题本那条路出问题不该连带扣掉掌握度。用 `masteryRecorded` 守卫防重复计数(该字段**刻意不持久化**,理由同 `isSubmitted`:交接过来的会话本来就是未作答状态)。
2. **参与出题排序,不动分层**:错题本仍是第一出题源 → 画像层按掌握度**升序**(最弱先出)→ 12 题题库兜底。`priority` **保留并照旧计算,降级为平局判据**(`PracticeSessionStore` 与 UI 都按位置读这 5 个字段,删了要动序列化;且同分很常见)。排序是全序:mastery → priority 降序 → tag 升序。
3. **课程进度不再压顶**,降级为卡片副标题里的一句提示。理由:被课程百分比钳住的累积值会让"答对 +6"在这门课上永远看不见。取舍是"课程进度 40% 但练得好"的知识点掌握度会高于 40。

### 0.3 接线与联动

- `AdaptivePracticeService`:新增 `historyStore` / `profileStore`(字段初始化器,`new AdaptivePracticeService()` 保持可用)、`profileSeeds`(启发式种子表,首次作答时用作起点)、`syncedWeakTagKey`(防止每答一题都写档案)。
- `buildProfiles(snapshot, now)`:启发式**整段保留,现在只当种子生成器**;有练习历史的 tag 用累积有效值覆盖并带上 `attempts/correct/lastPracticeTime`,`reason` 换成 `已练 6 次 · 正确 4 次（67%）· 10 天前练过，掌握度 66%（已衰减 8%）`。**只练过、启发式从未产出的知识点也会补进画像层**,否则题库答过的题掌握度涨了却没处显示。
- `resolveSeedMastery`:agent 交接路径(`PracticeAdaptiveViewModel` 直接 `loadPendingSession`,`generateSession` 没跑过)会**惰性调一次 `buildProfiles`** 补种子,最终兜底 `DEFAULT_SEED_MASTERY`。
- `pickQuestionForProfile` 加 `now` 参数,占位题 id 的 `Date.now()` 换成注入值 —— 排序路径上再无时钟读取。
- **`LearnerProfileStore` 弱点合流(关键坑)**:`updateFromAnalysis` 原本是 `profile.weakPoints = data.weakPoints.slice(...)` **整体覆盖** —— 朴素的"把练习弱点 push 进去"会被下一次「学习分析」静默抹掉。改成两个来源(`dataWeakPoints` / `practiceWeakPoints`,后者限 3)+ `mergeWeakPoints` 派生合并视图;练习项先占位,分析项补足到 8。**练习列表为空时行为与改造前逐字相同**(p2 的断言因此不受影响)。`recordToProfile` 对旧档案(没有 `dataWeakPoints`)退回用 `weakPoints` 填充。
- **tag 归一化统一(顺手补的坑)**:出题路径把乱码 category 映射成「未分类」,画像路径却用原始串 → 同一知识点两套 key,掌握度永远对不上。现在两边共用 `ErrorBookPracticeSource.normalizeKnowledgeTag`;服务里那套自带的 `containsMojibake`(会把半角 `?` 当乱码)**已删除**。
- **UI**:练习页重点面板加一行累计统计(`buildHistorySummaryLine`),总结页加"累计掌握度仍偏低:…"与累计统计。每个知识点自己的历史已经在 `profile.reason` 里,不重复渲染。`historyTotals` 由 ViewModel 持有并在 `syncState()` 里刷新(总结页只在 `finishSession` 后才有,面板需要更早拿到数据)。

### 0.4 离线测试

新增 `_test_artifacts/ai_agent_p6_test.cjs` **51 用例**:A 衰减档位 6 / B 写入 4 / C 存储往返 16 / D 选择顺序 6 / E 冷启动与抛弃 3 / F 幂等 1 / G 档案联动 4 / H 文案与静态 8 / I 确定性 2。

**全部通过:p6 51/51、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、p5 69/69、harness 33/33。**

**prelude 必须同步改**(加了字段/依赖就会打挂整个文件):
- `ai_offline_test_harness.cjs`:补 `PracticeHistoryStore`(桩成 `attempts===0`,三个默认画像与既有断言逐字不变)、`LearnerProfileStore`、`DEFAULT_SEED_MASTERY`、`normalizeKnowledgeTag`。
- `ai_agent_p5_test.cjs`:`servicePrelude` 改为**加载真 store**(p5 的写回组保持真集成测试),`vmPrelude` 补 `PracticeHistoryTotals`;模型桩补 `masteryRecorded` 与三个新画像字段。
- p6 自身:真 `ErrorBookPracticeSource` **也需要** `ErrorBookPracticeCandidate` 在作用域里 —— 服务会自己 new 一个 source,所以那个类必须出现在**服务**的 prelude,不是只出现在外层测试文件。

### 0.5 本轮踩坑(已并入第 4 节)

1. **淘汰逻辑的循环上界写错,淘汰对象会悄悄变成"第二新的"**:`for (i...; result.length < MAX-1; i++)` 在遇到被保护元素前就满足了长度条件而退出,于是保护元素从未被跳过、又被 push 到末尾 → **条数正确(仍是 60)但被淘汰的是最新那条**。只在"全序 + 保护元素恰好排在末尾"时暴露。修法:先把被保护元素**过滤出候选集**,再取最旧的 `MAX-1` 条 + 保护元素。教训:**"数量对"不等于"顺序对",淘汰类逻辑必须同时断言"留下的是哪些"**。
2. **`Math.round`/`min/max` 的期望值必须手算**:本次连续把三处期望值填错(72−8 写成 60、70+6 写成 74、阈值 60 写成"<=60 算弱"),全是**测试期望错、代码没错**。写死期望前先把种子值抄出来算一遍。
3. JS 解析陷阱:`new loadService().AdaptivePracticeService()` 会被解析成 `new (loadService().AdaptivePracticeService)()` 之外的形态,报 `Class constructor cannot be invoked without 'new'` 且**行号指向别处**;拆成 `const m = loadService(); new m.AdaptivePracticeService()` 或加括号即可。排查这类"报错行号说不通"的问题要看**调用栈**,别只看指向行。

### 0.6 真机验证(待用户)

1. 做一组练习 → 回重点面板,掌握度数字应变化、副标题出现「已练 N 次 · 正确 M 次」,面板底部出现累计统计。
2. **杀进程重进** → 数字与次数仍在(验证 `practiceHistory.json`)。
3. **中途退出**:答两题后返回,重启 → 掌握度已计入(这是"每题即写"的核心承诺)。
4. 出题顺序:某知识点连续答错 → 下轮它排前面;答对几次 → 沉底。
5. 「AI 眼中的你」的薄弱点应包含练习打出来的低分知识点,且**再点一次「学习分析」不会被抹掉**。
6. 若约 15 天后仍未练习某知识点,数字应比上次低(衰减生效)。

## 0. 2026-09-16 更新:气泡溢出——量了截图才找到的真根因

**用户又给了一张真机截图(文字仍溢出)。这次不再靠推断,把截图解码成像素来量。**

### 0.0 量出来的几何(决定性证据)

用 Node 解 PNG(zlib 解压 IDAT + 反 filter)后逐行扫描像素,结果:

| 元素 | 左边界 | 右边界 |
|---|---|---|
| 气泡白底 | x=2 | **x=353** |
| 气泡边框 | — | x=355 |
| 文字墨迹 | x≈7 | **x=424** |

**文字比气泡宽了约 70px(20%),而且只往右溢出**(之前三轮一直以为是"两侧被切")。白底右边那一截是干净的背景色,x=353 之后没有任何白色 —— 白底没有延伸到文字下面。

### 0.1 真根因:`constraintSize` 不约束子元素的测量宽度

前三轮的模型是"百分比在自适应容器里解析不对",方向没错但**改错了对象**。

真正发生的事:

- `constraintSize({ maxWidth: 309 })` 作用在气泡那个 `Column` 上,**只把气泡自己的最终尺寸截到 309**;
- 它**不会**把 309 变成子元素的测量约束。子元素(标记为 `width('100%')` 的 `MarkdownText` 根)**仍是按父级 Row 传下来的可用宽度(≈393)解析**;
- 于是文字列 = 393,气泡 = 315,气泡又是 `HorizontalAlign.Start` 对齐 → 右边 70px 落在白底外面。

**判定口诀:白底边界和文字墨迹边界不重合时,不是"百分比解析错",而是"两个宽度根本不是同一个数"。** 只要让它们来自同一个数值就闭合了 —— 不管引擎怎么解释百分比。

### 0.2 修法(这一版不再依赖任何百分比)

1. **`ChatBubble`**:`resolveBubbleMaxWidth` → **`resolveBubbleContentWidth`**,返回值改成**气泡内容区**宽度(= 旧值再减去气泡自身 12+12 内边距),`@State contentWidth`。
2. **三个内容分支全部 `.width(this.contentWidth)`**:USER 文本、markdown 包装 `Column`、兜底文本。文字和气泡现在由同一个数决定,不可能再差 20%。
3. **`MarkdownText` 新增必填 `@Prop contentWidth: number`**,根 `Column` 用 `.width(this.contentWidth)` 取代 `.width('100%')`。根一旦是确定数值,里面所有 `width('100%')`/`layoutWeight(1)` 分支才真正被兜住。
4. 气泡的 `constraintSize({ maxWidth: this.contentWidth + BUBBLE_INNER_PADDING })` 保留(同源数值,不再有歧义)。

> 由此可推:360vp 屏 → 内容 270vp、气泡 294vp、行宽 336vp;412vp 屏 → 282/306/364;480vp 屏 → 360/384/432(见测试)。

**离线测试**:`ai_agent_p5_test.cjs` → **69 用例**(新增两条回归护栏:①气泡内 `.width(this.contentWidth)` 恰好 3 处,防某个分支退回固有宽度测量;②`contentWidth + 内边距 ≤ 行宽`,防内容宽度反超气泡)。

**全部通过:p5 69/69、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、harness 33/33。**

**真机验证(待用户)**:编译后发一条长回复(如"分析我的学习情况"),文字应完整落在白色气泡内。

<details>
<summary><b>历史:2026-09-11 气泡修法(确定数值宽度)—— 只解决了一半,已被 09-16 取代</b>(点击展开)</summary>

**用户真机复测(2026-09-11)**:✅ 点选项出高亮、✅ 换一组换题 —— **P5 那两块 UI 冻结问题确认修好了**;❌ 只剩气泡溢出。

**这一轮不再推断,直接消灭歧义**:前两轮都在调"百分比怎么解析",这轮把宽度来源整个换成**确定数值**。

> ⚠ 事后看,这一轮**把气泡的宽度改对了、却没改文字容器的宽度**:`constraintSize` 只截气泡自己,子元素仍按 Row 给的行宽测量。所以气泡确实缩到了 309vp,文字却还是 393vp —— 溢出量只减了一点。教训记在第 4 节。

1. **`ChatBubble`:根因是 `constraintSize({ maxWidth: '75%' })` 这个百分比**。气泡是"内容自适应 + 百分比上限"的容器,百分比在测量阶段只能对**父级传下来的约束**求解,拿不到气泡自己的实际宽度 → 底下的 markdown 列比气泡宽 → 被气泡居中 → 左右两侧同时被切。
   改为模块级助手 `resolveBubbleMaxWidth(uiContext)`:`display.getDefaultDisplaySync().width` → `px2vp` → `min(宽度 − 24(外层 Row padding) − 24(气泡自身 padding), 宽度 × 0.75)`,四舍五入后作为 **vp 数值**传给 `constraintSize`。display 抛异常时兜底 264vp(即 360vp 屏的 75%),另有下限钳制防退化。`@State bubbleMaxWidth` 在组件创建时算一次(应用锁竖屏,气泡存活期间显示宽度不变)。
   > 360vp 屏 → 270vp;412vp 屏 → 309vp;480vp 屏 → 360vp(此时由 padding 预算封顶,不再是 75%)。

2. **`MarkdownText` 根 `Column` 补回显式 `.width('100%')`**(上一轮按"让父容器把宽度传下来"的假设**删掉**了它,那正是它更早溢出的一条通道:没有宽度时列按**子元素的固有宽度**测量,一个不换行的标题就能把整列撑到比气泡宽)。现在气泡有确定宽度,百分比才有意义,三处列表/段落分支都已被它兜住。

**离线测试**:`ai_agent_p5_test.cjs` 扩到 **67 用例**(G 组新增 3 条:①断言宽度上限是确定数值、源码里不再出现 `maxWidth: '<百分比>'`;②把 `resolveBubbleMaxWidth` 从源码里切出来真跑一遍——把四个屏宽档的目标值写死在期望里,等于是"这套宽度算法"的可执行规格;③断言 Markdown 根容器仍显式限定宽度)。静态检查前先 `stripArkTsComments`——**解释这次修复的注释里就写着 `maxWidth: '75%'` 和 `const` 片段,不剥注释会被自己的说明文字命中**(踩坑速查已记)。
**全部通过:p5 67/67、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、harness 33/33。**

**真机验证(待用户)**:发一条长回复(如"分析我的学习情况")→ 文字应完整落在白色气泡内、左右都不被切。若**仍溢出**,请给一张**能看见气泡左右边界**的完整截图——下一个候选是 `InlineSpans` 那个 `Text`(它是整条链上唯一没被显式约束宽度的节点)。

</details>

## 0. 2026-09-10 更新:P5 自适应练习·错题驱动题库(方案 1,已完成并离线测试)

### 0.0 本日总览(2026-09-10)

| # | 工作项 | 内容 | 离线测试 | 真机状态 |
|---|---|---|---|---|
| 1 | **P5 功能主体** | 错题本成为第一出题源(方案 1/6):清洗/去重/排序/round-robin 覆盖 + 4 层出题 + 写回闭环 | p5 55/55 | 编译通过,已进入页面操作 |
| 2 | 修复:点选项/换一组无响应(第一轮) | 选项 ForEach key 带选中状态 + `excludeIds` 候选轮换 | p5 60/60 | 复测**仍无反应** → 见 #3 |
| 3 | 修复:同上(第二轮,**真正根因**) | ArkUI `@Builder` 按值传参不刷新 → 无参 `PracticeContent()` + `cardKeys` render key + keyed ForEach 强制重建 | p5 62/62 | 待复测 |
| 4 | 修复:三项缺陷 | ①演示题答案键写错 ②错题本重复条目 ③AI 气泡文字溢出 | p5 63→64/64 | 待复测 |
| 5 | 修复:会话面板要点两次才出来 | 一个节点只能挂一个 `bindSheet` → 拆到两个节点 | p5 64/64 | 待复测 |

**最终离线测试:p5 64/64、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、旧 harness 33/33 —— 全绿。**

**当日最大的教训**:ArkUI 里"点了没反应"几乎都源于同一类问题——**状态改了但 UI 没刷新**。今天连踩三次(带参 `@Builder` 不刷新 / ForEach key 不含变化字段 / 一个节点挂两个 `bindSheet`),外加一次布局问题(内容自适应容器里子元素写 `width('100%')` 会两侧溢出)。**排查顺序建议:先怀疑渲染机制,再怀疑事件绑定。** 详见第 4 节踩坑速查。

**问题**:练习的出题源是 `AdaptivePracticeService.buildQuestionBank()` 里 12 道硬编码题。所谓"自适应"只是拿知识点 tag 去这 12 道里做字符串匹配,匹配不上就合成一道通用占位题(「关于「X」的复习,最有效的第一步是什么?」)——跟学生真正错过的内容无关。而错题本里躺着真实题目(选项/正确答案/知识点/错因/时间全都有),却只被用来统计 tag 频次。

**1. 新文件 `service/ErrorBookPracticeSource.ets`** —— 纯转换层(不碰 AppStorage/fileIo,100% 可离线单测):错题记录 → 可作答题目。
- **清洗**:题干空/乱码丢弃;选项非数组、含空白项、<2 项丢弃;**选项位置必须保持不变**——`['', 'B', 'C']` 这类若"过滤掉空项"会让 correctIndex 指向错误答案,只能整条丢弃;`rightQues` 越界丢弃;>6 项截断;知识点空/乱码 → `未分类`(题目仍保留)。
- **去重**:按归一化题干分组(手录页完全无去重,同题存两次就是两条记录),组内取最优一条,组大小并入 `repeatCount`。
- **排序**:`score = 100 × (0.6×时间档 + 0.4×错误次数档)`;**now 全程由参数注入**,类内禁用 Date.now()/Math.random(),同输入必然同输出;比较器是全序(score→演示数据靠后→最后活动时间→创建时间→题干→id);**绝不使用数组下标**——错题本用 unshift 写入(新在前),位置无意义。时间项取 `max(createTime, lastReviewTime)`,答错重做不刷新 createTime(保留"最初错在什么时候"的年龄信号)。
- **覆盖**:按知识点分桶后 round-robin,6 道题尽量来自 6 个不同知识点,不会全挤在一个 tag。
- **演示数据**(id 以 `err_default_` 开头)纳入出题源,但排序时靠后、且题目不带回写链接(`sourceRecordId=''`)。
  > ⚠ 最初的设计是"演示题答错一律走追加,不碰演示数据",后来发现这会造成重复条目(见 0.3 ②),已改为**按题干落到同一条记录上原地更新**,此处描述以 0.3 为准。

**2. `generateSession` 改 4 层**:错题源 → 原有 profile 驱动 → 跨层题干去重 → 原题库兜底。`pickQuestionForProfile`/`buildQuestionBank`/`buildProfiles` **一行未改**,错题本为空时行为与改造前完全一致。

**3. 写回闭环**(关键:防止同一道题在错题本里越滚越多)
- 重做**又答错** → `recordPracticeMistake` **原地更新**已有记录(方法名与匹配规则见 0.3 ②,最初版本叫 `refreshPracticeMistake`、只按 id 匹配),只写 `answer`/`wrongCount`/`lastReviewTime`/`analysis` 四个字段;题干、`rightQues`、学生自己的 `userNote`/`mistakeReason`、`createTime` 一律不动。找不到对应记录才追加,并把记录 id 写回题目(会话恢复后再提交仍能定位)。
- 重做**答对** → 只推入 `pendingReviewIds`,在 `buildSummary` 时批量刷新 `lastReviewTime` 让它下次排序沉底。**绝不自动删除用户数据**。
- 模型:`ErrorQuestionItem` 新增 `id`/`analysis`/`wrongCount`/`lastReviewTime`;`AdaptivePracticeQuestion` 新增 `sourceRecordId`(第 10 个可选构造参数);`PracticeSessionStore` 双向带上 `sourceRecordId`/`syncedToErrorBook`。

**4. UI**:练习页题号行新增「错题重做」标签;同步文案区分「已更新错题本记录」/「已加入错题本」。

**离线测试**:新增 `_test_artifacts/ai_agent_p5_test.cjs`(首版 55 用例:A 清洗 18 / B 去重 4 / C 排序 8 / D 覆盖 5 / E 接线 4 / F 回写 12 / G 静态 3;当日随修复扩到 **64 用例**,新增 H 组轮换与多条 G 组防退化静态检查)。**全部通过:p5 64/64、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、旧 harness 33/33**。

**真机验证(待用户)**:①发"帮我出几道练习题" → 出的题就是错题本里的题,题号行显示「错题重做」②故意答错一道 → 打开错题本,**条数不增加**、那道题的答案更新为本次误选 ③答对某道错题 → 练习页「换一组」看该题是否沉底 ④错题本清空 → 仍能出题(回退硬编码题库)。

### 0.1 P5 真机反馈修复(2026-09-10):练习页点选项/点「换一组」无响应

用户反馈:编译通过,但进入练习页后点答案选项无反应、点「换一组」也无反应。**用离线脚本复现 service 层后确认:出题/判分/写回全部正常、无任何异常,问题在 UI 层。** 两个独立原因:

**原因 1(点选项无反应)**:选项的 `ForEach` key 是 `${index}_${option}`——`option` 文本永远不变 → key 永远不变 → **ArkUI 复用旧组件不重建**,`getOptionBg`/`getOptionBorder` 算出的新高亮永远不生效。
> 反证:同一张卡片里的「提交答案」按钮不在 ForEach 内,它能正常更新——这说明页面整体是在刷新的,只有 ForEach 子树被复用卡住了。
> 这正是第 4 节早就记过的坑("ForEach key 必须包含所有会变化的渲染字段"),这次又踩了一次。

修复:key 加上渲染状态 `${index}_${option}_${selectedIndex===index?'sel':''}_${isSubmitted?'sub':''}`。

**原因 2(换一组无反应)**:错题源的排序是**确定性**的(设计如此,见上),而它一旦凑够 6 道,profile 层与题库层就完全不参与 → 每次 `generateSession` 返回的是**同一组题**,页面看起来像没响应。

修复:新增候选排除机制。`ErrorBookPracticeSource.buildQuestions(items, count, now, excludeIds)` 先按 id 过滤候选再 round-robin;`generateSession(count, now, excludeIds)` 透传;`AdaptivePracticeViewModel` 记录 `seenQuestionIds`(只累计错题源题目),`resetSession()` 把它们排除掉 → 「换一组」沿排名往下走;当错题源的题全部出完(`collectErrorBookIds` 返回空)则清空累计、重新轮换,避免死循环。

**离线测试**:`ai_agent_p5_test.cjs` 扩到 60 用例(新增 H 组 4 例:排除后返回不同的一组/题量取尽返回空/ViewModel.resetSession 给出不同的一组/选选项后可提交判分)。**全部通过:p5 60/60、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、旧 harness 33/33**。

### 0.2 P5 二次修复(2026-09-10):上面两条改完**仍然**没反应 → 真正的根因

用户复测:点选项依旧无反应、点「换一组」题目依旧不刷新。说明**不是 ForEach key 的问题,而是整张卡片根本没被重新渲染**——`QuestionCard` 自己就没重新执行,它里面的 ForEach 自然没机会重新求值。

**真正根因:`@Builder` 按值传参不刷新。** ArkUI 的规则是"`@Builder` 按值传递参数时,参数值变化不会引起 UI 刷新"。而 `AdaptivePracticeView` 是全项目**唯一**一个把会变的对象当参数传进 builder 的页面:

```
build() → this.PracticeContent(this.questions[this.currentIndex])   // 按值
            → this.QuestionCard(question)                            // 按值
              → this.ActionBar(question) / this.AnswerAnalysis(question)
```

所以整张卡片停在首次渲染:标题、选项高亮、解析、换一组后的新题**全都不会变**。

**对比佐证**:`LearnerProfileView`(P2.5 真机验证过、重置按钮生效)把所有会变的数据放在 `@State` 标量/数组里、builder 直接读,而不是当参数传——所以它刷新正常。`Header()`/`FocusPanel()` 也是无参 builder,同样正常。

**修复**(用本项目已验证过的机制,即 P1 修 STEP 消息用的"ForEach key 变化强制重建"):
1. 新增 `@State cardKeys: string[]`,`syncState()` 末尾算 `buildCardKey()`(含 `id#currentIndex#selectedIndex#isSubmitted`)。**必须放在 syncState 最后**——它依赖前面刚赋的值。
2. `build()` 的 else 分支用 `ForEach(this.cardKeys, ...)` 包住卡片;`Scroll` + `Column` 留在 ForEach **外面**,这样滚动位置不会因为重建而丢失。
3. `PracticeContent` 改成**无参**,内部自己取 `this.questions[this.currentIndex]` 再传给 `QuestionCard`/`ActionBar`(子树由 ForEach 整棵重建,所以内层保留参数是安全的)。
4. 注意 `Scroll` 只接受单一子组件 → `ForEach` 不能直接放在 `Scroll` 里,必须包一层 `Column`。

**若这条仍不生效**,下一站是 `@Observed`(加在 `AdaptivePracticeQuestion` 上)+ `@ObjectLink`(子组件),即 ArkUI 观察深层对象变化的标准方案。

**离线测试**:`ai_agent_p5_test.cjs` 扩到 62 用例(G 组新增 2 条静态检查,防止退回"按值传参 builder":断言存在 `ForEach(this.cardKeys`、`PracticeContent()` 无参、且不再出现 `PracticeContent(question`;以及选项 key 带 `sel`/`sub`)。**全部通过:p5 62/62、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、旧 harness 33/33**。⚠ UI 文件无法离线转译,这两条静态检查只是防退化护栏,**真正的验证只能靠真机**。

### 0.3 P5 三项缺陷修复(2026-09-10):错答案 / 错题本重复 / 气泡文字溢出

**① 生成的题目答案是错的 —— 默认演示题的数据本身写错了(不是代码问题)。**

`MineLocalData.ets` 的 `DEFAULT_ERROR_MODELS` 第 2 条:

```
'函数 f(x)=x³-3x 在区间[-2,2]上的最大值是？', ['-2','0','2','6'], 3, 1, ...
```

`rightQues=3` → 指向选项 `'6'`。但 f(-2)=-2、f(-1)=2、f(1)=-2、f(2)=2,**最大值是 2**(下标 2)——连这条记录自己的 `userNote` 里都写着"最大值2"。改成 `2, 3`。其余 4 条演示题与 12 道硬编码题库题逐题核对**均正确**。

> 教训:演示数据以前只是"错题本里看看",没人做题,所以答案键写错一直没暴露;一旦它成为**出题源**,数据正确性就直接变成产品质量问题。**改动数据源用途时,要连带复核数据本身。**

**② 重复错过的题目在错题本里重复出现 —— 去重键不可靠。**

原因:`appendPracticeMistake` 只按 `records[i].sourceId === question.id` 去重。但:
- 演示题的 id 是 `err_default_${Date.now()}_${i}`,**AppStorage 清空后每次冷启动都重新生成、id 全变** → 跨重启去重失效;
- 演示题被排除在"原地更新"之外(`sourceRecordId=''`),只能走追加;
- 题库题在不同会话里若 id 变化同样失效。

结果:同一道题每答错一次(尤其每次重启后)就多一条。

修复:合并 `refreshPracticeMistake` + `appendPracticeMistake` 为 `recordPracticeMistake`,新增 `findMistakeRecordIndex` —— **优先按记录 id 匹配,匹配不到则按归一化题干匹配**(优先取学生自己的记录,而不是随包的演示记录),命中就原地更新,**匹配不到才追加**。这样无论 id 怎么变,同一道题永远只有一条记录。

**③ AI 回复文字溢出白色气泡 —— `MarkdownText` 段落分支没有宽度约束。**

列表分支用的是 `Column(){...}.layoutWeight(1)` 明确限定宽度让文字换行,**段落分支却是 `Row() { InlineSpans(...) }` 没有约束** → 文本按自身固有宽度测量、撑出气泡。修复:段落分支与列表分支对齐(套 `Column().layoutWeight(1)`);代码块加 `.wordBreak(WordBreak.BREAK_ALL)`(代码常有一整行无空格的长 token,默认换行规则断不开)。
> ⚠ 这一条是**基于代码不对称性的推断**,UI 无法离线验证。

**③ 补修(收到截图后)**:上一轮改完**仍然溢出**。截图显示文字**被左右两侧同时切掉**——这是"子元素比父容器宽 + 父容器把子元素居中"的特征(`Column` 默认 `alignItems` 就是居中)。据此补了三处:

1. **`MarkdownText` 根 `Column` 去掉 `.width('100%')`**(关键)。父气泡是"内容自适应 + maxWidth 上限"的容器,根上写百分比会解析成**父级的约束宽度**(整行宽)而不是气泡实际宽度 → markdown 列比气泡宽 → 被气泡居中 → 两边都溢出。去掉后由气泡把宽度传给内容。
2. **标题分支补 `.width('100%')`**。这是唯一没做宽度约束的分支(`Text(block.lines[0])` 裸奔),会按自身固有宽度把整列撑宽。
3. **`ChatBubble` 气泡 `Column` 加 `.alignItems()`**:AI/ANALYSIS 用 `HorizontalAlign.Start`,其余保持 `Center` —— 即使还有残留超宽,也只会朝一侧溢出,不会再两边都切。

**离线测试**:`ai_agent_p5_test.cjs` 扩到 63 用例(F 组新增"同题以不同题目 id 再答错时不新增重复条目"——正是本次重复问题的回归;演示题那条改为断言"落到同题记录上,不新增重复条目")。**全部通过:p5 63/63、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、旧 harness 33/33**。

### 0.4 会话面板要点两次才出来(2026-09-10)

用户反馈:点左上角 📋 不弹会话切换面板,**要先点右上角 ⚙️ 才会冒出来**(而且是会话面板,不是设置面板)。

**根因:`AiChatView` 把两个 `bindSheet` 链式挂在同一个节点上**(根 `Column`):

```ts
.backgroundColor('#F3F4F6')
  .bindSheet($$this.showSessionSheet, this.SessionSheet(), {...})
  .bindSheet($$this.showSettingsSheet, this.SettingsSheet(), {...});
```

ArkUI 一个节点只能承载一个 `bindSheet`,两个会互相覆盖。实际表现正好对上用户描述:生效的是 `showSettingsSheet` 开关 + `SessionSheet` 内容 → 点 📋 改的是 `showSessionSheet`(没人监听,无反应),点 ⚙️ 才把会话面板弹出来。

**修复**:拆到两个节点 —— 设置面板挪到头部 `Row` 上,会话面板留在根 `Column` 上。

**离线测试**:`ai_agent_p5_test.cjs` 扩到 64 用例(G 组新增静态检查:`.bindSheet(` 出现次数为 2 且两处行号不相邻)。**全部通过:p5 64/64、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、p4 26/26、旧 harness 33/33**。

## 0. 2026-09-09 更新:P4 工程化(已完成,harness 首次 33/33 全绿)

### 0.0 P4 内容:API Key 运行时化 + 重试 + Markdown + 图片三项

**1. API Key 运行时配置(编译产物不含密钥)**
- 新文件 `service/ApiKeyStore.ets`:key 存 AppStorage + `filesDir/aiApiKey.txt`(沿用 SessionStore 模式),提供 getKey/saveKey/clearKey/isConfigured。
- `AiConstants.ets` 删除硬编码 `API_KEY` 常量(源码与编译产物均不再含密钥)。
- `AiService` 注入 ApiKeyStore:buildHeaders 用运行时 key;`chat`/`chatStream` 前置 `ensureKeyConfigured()`,缺 key 抛 `ApiKeyMissingError`(用户友好提示"请点击聊天页右上角 ⚙️ 填写 API Key")。
- `ChatViewModel.buildErrorMessage` 对 ApiKeyMissingError 直接透出友好文案(不带"错误详情"壳)。
- UI(`AiChatView.ets`):头部新增 ⚙️ 按钮 → bindSheet「AI 设置」:显示配置状态、密码型输入框、保存/清除按钮(清除/保存均有 toast)。key 仅存本机沙箱,绝不打包。
- ⚠ 用户须知:①旧 key 已硬编码进历史构建产物,建议到 DeepSeek 开放平台**吊销并轮换**;②升级安装后需在 ⚙️ 里重新填一次 key。

**2. 超时重试 + 流式构造拆分**
- `AiService.doRequest`:MAX_RETRY_COUNT(3)次重试、线性退避(600ms×attempt,构造参数可注入便于测试)、每次尝试前检查 isCancelled(取消立即中止,绝不再发新请求);responseCode===200 直接返回,非 200 视为传输级错误不重试直接抛(400 等重试无意义)。补 sleep 辅助函数。
- `buildRequestBody(messages, stream)` 拆为 `buildStreamRequestBody`(stream=true 字面量)与 `buildPlainRequestBody`,满足 harness「AI请求启用流式响应」静态检查。

**3. Markdown 渲染**
- 新文件 `components/MarkdownText.ets`:轻量解析器(纯 indexOf 无正则,规避 ArkTS 限制)+ 渲染组件。支持:标题(#~######,7# 不算)、围栏代码块、无序列表(-/*)、有序列表(1.~9999.)、段落聚合、行内 **粗体** 与 `代码`(未闭合标记按原文)。导出 parseMarkdown/parseInline 供离线测试;UI 部分 `MarkdownText` 组件 + `@Builder InlineSpans`。
- `ChatBubble.ets`:AI/ANALYSIS 消息走 MarkdownText;USER 消息保持纯 Text(白色气泡不解析);STEP 不变。

**4. 图片搜题三项(PhotoSearchView.ets)**
- 后端 URL 改 `https://10.0.2.2:8000`(测试报告要求 HTTPS)。
- 新文件 `utils/ImageMeta.ets`:魔数检测真实图片格式(JPEG/PNG/GIF87a/89a/WEBP,未知兜底 jpeg);上传 multipart 的 filename 与 Content-Type 按真实格式(不再是写死 image.jpg/jpeg)。
- imageFd 三处关闭:选择新图前、✕ 移除图片时、上传完成(finally)与页面退出(aboutToDisappear),统一 `closeImageFd()`(closeSync 带 try/catch)。

**离线测试**:新增 `_test_artifacts/ai_agent_p4_test.cjs`(26 用例:ApiKeyStore 往返/清空、缺 key 双路径友好报错、重试成功(2 败后第 3 次成)、重试耗尽(4 次)、重试中取消(立即中止不再发)、Markdown 块/行内/未闭合/空串、魔数×5、静态检查×6)。**全部通过:p4 26/26、p0 7/7、p1 22/22、p2 24/24、p3 24/24、p3.5 13/13、旧 harness 33/33(历史上首次全绿,7 项遗留 FAIL 全部修复)**。

**真机验证(待用户)**:①编译 ②⚙️ 输入新 key → 发消息验证 CHAT/ANALYTICS 均通 ③缺 key 时发消息 → 出现"尚未配置 API Key…"友好提示 ④AI 回答中的标题/列表/代码块/粗体正确渲染 ⑤图片搜题:选 PNG 图上传,后端日志确认收到 png MIME;移除图片/退出页面无 fd 泄漏(需后端在线,否则只看文本搜题)。


## 0. 2026-09-09 更新:P3.5 真流式(已完成并离线测试)

**改造 `AiService.chatStream` 为三层降级**:
- **Tier 1 真流式**:`http.requestInStream` + `dataReceive` 事件逐字交付(旧 `request.request` + 事件注册保留在 Tier 2);`dataEnd` 视为完成;完成后冲刷行缓冲尾部。15s 看门狗:请求发出后若从未收到 dataReceive 事件 → 销毁请求、置 `static streamEventsBroken = true`(**设备记忆**:本次运行后续请求直接跳过 Tier 1,不再白等 15s)。
- **Tier 2 缓冲流**:原 `request.request` 逻辑原样保留 — 事件能触发就走事件,`event=false` 时从 `response.result` 解析整段 SSE(你设备上已验证可用)。
- **Tier 3**:`chatWithFallback` 已有非流式兜底(带 isCancelled 护栏)。
- 取消仍即时生效:新增 100ms 轮询取消哨兵,与 dataEnd 竞速,用户点 ⏹ 后 Promise 立即 settle(旧实现要等请求自然结束)。
- 空流(dataEnd 无数据)不算"设备不支持",不置 broken 标记。
- 非 200 抛错,不触发 broken 标记。

**离线测试**:新增 `_test_artifacts/ai_agent_p3_5_test.cjs`(13 用例:真流式 requestInStream 路径/跨包 SSE 重组/看门狗触发+销毁+broken 记忆/后续请求跳过 Tier 1/缓冲解析交付/取消三态/非 200/空流)。**全部通过:p3.5 13/13、p0 7/7、p1 22/22、p2 24/24、p3 24/24、旧回归 26/33(7 个 FAIL 仍为已知旧遗留)**。

**真机验证(待用户)**:①编译 ②发一条 CHAT 消息("什么是导数"):若该设备支持 dataReceive,回答逐字出现;若日志出现 `requestInStream fired no events; using buffered stream from now on`,说明设备不支持,功能不变(整段返回,与 P3 行为一致)③⚠ 若设备上 `requestInStream` 本身报错(API 20 支持,理论上不会),聊天会按旧路径失败 —— 请反馈日志。



## 0. 2026-09-09 更新:P3 体验优化(已完成并离线测试)

### 0.0 P3 内容:取消按钮 + 会话历史管理 UI + agent 轨迹日志

**1. 取消按钮(接线完成)**
- `ChatViewModel.cancelRequest()` 新增:置 userCancelled + runSeq 自增 + 同时 `aiService.cancelRequest()` 与 `agentService.cancel()`;移除当前会话中所有 SENDING 气泡;立即复位 isLoading/isStreaming 并落盘。
- `sendMessage` 引入 runSeq 运行代次:取消后旧运行残留回调(异常/成功)不再写入任何消息(代次不匹配跳过 finally),杜绝"取消后冒出 FAILED 假错误"。
- `AiService.chatWithFallback` 增加 isCancelled 护栏:取消后绝不发兜底新请求。
- `AgentService` 增加 cancelledByUser 护栏:取消后工具轮报错不再触发 plain 降级;deliverFinalAnswer 前置取消检查。
- UI(`AiChatView.ets`):输入区发送按钮在 isLoading/isStreaming 时切换为红色「⏹ 停止」按钮 → `chatVM.cancelRequest()`。

**2. 会话历史管理 UI**
- `AiChatView` 头部 📋 按钮从"直接新建会话"改为打开 **bindSheet 会话面板**:顶部「＋ 新会话」按钮;List 展示每个会话(标题/消息数/更新于时间,当前会话高亮蓝底);点击条目 switchSession;每条 🗑 按钮 → AlertDialog 确认删除 → deleteSession;底部隐私说明。
- `ChatViewModel.createNewSession` 修复 id 撞车 bug:原 `session_${Date.now()}` 在同一毫秒内连建会生成重复 id(测试暴露,真机快速连点也可能触发),现为 `session_${Date.now()}_${自增计数器}`。
- 会话删除后自动落到剩余会话;全删完自动建新会话(沿用已有逻辑)。

**3. agent 轨迹日志**
- 新文件 `service/AgentTraceLogger.ets`:追加写 `filesDir/agentTrace.log`,每行 `[yyyy-MM-dd HH:mm:ss] 内容`,超过 256KB 自动截断重写,失败静默不打断聊天。
- `AgentService` 全程埋点:run 开始/完成(总耗时 ms)、每轮 tool calls 数、每个工具执行耗时(ms)+结果字符数+轮次、去重跳过、降级路径、超时/超轮数/失败原因。

**离线测试**:新增 `_test_artifacts/ai_agent_p3_test.cjs`(24 用例:轨迹写入/追加/截断/无 filesDir 不崩;Agent 轨迹埋点/降级路径;取消三流:无请求 no-op、CHAT 中取消、ANALYTICS 中取消(agent+请求双终止、无 FAILED 残留);会话管理 newSession/switch/delete/落盘)。全部通过:**p3 24/24、p0 7/7、p1 22/22、p2 24/24、旧回归 26/33(7 个 FAIL 仍为已知旧遗留)**。

**真机验证(2026-09-09 已通过)**:编译;⏹ 停止正常(无假错误、可继续对话);📋 面板切换/新建/删除、重启后一致;agentTrace.log 轨迹正常。

## 0. 2026-09-08 更新:P2 学习者档案(已完成并离线测试)

**新增文件 `service/LearnerProfileStore.ets`**:`LearnerProfile` 模型(weakPoints≤8 / strengths≤5 / focusTopics≤6 / examCount / avgScore / totalStudyTime / analysisSummaries≤3×200字,全部源自真实本地数据或助教自己的分析结论,零额外 API 调用);AppStorage + JSON 文件双持久化(`filesDir/learnerProfile.json`,沿用 SessionStore 模式);`updateFromAnalysis`(数据派生字段整体回写)、`recordFocusTopics`(去重合并,上限 6)、`recordAnalysisSummary`(仅存 ≥20 字结论,截断 200 字,保留最近 3 条)、`buildProfilePrompt`(空档案返回 '',非空生成中文档案文本)。

**接入点**:
- `AgentTools.ets`:新增第 7 个工具 `get_learner_profile`(读档案)+ 步骤文案;`get_learning_stats`/`analyze_error_book` 执行后用 `analyzeData` 结果回写档案;`generate_practice` 执行后 `recordFocusTopics(题目)`。
- `ChatViewModel.ets`:`buildContextMessages` 把档案 prompt 作为第二条 system 消息注入(CHAT 快路径与 agent 路径共用);agent system prompt 增加工具 7 规则与"结合历史档案"规则;ANALYTICS agent 回答完成后 `recordAnalysisSummary(回答内容)`。

**离线测试**:新增 `_test_artifacts/ai_agent_p2_test.cjs`(21 用例:prompt 构建/截断/去重/摘要保留/文件往返/损坏数据/工具回写);p0 更新为 7 工具断言。全部通过:p2 21/21、p0 7/7、p1 22/22、旧回归 26/33(7 个 FAIL 仍为已知旧遗留)。

**真机验证(待用户)**:①Bug #4 复验 ②发"分析我的学习情况"→ 档案生成 → 新建会话发"还记得我之前哪些知识点薄弱吗"看是否跨会话记忆 ③档案注入后普通 CHAT 是否仍正常。

**踩坑记录(新增)**:离线测试中 `.ets` 源码在 `new Function` 里执行,只能看到 global 作用域 —— prelude 里的 `const fileIo = {...}` 对被测模块不可见,必须挂 `global.fileIo`。

### 0.1 2026-09-08 第二次更新:P2.5 打磨(已完成并离线测试)

**1. 会话重启后练习按钮可点击**:`SessionStore` 的 `PersistedMessageRecord` 新增 `action?: string`,落盘/恢复时保留 `action`(如 `start_practice`)。修复了"重启后 AI 回答还在但 🚀 开始自适应练习 按钮丢失"的问题。

**2. PRACTICE 本地路径也回写档案**:`ChatViewModel.replyPracticeReady`(本地生成练习、不走工具的路径)与 agent 的 `generate_practice` 行为对齐,生成后 `recordFocusTopics(题目)`。

**3. 档案可视化页面「AI 眼中的你」**:
- 新文件 `components/LearnerProfileView.ets`:NavDestination 页面,展示学习概况(考试次数/平均分/累计学习,含最后更新时间)、薄弱点/优势/近期练习(标签卡片,Flex wrap)、AI 的分析记忆(编号列表);空档案显示引导态("去和 AI 助教聊聊")+ 返回按钮;右上角「重置」按钮(AlertDialog 确认后 `clearProfile()`,新加的 store 方法);底部隐私说明(仅本机保存)。
- 聊天页头部中间右侧新增 👤 按钮(`AiChatView.ets`)→ `pushPathByName('LearnerProfileView')`。
- `route_map.json` 注册 `LearnerProfileView` / `LearnerProfileViewBuilder`;`Index.ets` 导出。
- 注意:`LearnerProfileView` 是 UI 文件,离线 transpile 无法检查,需 DevEco 编译验证。

**离线测试**:`ai_agent_p2_test.cjs` 扩展至 24 用例(新增:clearProfile、SessionStore action 往返、STEP 不落盘)。全部通过:p2 24/24、p0 7/7、p1 22/22、旧回归 26/33(7 个 FAIL 仍为已知旧遗留)。

**真机验证(2026-09-08 已全部通过)**:①DevEco 编译通过 ②聊天页 👤 →「AI 眼中的你」正常展示(有档案/空档案两态)③重置按钮生效 ④重启后历史会话的练习按钮可点击。

## 1. 今日完成:P0(Agent 地基)+ P1(意图路由/练习交接/历史摘要)

### 1.1 P0 — Agent 地基(已完成并验证)

**架构变化**

```
之前: UI按钮 → 写死流程 → 拼prompt → 一次调用 → 假流式(一次回调)
现在: 用户输入 → IntentRouter 分流
        ├─ CHAT       → 直接流式(不带工具,快路径)
        ├─ ANALYTICS  → AgentService ReAct 循环 ──带tools调用──▶ DeepSeek
        │                ▲                                   │
        │                │                             返回 tool_calls?
        │                │                        ┌─是─▶ ToolRegistry 执行本地服务
        │                │                        │      (DataCollect/归因/练习)
        │                └──── tool 结果回填消息列表 ──┘
        │                              └─否─▶ SSE 流式(失败降级非流式)
        └─ PRACTICE   → 本地生成练习会话(0 API 调用)→ 动作卡片 → 练习页
```

**新增文件**

| 文件 | 职责 |
|---|---|
| `features/aiagent/src/main/ets/service/AgentService.ets` | ReAct 循环:最多 5 轮工具调用;总超时看门狗 120s(`AgentTimeoutError`);同一工具单次运行只执行一次(去重防循环);工具轮 max_tokens=1024;`AgentLimitError`(超轮数,用户友好提示) |
| `features/aiagent/src/main/ets/service/AgentTools.ets` | 6 个工具(get_learning_stats / get_exam_history / get_course_progress / get_error_questions / analyze_error_book / generate_practice)+ `buildToolDefinitions()`(真数组,非 JSON 字符串!)+ `getToolStepLabel()` 步骤文案 + `ToolErrorResult` 类 |
| `features/aiagent/src/main/ets/service/SessionStore.ets` | 会话持久化到 `filesDir/aiChatSessions.json`(AppStorage + 文件双写,沿用错题本模式);STEP 消息不落盘;恢复时 role/status 显式转换 |
| `features/aiagent/src/main/ets/service/IntentRouter.ets` | 本地意图分类(纯关键词,零成本):PRACTICE > ANALYTICS > CHAT |
| `features/aiagent/src/main/ets/service/ContextCompressor.ets` | 超窗口历史压缩为确定性本地摘要(用户问题要点,最多 10 条 × 30 字),0 API 调用 |
| `features/aiagent/src/main/ets/service/PracticeSessionStore.ets` | 练习会话交接:agent/路由生成的 `AdaptivePracticeSession` 存 AppStorage,练习页一次性消费 |
| `_test_artifacts/ai_agent_p0_test.cjs` | P0 离线冒烟测试(7 用例) |
| `_test_artifacts/ai_agent_p1_test.cjs` | P1 离线测试(22 用例) |

**修改文件**

| 文件 | 改动要点 |
|---|---|
| `service/AiService.ets` | 拆为 `chat`(非流式,解析 tool_calls,可传 `ApiToolSchema[]`)+ `chatStream`(SSE,dataReceive 事件 + 跨包行缓冲,事件不触发时从 response.result 兜底解析)+ `chatWithFallback`(流式失败/0 字符自动降级非流式,返回交付字符数);删除了旧 `sendChatMessage` |
| `viewmodel/ChatModels.ets` | `AiMessage` + `tool_calls`/`tool_call_id`;新增 `AiToolCall`/`AiResponseResult`/`ApiToolSchema` 等;`MessageRole` + `STEP`;`ChatMessage` + `action`/`stepDone` |
| `viewmodel/ChatViewModel.ets` | `sendMessage` 按 IntentRouter 分流;agent 版 system prompt(工具使用规则 + `{"action":"start_practice"}` 输出协议);`extractAction` 剥离动作 JSON;STEP 消息原地更新(activeStepId);会话恢复/保存;`buildContextMessages(prompt)` + 历史摘要注入 |
| `components/ChatBubble.ets` | STEP 消息渲染(spinner → 完成变绿 ✓,透明背景无边框无时间戳) |
| `components/AiChatView.ets` | 渲染 STEP 消息;AI 消息 `action==='start_practice'` 时显示「🚀 开始自适应练习」按钮跳练习页;ForEach key 含 `stepDone` |
| `components/PhotoSearchView.ets` | `sendChatMessage` → `chatStream`(累计回调) |
| `viewmodel/AdaptivePracticeViewModel.ets` | 构造时优先消费 PracticeSessionStore 的待练习会话,否则本地生成 |

### 1.2 期间修复的 Bug(按时间序,含根因,供回查)

| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | 编译错误 16 个 | ArkTS 严格限制:对象字面量需显式类型、throw 只允许 Error、await 需 async 函数、Color≠string、索引式访问 | payload 全部显式接口化;`applyToolCalls` 改 async;`parseRole/parseStatus` switch 转换;`'transparent'`;`getToolStepLabel()` 函数 |
| 2 | 对话提示"请检查网络连接后重试" | `TOOL_DEFINITIONS_JSON` 是 JSON 字符串,赋给 `tools` 字段序列化后是 `"tools":"[...]"`(字符串),API 期望真数组 → 400 | `tools` 改为 `ApiToolSchema[]`,`buildToolDefinitions()` 逐条构建 |
| 3 | 步骤消息变绿后"分析数据"仍转圈 | 完成回调创建了第二条消息(标绿),原消息永远 `stepDone=false` | ChatViewModel 用 `activeStepId` 原地更新同一条消息;ForEach key 加入 `stepDone` |
| 4 | "什么是导数"(CHAT 快路径)报"AI 未返回有效回答" | 设备上 HarmonyOS http 的 `dataReceive` 事件不触发,流式 0 字符;agent 路径有非流式兜底,CHAT 快路径没有 | `chatStream` 增加 `receivedViaEvent` 检测,事件不触发时从 `response.result` 解析完整 SSE 体;抽 `chatWithFallback` 供 CHAT 与 agent 两条路径共用 |

## 2. 验证状态

### 2.1 离线测试(全部通过)

```bash
node _test_artifacts/ai_agent_p0_test.cjs   # 7/7   SSE解析/工具Schema(7个)/工具执行/兜底/步骤文案
node _test_artifacts/ai_agent_p1_test.cjs   # 22/22 意图路由×10/摘要×4/练习会话往返×8
node _test_artifacts/ai_agent_p2_test.cjs   # 24/24 档案prompt/截断/去重/摘要/往返/工具回写/clearProfile/会话action往返
node _test_artifacts/ai_agent_p3_test.cjs   # 24/24 P3:轨迹日志×4/Agent轨迹埋点×5/取消三流×7/会话管理×4
node _test_artifacts/ai_agent_p3_5_test.cjs # 13/13 P3.5:真流式/跨包重组/看门狗+broken记忆/缓冲解析/取消/非200/空流
node _test_artifacts/ai_agent_p4_test.cjs   # 26/26 P4:ApiKeyStore往返/缺key报错/重试成功/耗尽/取消中止/Markdown解析/魔数/静态检查
node _test_artifacts/ai_agent_p5_test.cjs   # 69/69 P5:错题源清洗/去重/排序/覆盖/接线/写回闭环/气泡宽度算法/静态检查
node _test_artifacts/ai_agent_p6_test.cjs   # 51/51 P6:衰减档位/写入规则/存储往返/选择顺序/冷启动与抛弃/幂等/档案联动/静态/确定性
node _test_artifacts/ai_offline_test_harness.cjs  # 旧回归:33/33 全绿(2026-09-09 P4 修复后首次)
```

### 2.2 真机验证(用户执行)

- ✅ 编译通过、可运行
- ✅ "帮我出几道练习题" → PRACTICE 路径正常(动作卡片 + 练习页同一组题)
- ✅ "分析我的学习情况" → agent 路径正常(步骤消息 → 回答)
- ⏳ "什么是导数" → 曾报"AI 未返回有效回答",**已修复(Bug #4),待重新验证**
- ✅ P2 档案(2026-09-08):编译通过;跨会话记忆生效("还记得我之前哪些知识点薄弱吗"正确回忆);普通 CHAT 正常
- ✅ P2.5 打磨(2026-09-08):编译通过;👤 →「AI 眼中的你」有档案/空档案两态正常;重置按钮生效;重启后历史会话练习按钮可点击
- ✅ P3(2026-09-09 已真机验证):编译通过;⏹ 停止按钮(取消后无假错误、可继续对话);📋 会话面板(切换/新建/删除/重启一致);agentTrace.log 轨迹文件正常
- ✅ P3.5(2026-09-09 已真机验证):编译通过;真流式正常,「什么是导数」Bug #4 复验通过
- ⏳ P4(2026-09-09 实现,待用户验证):⚙️ 填 key 后对话正常;缺 key 友好提示;Markdown 渲染;图片 MIME/fd(图片路径需后端在线)
- 🟡 P5(2026-09-10 实现;5 轮真机反馈修复后,核心交互已通过)
  - ✅ **已验证(2026-09-11 真机)**:编译通过;能进入练习页并操作;**点选项 → 出蓝色高亮**、**点「换一组」→ 换题**(前两轮没修对根因的两条,`@Builder` 按值传参 + keyed ForEach 重建的修法确认有效);⚙️ 会话面板/API 设置面板问题已定位
  - ⏳ **待复测**:
    1. AI 长回复(如"分析我的学习情况")→ 文字不超出白色气泡(**2026-09-16 第四修:气泡与文字同源同宽,待验**)
    2. 出题来自错题本,题号行显示「错题重做」
    3. 故意答错一道 → 打开错题本:**条数不增加**、该题答案更新为本次误选
    4. 答对某道错题 → 下一轮该题沉底
    5. 错题本清空 → 仍能出题(回退硬编码题库)
    6. 左上角 📋 → 立刻弹出会话面板(不需要先点 ⚙️)
  - 📌 气泡溢出(2026-09-16):前三轮修法都只改了气泡自己的宽度上限,**没改文字容器的宽度**,所以一直复现。本次按像素测量定位后,让气泡与内容用同一个确定数值(见 0.16 节),第 4 节记了完整教训。**若仍溢出**,请给一张完整未裁剪的截图(用同一套量像素的方法就能立刻定位)

### 2.3 遗留提示(P3.5/P4 后更新)

- 流式日志语义:若日志出现 `requestInStream fired no events; using buffered stream from now on`,说明该设备不支持 `requestInStream` 事件流,本次运行后续请求直接走缓冲层;`stream done: N chars (event=false)` 仅表示该次走的是 Tier 2 缓冲解析(Tier 1 被设备记忆跳过或已耗尽),功能正常、无逐字效果。若 `event=true` 则是真流式。
- 测试报告 7 项历史遗留(API Key 硬编码/图片 HTTP/MIME/fd/Markdown/流式/重试)已于 P3.5+P4 全部修复,harness 33/33。仅剩用户动作:①DeepSeek 平台吊销轮换旧 key,新 key 填进 ⚙️ ②图片搜题后端需本机在线(HTTPS 自签证书,客户端如需验证需额外配置)。

## 3. 下一步计划(按优先级)

| 阶段 | 内容 | 说明 |
|---|---|---|
| **P3** | ~~体验优化~~ ✅ 已完成并真机验证(2026-09-09) | ①取消按钮(⏹ 停止 + runSeq 防假错误)②会话历史管理 UI(bindSheet 面板)③agent 轨迹日志(agentTrace.log) |
| **P3.5** | ~~真流式~~ ✅ 已完成并真机验证(2026-09-09) | chatStream 三层降级:requestInStream 事件流(15s 看门狗 + 设备记忆 streamEventsBroken)→ 缓冲解析 → 非流式兜底;取消哨兵 100ms 轮询 |
| **P4** | ~~工程化~~ ✅ 已完成,待真机验证(2026-09-09) | ①API Key 运行时配置(⚙️ 设置,编译产物不含密钥;建议吊销轮换旧 key)②MAX_RETRY_COUNT 重试循环 ③Markdown 渲染(MarkdownText)④图片 HTTPS/真实 MIME/fd 三处关闭。harness 33/33 全绿 |
| **P5** | ~~自适应练习·错题驱动题库(方案 1)~~ ✅ 功能完成,真机修复中(2026-09-10) | 错题本成为第一出题源;写回闭环(答错原地更新/答对降权);离线测试 64/64。当日 5 轮真机反馈修复,**最后一轮待复测** |
| **P5.1** | 待办 | ①~~气泡溢出~~ ✅ 2026-09-16 已修(真机待验)②已产生的重复错题记录需要一次性去重逻辑(或手工清理)③`MineLocalData` 其余演示题虽已逐题核对正确,但**数据正确性没有自动护栏**,可考虑加一条演示题答案校验 |
| **P6 方案2** | ~~掌握度累积模型 + 练习历史~~ ✅ 已完成,待真机验证(2026-09-16) | 新文件 `PracticeHistoryStore`(答对 +6/答错 −8/时间衰减 −0~−16/max 60 条);每题提交即落盘;画像层按掌握度升序出题(错题本仍第一出题源);课程进度不再压顶;弱点合流进 `LearnerProfileStore`;练习页累计统计。离线测试 **p6 51/51**,其余 8 套件不回归 |
| **P6 候选** | 自适应练习后续方案 | **方案 3 会话内动态出题**(答对升级/答错降级/追加巩固题;强依赖 P5 的题量)→ **方案 4 LLM 生成变式题**(题库自增长,需校验+缓存)→ **方案 5 间隔重复调度**(1/3/7/15 天;P6 的衰减档位已经就位,可直接用 `effectiveMasteryOf` 做排序)→ **方案 6 练习结论回流聊天 + 断点续练** |
| **后续** | 可选 | ①真服务端代理(P4 的运行时配置是过渡方案,测试报告首选"密钥仅保存在可信后端"的代理模式,答辩前可补)②AgentTools 测试报告要求的其余指标(多轮上下文保持率等)③代码块 copy 按钮、Markdown 表格等渲染增强 |

## 4. 关键经验(ArkTS 踩坑速查)

- 对象字面量必须对应显式接口/类(`arkts-no-untyped-obj-literals`)
- `throw` 只接受 Error 实例(`arkts-limited-throw`)
- 回调含 `await` 时,回调所在方法必须 `async`
- `.backgroundColor()` 传 `'transparent'` 字符串,不能传 `Color.Transparent`(返回类型为 string 时)
- OpenAI tools 参数必须是**真数组**,不能是 JSON 字符串(序列化会变 `"tools":"[...]"`)
- ForEach key 必须包含所有会变化的渲染字段(如 `stepDone`),否则原地更新不刷新。**P5 又踩一次**:练习页选项 key 只用了 `${index}_${option}`,选项文本永不变化 → 选中高亮永远不刷新。判断技巧:**同一个卡片里没被 ForEach 包住的控件(如「提交答案」按钮)能正常更新、而 ForEach 里的不更新时,基本就是这个坑**
- **确定性排序 + "换一组"是天然冲突的**:排序确定 ⇒ 每次返回同一组 ⇒ 用户以为按钮坏了。任何"再来一批"的功能都必须自带排除/轮换机制,且要能自愈(题量取尽后清空排除表重新开始),否则要么重复、要么彻底出不出题
- ⚠ **`@Builder` 按值传参不刷新(最坑的一个)**:`@Builder` 按值传递参数时,**参数值变化不会引起 UI 刷新**。症状是"整块 UI 死在那里":点选项不出高亮、换一组不换题、解析不出来——而且**同一页面里无参的 builder(`Header()`/`FocusPanel()`)是正常的**,极易误判为"事件没绑上"或"ForEach key 写错了"。判定:**如果一块 UI 整个不刷新,先看它是不是被 `this.Xxx(某个对象)` 这种带参 builder 渲染的**;修法是用 keyed `ForEach` 强制重建(P1 已验证的机制),或改无参 builder 直接读 `@State`,或上 `@Observed` + `@ObjectLink` 子组件
- **`Scroll` 只接受单一子组件** → 想用 ForEach 驱动滚动内容重建时,`ForEach` 不能直接放在 `Scroll` 里,必须包一层 `Column`(否则编译期就报错)
- **`state` 的赋值顺序在 `syncState()` 里是有意义的**:派生出来的字段(如 render key)必须放在被依赖字段**之后**赋值,否则算出来的是上一轮的值
- ⚠ **内容自适应容器里不要给子元素写 `width('100%')`**:父容器若只有 `constraintSize({maxWidth:'75%'})` 而没有确定宽度,子元素的百分比会解析成**父级的约束宽度**(往往 = 整行宽)而不是父级实际宽度 → 子元素比父容器宽 → 又因为 `Column`/`Row` 默认 `alignItems` 是**居中**,子元素会**左右两侧同时溢出**。**看截图判断法:两侧同时被切 = 居中对齐的超宽子元素;只有一侧被切 = 起始对齐的超宽子元素。** 修法:去掉根的百分比宽度,让父容器把宽度传下来
  - **⚠⚠ 上面两条都只是半个答案,别再在这条路上打转(2026-09-16 量了截图才收敛,一共修了四轮)**。三轮的模型都是"百分比解析不对",于是反复在 `width('100%')` / `constraintSize({maxWidth})` / 删宽度之间来回改。**真正的问题是:`constraintSize` 只截"挂它的那个组件自己的最终尺寸",它不会变成子元素的测量约束。** 所以气泡确实缩到了 309vp,而里面 `width('100%')` 的 markdown 列**仍然按父级 Row 传下来的可用宽度(≈393)解析** —— 两个宽度压根不是同一个数,差 20%,气泡按 Start 对齐 → 右边那一截掉在白底外面。
    **判断法则(一句话):白底边界和文字墨迹边界不重合时,不是"百分比解析错了",而是"这两个宽度根本不是同一个数"。** 解法只有一个方向:**让气泡和它的内容都由同一个确定数值决定** —— 把宽度作为一个 `@Prop` 从气泡传给内容组件,内容根用 `.width(那个数)`,气泡自己用 `.constraintSize({maxWidth: 那个数 + 内边距})`。**改完这一条,引擎怎么解释百分比都无所谓了。**
  - **怎么"看见"布局真相(这次能收敛全靠它)**:把真机截图解码成像素来量,别再用眼睛估。Node 里 zlib 解 PNG 的 IDAT、按 PNG filter 规则反解(0/1/2/3/4 五种)得到 RGBA,然后逐行扫"最右边的非背景色像素"和"最右边的白色像素"。这两个数一对比,溢出方向、溢出量、气泡真实边界全都变成硬数字(本次:白底止于 x=353,文字到 x=424)。**截图上"看起来像"的比例关系会骗人 —— 我一开始按眼估的比例推算,结论全错。**
  - **对照实验**:`display.getDefaultDisplaySync().width` 是 px,记得 `getUIContext().px2vp()` 换算;算出来的宽度要减去**所有**祖先的横向 padding(外层 Row 的 + 气泡自身的),否则还是会差几个 vp 溢出;`display` 调用要 try/catch 兜底(否则预览/无显示环境直接抛)
  - **`Math.round` 不会帮你兜底**:`resolveBubbleContentWidth` 的期望值写错过一次 —— 我以为 412vp 屏会走到"减去内边距"那支,其实 75% 上限(309)本身就小于预算(340),减法根本没生效。**写死期望值前,先手算一遍每一条分支到底走哪边**,别凭直觉填表
- ⚠ **一个节点只能挂一个 `bindSheet`**:链式写两个会互相覆盖,而且覆盖方式是"**取后一个的 isShow、前一个的 builder**"之类的错位组合——表现为"点 A 按钮没反应,点 B 按钮却弹出 A 的面板"。**任何 `bind*` 修饰符都不要在同一节点上链式重复挂**(P5 期间修 `AiChatView`:📋 会话面板 + ⚙️ 设置面板两个 bindSheet 挂在同一个根 Column 上)。修法:拆到两个不同节点,P5 后静态检查已加护栏
- 持久化反序列化后 string→enum 需显式 switch 转换(ArkTS 无隐式枚举转换)
- 离线测试模式:Node + `ts.transpileModule` + prelude 注入 stub(见 `_test_artifacts/*.cjs`)
- 离线测试 prelude 里的 stub(`fileIo` 等)必须挂 `global.*`——被测模块在 `new Function` 里执行,看不到测试脚本的词法作用域
- 会话/消息 id 若用 `Date.now()` 拼接,同一毫秒连建会撞重复 id → 必须加自增计数器(P3 修复)
- `transpileModule` 会把 `@Observed`/`@Prop` 等装饰器转成 `__decorate(...)` 调用,离线测试 prelude 需 stub 同名函数;`@Builder`/`@Component`/`struct` 无法 transpile → 裁掉 UI 段只测纯函数(见 p4_test 的 `stripMarkdownUi`)
- 被测模块导出用 `const enum` 时 transpile 后枚举被内联,测试脚本取不到枚举名 → 源码用普通 `export enum`(ArkTS 同样支持)
- 测试 stub 的异步函数在 resolve 前要 `await Promise.resolve()` 让出事件循环,否则"先调用、后赋值"(如 bufferedResult)的时序会失效
- `.cjs` 测试不支持顶层 await,异步用例包 async IIFE
- 慎用 sed 批量替换标识符:会把 `const headingLevel = headingLevel(line)` 改成遮蔽自身的死循环错误 → 替换后必须通读一遍
- **给数组元素"过滤空项"会让下标错位**:错题的 `rightQues` 是下标,`option` 里若有一个空串被过滤掉,后面所有选项的下标都前移,`correctIndex` 就指到错误答案上了 → 只能整条丢弃,不能过滤(P5 的 `resolveOptions`)
- **测试里要断言"打乱输入顺序结果不变"**:错题本用 `unshift` 写入(新在前),任何依赖数组下标排序的实现都会随输入顺序漂移;确定性的排序必须是输入顺序无关的全序
- **静态检查别用裸词匹配**:`sourceSrc.indexOf('AppStorage')` 会被文件里解释设计的**注释**命中 → 改成匹配成员访问 `'AppStorage.'`(P5 踩到)。**2026-09-11 又踩升级版**:解释这次修复的注释里直接写着 `constraintSize({ maxWidth: '75%' })`,把"禁止百分比"的静态检查给命中了 → 静态检查前先 `stripArkTsComments()` 剥掉 `/* */` 与 `//`,**匹配代码而不是散文**
- **静态检查想验算法时,把函数从源码里切出来真跑**(比断言"某行存在"强得多):按唯一字面量切出函数体 → `ts.transpileModule`(**必须转译,`new Function` 不认 `const x: number = 1` 这种类型注解**,会报 `Missing initializer in const declaration`)→ `new Function('display', js)(stub)` 注入桩 → 把几个屏宽档的目标值写死在期望里,等于是这套算法的可执行规格(P5 气泡宽度助手)
- **给共享类加字段后,所有 `Object.assign` 式的测试桩都要同步**:p5/harness 的 prelude 里 `AdaptivePracticeQuestion` 是手写桩,不加 `sourceRecordId` 会让 `question.sourceRecordId.length` 直接抛错
- **给 service 加新依赖后,旧 harness 的 prelude 必须补桩**:`new ErrorBookPracticeSource()` 一旦缺桩就是 `TypeError: not a constructor`,会一次性打挂该文件里的全部用例(P5 改完 harness 8 例仍 33/33)。**P6 又踩一次**:服务加了 `PracticeHistoryStore` + `LearnerProfileStore` 两个字段初始化器,harness 与 p5 一并补桩才恢复。**并且:被服务自己 new 出来的类的依赖,必须出现在服务的 prelude 里** —— p6 里真 `ErrorBookPracticeSource` 需要 `ErrorBookPracticeCandidate`,只写在外层测试文件的作用域里没用
- ⚠ **"数量对"不等于"顺序对":淘汰/截断逻辑必须同时断言"留下了哪些"**(2026-09-16 P6 踩到)。`for (i...; result.length < MAX-1; i++)` 在遇到被保护元素之前就满足长度条件而退出 → 保护元素从未被 `continue` 跳过,又被 push 到末尾 → **条目数仍是 60,但被淘汰的是最新那条而不是最旧的**。只看长度的测试全绿,只看"最新的还在吗"也全绿(它在末尾)。修法:先把保护元素**过滤出候选集**再按序取,并补一条"最旧的那条确实没了"的断言
- **写死期望值前先手算一遍**(同上一天连续踩三次:72−8 算成 60、70+6 算成 74、阈值"60"当成 `<=60` 算弱)。种子值就在 `buildProfiles` 里,抄出来代进去再写断言 —— 这类失败永远是**测试期望错**,别急着改代码
- **JS 解析陷阱会让报错行号指向别处**:`new loadService().AdaptivePracticeService()` 这类链式写法在报 `Class constructor ... cannot be invoked without 'new'` 时,行号可能指向**后面某个正确的位置**。看**调用栈**而不是指向行;拆成 `const m = loadService(); new m.AdaptivePracticeService()` 最稳
- **写"两个来源合并"的字段前,先查原字段是不是被整体覆盖**:`LearnerProfileStore.updateFromAnalysis` 是 `profile.weakPoints = data.weakPoints.slice(...)`,朴素的 `push` 会被下一次分析静默抹掉。做法是**拆成两个来源字段 + 一个派生合并视图**(UI/prompt 仍只读合并后的那个),并保证"新来源为空时行为与改造前逐字相同",这样老断言不会动
- ⚠ **`ts.transpileModule` 只转译、不做类型检查**:离线套件全绿**不等于**能编译。P8 有两条 ArkTS 类型错误就是这样溜过去的 —— 629/629 全绿之后真机直接 `COMPILE RESULT:FAIL`。**改完 `.ets` 必须跑一次真机编译,没有任何离线手段能替代。**
- **松散读取 JSON 的两个写法(P8 编译失败后定下)**:从文件/模型回复里读未知结构时 ——
  ① 参数**声明成 `Object` 再 `as Record<string, Object>`**:从一个没有索引签名的 interface(如 `PoolQuestionRecord`)直接 `as Record<string, Object>` 会被判 **`10505001`** 拒绝;
  ② `Array.isArray(x)` 之后的裸下标读会得到 **`any`**,必须**显式 `const items = x as Object[]`** 再逐项 `const item = items[i]`,否则触发 **`arkts-no-any-unknown`(`10605008`)**。
- ⚠ **判「通过/失败」的判据必须拿一份已知失败的样本验一次**(本项目第三次踩):`_build_report.cjs` 按 utf8 读 **UTF-16LE** 的 hvigor 日志,所有 `indexOf` 返回 -1,于是 **BUILD FAILED 被报成「0 error」且退出码 0**。现在它会嗅探 BOM、剥 ANSI、**删掉换行再匹配**(hvigor 在 ~100 列硬换行,甚至会从路径中间断开)、抓完整 `At File: …:\d+:\d+`,并且**构建失败 exit 1**。前两次同类:server 的 `smoke.mjs` 把"没配鉴权 + 聊天坏掉"报成全 SKIP 零 FAIL;自写 YAML 解析器的层级 bug 让它对 compose 给出一份全绿假报告
- **模型的契约要量出来再写代码**:P8 的探针真调后端才发现 `sourceIndex` 被忽略(系统提示里写明了也没用),于是「出处」只能改成"参考了哪几段"。凭想象写解析器会在真实回复上翻车
- **`as` 是编译期断言,运行时什么都不检查**:模型偶发返回 `null` 元素时 `item as Record<...>` 会直接走进属性读崩溃。**构造函数/解析函数开头必须显式判 `null / typeof !== 'object' / Array.isArray`**
- **解析数组时"跳过坏项"会让下标错位**:选项里有一个乱码就用 `continue` 跳过 → 它后面所有选项前移,`correctIndex` 指到别的选项上,**把对的判成错的**。只能整题作废(`readOptions` 全有或全无)。同源旧坑见上面 `resolveOptions` 的"过滤空项"

## 5. 文件索引

- 核心服务:`features/aiagent/src/main/ets/service/` 下 AiService / AgentService / AgentTools / SessionStore / IntentRouter / ContextCompressor / PracticeSessionStore / LearnerProfileStore(P2 新增) / AgentTraceLogger(P3 新增) / ApiKeyStore(P4 新增) / ErrorBookPracticeSource(P5 新增,错题→题目纯转换层;`reviewAgeLevel`/`normalizeKnowledgeTag`/`stableTextHash`/`isCorruptedText` 对外导出,是这些判定的**唯一出处**) / PracticeHistoryStore(P6 新增,累积掌握度+练习历史) / **PracticeQuestionSynthesizer(P8 新增,纯逻辑:prompt 构造 + 响应解析 + 出处) / GeneratedQuestionPool(P8 新增,生成题缓存池,唯一碰 fileIo 的一侧)**
- RAG 层(P7 新增,全在 `service/` 下):**KnowledgeStore**(检索门面/单例/懒构建/预热/草根持久化,唯一入口) / KnowledgeCorpusProvider(与其余 App 的唯一接缝) / KnowledgeChunker / KnowledgeIndex(倒排 BM25) / KnowledgeSnapshotCodec(快照编解码,含 int8+base64 量化) / KnowledgeIndexStore(filesDir 落盘) / KnowledgeEmbedding + KnowledgeEmbeddingProxy(客户端只发文本,不持厂商密钥) / KnowledgeVectorRetriever(RRF 融合) / KnowledgeEvalSet + KnowledgeRetrievalMetrics(33 例评测与指标) / KnowledgeSeedCorpus / KnowledgeCourseCatalog / KnowledgeNotesCatalog + KnowledgeNotesSupport + KnowledgeNotes*×16(讲义正文)
- 数据模型:`features/aiagent/src/main/ets/viewmodel/ChatModels.ets`、`AdaptivePracticeModels.ets`(P8 起 `AdaptivePracticeQuestion` 多一个 `citation`)、`KnowledgeModels.ets`
- UI:`features/aiagent/src/main/ets/components/` 下 AiChatView / ChatBubble / MarkdownText(P4 新增) / AdaptivePracticeView(P8 起在解析下方渲染「参考讲义」) / LearnerProfileView(P2.5 新增,👤 入口在 AiChatView 头部)
- 工具:`features/aiagent/src/main/ets/utils/` 下 AiConstants.ets(URL/模型/超时,MAX_HISTORY_MESSAGES=20,不再含 Key)、ImageMeta.ets(P4 新增,魔数检测)
- 离线测试:`_test_artifacts/` 下 ai_agent_p0/p1/p2/p3/p3_5/p4/p5/p6/p7/p8_test.cjs + ai_offline_test_harness.cjs(33/33),一条命令全跑:`node _test_artifacts/_run_all.cjs`(**629**)。真调脚本(都真打本机后端):`_eval_real_embedding.cjs`(选权重)/ `_e2e_dense_real.cjs`(向量通路)/ `_e2e_generation_real.cjs`(出题通路,P8)。变异脚本:`_mutate_*.cjs`。编译日志解析:`_build_report.cjs`(**构建失败会 exit 1**)
