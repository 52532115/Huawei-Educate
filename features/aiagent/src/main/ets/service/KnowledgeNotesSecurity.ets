/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const INFORMATION_SECURITY = `## 信息安全概论

### 密码学基础

密码学的目标是在不可信信道上实现保密通信。基本模型包含明文、加密算法、密钥、密文、解密算法与攻击者。

对称加密的加解密使用同一密钥，按处理单位分为分组密码与流密码。分组密码把明文分块后逐块加密，工作模式决定块与块之间的关系：电码本模式相同明文块产生相同密文块，会泄露结构信息；密码分组链接模式让前一块密文参与后一块加密，隐藏了统计特征；计数器模式把分组密码改造成流密码，支持并行计算与随机访问。数据加密标准与高级加密标准是最常见的分组密码算法，后者分组长度 128 位，密钥长度可选 128、192 或 256 位。

非对称加密使用一对密钥，公钥公开用于加密或验证签名，私钥保密用于解密或生成签名。其安全性建立在大数分解或离散对数等数学难题之上。非对称加密运算速度远低于对称加密，因此实际协议中用它协商会话密钥，再用对称加密传输大量数据。

哈希函数把任意长度输入映射为定长输出，要求单向性（无法由摘要反推输入）与抗碰撞性（难以找到两个不同输入产生相同摘要）。哈希用于完整性校验、口令存储与数字签名。口令存储必须加盐，否则相同的口令会产生相同摘要，彩虹表可以批量破解；加盐后每个用户的摘要都不同，破解成本显著上升。

消息鉴别码用共享密钥为消息生成校验值，同时保证完整性与来源真实性。数字签名用私钥签名、公钥验证，除完整性外还提供不可否认性。公钥基础设施通过认证中心签发证书把公钥与身份绑定，证书链的信任根是预置在系统或浏览器中的根证书。

### 身份认证与访问控制

身份认证验证用户声称的身份，三要素分别是所知（口令、PIN）、所有（令牌、手机）、所是（指纹、人脸、虹膜）。多因素认证组合不同类型的要素，安全性显著高于重复使用同一类型的多个因素。

口令认证要防暴力破解与撞库，措施包括限制尝试次数、引入延时、检测异常登录地点。挑战响应认证不直接传输口令，而是由服务器发随机挑战、客户端用密钥计算响应，避免口令在信道上暴露。

单点登录让用户一次认证后访问多个系统，代价是认证中心成为关键单点，需要高可用设计。令牌化认证把身份信息封装成有时效的令牌，服务端通过验签确认令牌合法性，无须每次查询用户库。

访问控制的模型有自主访问控制、强制访问控制与基于角色的访问控制。基于角色的模型把权限赋予角色、再把角色赋予用户，管理成本远低于逐用户授权，是实际系统的主流做法。最小权限原则要求主体只获得完成任务所必需的最小权限，职责分离原则要求关键操作由多人分步完成。

### 网络安全、系统安全与应用安全

网络攻击分被动与主动。被动攻击是窃听与流量分析，难以察觉但可通过加密预防；主动攻击包括篡改、伪造、重放与拒绝服务，难以预防但可以通过完整性校验与行为检测发现。

拒绝服务攻击通过耗尽带宽、连接数或计算资源使服务不可用，分布式版本从大量受控主机同时发起。防御手段包括流量清洗、限速、连接数限制与源地址验证。

系统安全关注操作系统与主机层面的防护。缓冲区溢出是最经典的漏洞类型：程序向固定长度缓冲区写入超长数据，覆盖相邻栈帧中的返回地址，从而劫持控制流。防御手段包括边界检查、栈保护金丝雀值、数据执行保护与地址空间布局随机化。提权攻击利用配置错误或内核漏洞从普通用户获得管理员权限，因此要及时打补丁并遵循最小权限配置。

应用安全中，注入类漏洞源于把用户输入当作代码或命令执行。SQL 注入通过构造特殊输入改变查询语义，防御的根本方法是使用参数化查询而不是拼接字符串。跨站脚本把恶意脚本注入页面，防御要对输出进行转义并按内容类型设置策略。跨站请求伪造诱使已登录用户发起非预期请求，防御手段是校验请求来源与使用一次性令牌。

### 安全管理

安全管理把技术手段与制度流程结合起来。安全策略规定组织的安全目标与责任分工；风险评估识别资产、威胁与脆弱性并估算影响；安全审计记录关键操作以便事后追溯。

密码学提供的是可证明的安全性，而系统安全性取决于最薄弱环节。人往往是最薄弱的一环，因此安全意识培训与钓鱼演练是必要的。安全开发生命周期要求在需求阶段就考虑安全需求，在设计阶段做威胁建模，在编码阶段遵循安全编码规范，在测试阶段做渗透测试，在上线后持续监控与响应。`;

const DISTRIBUTED_SYSTEM = `## 分布式系统

### 分布式系统基础与一致性

分布式系统是由通过网络通信的多台计算机组成的系统，这些机器对用户表现为一个整体。其核心挑战来自两个方面：网络不可靠（消息可能丢失、延迟、重复、乱序）和节点可能失效（且难以区分节点失效与网络分区）。

CAP 定理指出，在网络分区发生时，一致性（所有节点看到相同数据）与可用性（每个请求都能得到响应）不可兼得，只能二选一。这解释了为什么不同系统有不同的取舍：银行系统倾向一致性，社交动态倾向可用性。BASE 理论是对可用性的务实补充，即基本可用、软状态、最终一致。

一致性模型从强到弱包括：线性一致性要求所有操作看起来在某个瞬时点原子完成；顺序一致性只要求所有节点看到相同的操作顺序；因果一致性只保证有因果关系的操作顺序正确；最终一致性只保证不再有新写入后副本会收敛。一致性越强，延迟与可用性代价越大。

### 一致性协议与共识

复制是分布式系统实现可靠性、扩展性与低延迟的基础，分主从复制与多主复制。主从复制写请求集中在主节点，读可以分散到从节点，实现简单但主节点是瓶颈与单点；多主复制允许就近写入，但要解决写冲突。

共识算法的目标是让一组节点对某个值达成一致，且在有节点失效时仍能继续工作。Raft 把共识分解为领导者选举、日志复制与安全性三个子问题，通过任期号与选举超时机制选举领导者，领导者把日志条目复制到多数节点后提交。多数派原则保证了任意两个多数派集合必然有交集，因此新领导者一定包含已提交的日志。Paxos 是共识问题的经典解法，但描述较抽象，Raft 可视为其工程化的变体。

分布式事务要保证跨多个服务的操作要么全部成功要么全部回滚。两阶段提交先由协调者询问所有参与者能否提交，全部同意后再统一提交，问题在于协调者失效会导致参与者长期阻塞。三阶段提交引入超时机制缓解阻塞，但仍有窗口。实践中更多采用补充事务（为每个操作定义反向操作，失败时依次补偿）与基于消息的最终一致方案。

共识与分布式事务的代价是延迟，因为它们都需要多轮网络往返与多数派确认。因此设计时要在一致性需求与性能目标之间明确取舍。

### 分布式存储与分布式计算

分布式存储要解决数据分片、副本放置与故障恢复三个问题。分片策略有范围分片（相邻键放在一起，范围查询高效但易热点）与哈希分片（分布均匀但范围查询低效）。一致性哈希把节点与数据都映射到哈希环上，节点增减时只有相邻区间的数据需要迁移，避免了取模方式下几乎全部数据重新分配的问题。

副本放置要考虑故障域隔离，把副本放在不同机架甚至不同机房，避免单点故障同时影响多个副本。仲裁机制用读写副本数之和大于副本总数来保证读写集合必有交集，从而读到最新已提交的值。

分布式计算模型有批处理、流处理与图计算。批处理按固定数据集整体计算，适合离线分析；流处理按事件到达顺序增量计算，适合实时监控；图计算针对迭代式的顶点更新优化。

### 微服务与容器化

微服务把单体应用拆分为多个独立部署的小服务，每个服务围绕一项业务能力构建，独立开发、独立部署、独立扩展。好处是技术栈可以异构、故障可隔离、扩展粒度更细；代价是分布式复杂度上升：需要服务发现、负载均衡、配置管理、链路追踪与集中日志。

服务间通信分同步与异步。同步调用简单直观但会形成调用链，链路中任一环节变慢都会拖垮上游，因此必须设置超时、重试与熔断。熔断器在失败率超阈值时快速失败，避免持续打向已经故障的服务；舱壁模式为不同依赖分配独立线程池或连接池，防止一个依赖耗尽全部资源。异步通信通过消息队列解耦，带来削峰填谷与最终一致，代价是需要处理重复消费与消息顺序。

容器化把应用及其依赖打包成镜像，保证在不同环境中行为一致。容器共享宿主机内核，因此比虚拟机轻量、启动更快。编排系统负责容器的调度、扩缩容、滚动更新与自愈，声明式地描述期望状态并由控制器持续收敛。

服务网格把服务间通信的可靠性、安全与可观测性能力从业务代码中下沉到基础设施层，通过边车代理拦截流量实现流量管理、双向认证与遥测，业务代码无须关心这些横切关注点。

### 云计算基础

云计算把计算、存储与网络资源池化，按需分配并按用量计费。服务模型分三层：基础设施即服务提供虚拟机与网络等基础资源；平台即服务提供运行环境与中间件；软件即服务直接提供可用应用。部署模型有公有云、私有云与混合云。

弹性伸缩是云计算的核心价值，通过监控指标自动增减实例以匹配负载。无服务器计算的进一步抽象让开发者只关心函数逻辑，平台负责扩缩容与运维，但冷启动延迟与执行时长限制是需要权衡的约束。

可用性用几个九衡量，可用性每提高一个九，成本与复杂度都显著上升。可用区是同一地域内电力与网络相互独立的机房，跨可用区部署是获得高可用的基本手段，跨地域部署则用于容灾。`;

/** 信息安全概论与分布式系统：密码学、访问控制、共识与微服务。 */
export function buildSecurityNotes(): SeedLectureInput[] {
  return [
    makeLecture('21', '信息安全概论', '专业方向', INFORMATION_SECURITY),
    makeLecture('22', '分布式系统', '专业方向', DISTRIBUTED_SYSTEM),
  ];
}
