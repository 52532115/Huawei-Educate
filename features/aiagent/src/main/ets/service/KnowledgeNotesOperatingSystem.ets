/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const OPERATING_SYSTEM = `## 操作系统

### 操作系统概述

操作系统是管理计算机硬件与软件资源、为用户和应用程序提供服务的系统软件。它的四大管理职能是处理机管理、存储器管理、设备管理与文件管理，同时提供用户接口。

操作系统的基本特征包括并发、共享、虚拟与异步。并发指多个程序在同一时间间隔内交替执行，注意它与并行的区别：并行要求同一时刻真正同时运行，需要多处理器支持。共享指资源可被多个并发进程共同使用，分为互斥共享与同时共享。虚拟把物理实体变成逻辑对应物，如虚拟内存与虚拟处理器。

操作系统的类型有批处理系统、分时系统与实时系统。分时系统按时间片轮转，追求交互响应；实时系统又分硬实时与软实时，必须在规定时间内完成处理。内核的功能包括中断处理、时钟管理、进程调度与资源管理，按结构可分为宏内核与微内核：宏内核把多数功能放在内核态以获得高性能，微内核只保留最基础的功能以换取可靠性与可扩展性。

### 进程管理

进程是程序的一次执行过程，是系统进行资源分配的基本单位。进程实体由程序段、数据段与进程控制块组成，进程控制块保存进程标识、状态、程序计数器、寄存器现场与资源清单，是操作系统感知进程存在的唯一标志。

#### 进程与线程

线程是进程内的一条执行流，是 CPU 调度和分派的基本单位。线程拥有自己的栈、程序计数器和寄存器现场，但共享所属进程的地址空间、打开的文件与全局数据。

用一句话概括二者的关系：**进程是资源分配的基本单位，线程是处理机调度的基本单位**。因此同一进程内的线程切换只需保存少量寄存器，开销远小于进程切换；但一个线程崩溃可能拖垮整个进程，隔离性不如进程。

线程的实现方式分用户级线程与内核级线程。用户级线程由库在用户态调度，切换快但一个线程阻塞会导致整个进程阻塞，且无法利用多核；内核级线程由操作系统调度，能真正并行但切换开销较大。多线程模型还有多对一、一对一与多对多几种组合。

#### 进程状态与调度

进程的三种基本状态是就绪、运行与阻塞。就绪表示已具备运行条件只等 CPU；运行表示正占用 CPU；阻塞表示因等待某事件而暂停。状态转换中，就绪到运行由调度引起，运行到就绪由时间片用完引起，运行到阻塞由等待事件引起（主动），阻塞到就绪由事件完成引起。注意阻塞不能直接变为运行。

进程调度算法包括：先来先服务（简单但短作业等待长）、短作业优先（平均等待时间最短但可能饥饿）、时间片轮转（适合分时系统，时间片过大会退化为先来先服务）、高响应比优先、多级反馈队列（兼顾短作业与交互性）。调度方式分为非抢占式与抢占式。

进程同步与互斥是并发控制的核心。临界资源一次只允许一个进程使用，访问临界资源的代码称为临界区。同步机制应满足空闲让进、忙则等待、有限等待与让权等待四条准则。实现方法有软件方法（如彼得森算法）、硬件方法（关中断、测试并设置指令）以及信号量机制。

信号量的取值含义是：正值表示可用资源数，负值的绝对值表示等待队列中的进程数。信号量机制中的 P 操作申请资源，V 操作释放资源，必须成对使用且顺序正确。管程把共享变量与操作封装在一起并保证互斥，比裸信号量更不易出错。

经典同步问题包括生产者消费者问题（需要互斥信号量与两个同步信号量）、读者写者问题（读者优先可能导致写者饥饿）、哲学家进餐问题（避免循环等待）。

### 内存管理

内存管理要解决的问题是地址转换、内存分配与回收、内存保护与扩充。程序中的地址分符号地址、逻辑地址与物理地址，编译链接后形成逻辑地址空间，再由地址变换机构映射到物理内存。

连续分配方式有单一连续分配、固定分区分配与动态分区分配。动态分区分配的分配算法有首次适应、最佳适应、最坏适应与邻近适应。首次适应综合性能较好；最佳适应容易产生大量难以利用的外部碎片。

非连续分配方式把程序分成若干块离散存放。页式管理把逻辑地址空间等分为页、物理内存等分为块，页表记录页到块的映射，优点是消除外部碎片、便于内存共享，缺点是最后一次可能产生内部碎片，且仍需两次访存（可用快表缓解）。段式管理按逻辑单位分段，便于共享与保护，但会产生外部碎片。段页式先分段再分页，兼有两者的优点。

虚拟内存基于局部性原理，只把当前需要的部分装入内存，其余留在外存。请求分页是最常见的实现方式，需要页表项中的状态位、访问字段、修改位与外存地址。页面置换算法包括最佳置换（理论最优但不可实现）、先进先出（可能出现贝拉迪异常，即内存增加反而缺页增多）、最近最久未使用与时钟算法。抖动是指页面频繁换入换出导致 CPU 利用率骤降，解决方法有调整工作集、控制多道程序度。

### 文件系统

文件是具有符号名的相关信息的集合。文件的逻辑结构有顺序文件、索引文件与索引顺序文件；物理结构有连续分配、链接分配与索引分配。连续分配支持随机访问但难以扩展；链接分配便于扩展但不支持随机访问；索引分配用索引块记录物理块号，兼顾随机访问与扩展性，多级索引可支持大文件。

目录是文件控制块的集合，用于按名存取。目录结构从单级目录发展到两级目录、树形目录，现代系统用有向无环图支持文件共享。文件的共享方式有基于索引结点的硬链接（多个目录项指向同一索引结点，删除其中一个不影响文件）与基于符号链的软链接（保存路径名，原文件删除后链接失效）。

磁盘空间管理用空闲表、空闲链表或位示图记录空闲块。磁盘访问时间由寻道时间、旋转延迟与传输时间组成，减少寻道是优化的重点。

### 设备管理与死锁

设备管理通过设备控制器与通道完成。输入输出控制方式有程序直接控制、中断驱动、DMA 与通道控制，后两者减少了 CPU 的干预。缓冲技术缓解 CPU 与设备的速度不匹配，分为单缓冲、双缓冲与缓冲池。设备分配要注意设备的独占、共享与虚拟属性，分配不当会造成死锁。

死锁是多个进程因竞争资源而相互等待、无外力干预都无法推进的局面。死锁产生的四个必要条件必须牢记：**互斥条件、请求并保持条件、不可剥夺条件、循环等待条件**。四个条件同时成立才可能死锁，因此破坏其中任意一个即可预防死锁。

处理死锁的策略有四种：预防（破坏必要条件，如资源有序分配破坏循环等待）、避免（用银行家算法在分配前判断是否进入不安全状态）、检测与解除（允许发生再检测，通过资源分配图找环，用剥夺资源或终止进程解除）、以及鸵鸟策略（不处理，多数通用系统的实际选择）。

### 操作系统安全

操作系统安全的目标是保密性、完整性与可用性。安全机制包括身份认证、访问控制、审计与加密。访问控制模型有自主访问控制（由文件属主决定权限）与强制访问控制（由系统按安全级别决定），最小权限原则要求进程只获得完成任务所必需的权限。

常见威胁有病毒、蠕虫、木马、缓冲区溢出与提权攻击。防护手段包括地址空间布局随机化、数据执行保护、系统调用过滤与定期打补丁。可信计算基是系统中负责安全策略实施的全部硬件与软件，其规模越小越容易验证正确性。`;

/** 操作系统：进程线程、调度同步、内存与文件系统、死锁。 */
export function buildOperatingSystemNotes(): SeedLectureInput[] {
  return [
    makeLecture('12', '操作系统', '专业核心', OPERATING_SYSTEM),
  ];
}
