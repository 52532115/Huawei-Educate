/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const GRAPHICS = `## 计算机图形学

### 图形系统与光栅图形学

计算机图形学研究用计算机生成、处理和显示图形。图形系统由图形处理器、显示设备与图形软件构成。显示设备的分辨率、色彩深度与刷新率共同决定显示质量。图形管线把三维场景逐步转换为屏幕像素，主要阶段包括顶点处理、图元装配、光栅化、片元处理与像素输出。

光栅图形学研究如何在离散像素网格上绘制图形。直线生成最经典的是数值微分法与中点画线法，后者只用整数加法即可判断下一个像素位置，适合硬件实现。圆与椭圆的生成利用对称性，只需计算八分之一圆弧再镜像。

多边形填充有扫描线算法与边界填充算法。扫描线算法按行求交点并配对填充，关键在于处理顶点处的奇偶性判断以及活性边表的维护。区域填充从种子点出发向四周扩散，分四连通与八连通两种。

图形裁剪去掉窗口之外的图形部分。直线裁剪中，编码裁剪法把端点按相对窗口的位置编码，通过逻辑判断快速排除完全在外的线段；中点分割法用二分逼近求交点。多边形裁剪要处理裁剪后产生多个多边形以及需要闭合边的情形。

### 几何变换与三维观察

二维变换用齐次坐标与三阶矩阵统一表示：平移、旋转、缩放、对称与错切。引入齐次坐标的目的是把平移这种加法运算也纳入矩阵乘法的统一框架，从而使多个变换可以合并成一个矩阵。

复合变换通过矩阵相乘实现，注意矩阵乘法不满足交换律，因此变换顺序会影响结果。绕任意点旋转的做法是先把该点平移到原点，旋转后再平移回去；绕任意直线旋转同理。

三维变换在四阶矩阵上进行。三维观察流程是：先把世界坐标系的物体变换到观察坐标系，再投影到投影平面，最后映射到设备坐标。投影分平行投影与透视投影，平行投影保持平行线仍然平行，常用于工程图；透视投影产生近大远小的效果，更符合视觉习惯，其关键参数是视点与视平面距离。

消隐用于确定哪些表面可见，常用算法有画家算法（按深度排序后由远及近绘制）、Z 缓冲算法（为每个像素记录当前最近深度）、背面剔除与扫描线算法。

### 曲线曲面与真实感图形

曲线曲面用少量控制点描述光滑形状。贝塞尔曲线由控制多边形决定，具有端点插值、凸包性与整体性，缺点是没有局部修改能力——移动一个控制点会改变整条曲线。B 样条曲线把影响范围限制在局部，因而更适合交互式设计。非均匀有理 B 样条进一步引入权因子，能精确表示圆锥曲线，是工业建模的主流表示。

真实感图形追求接近真实光照效果。光照模型分局部光照模型与全局光照模型：局部模型只考虑光源直接照射与表面反射，包含环境光、漫反射与镜面反射三项；全局模型还考虑物体之间的光能传递，能产生阴影、反射与折射效果。

光照计算需要表面法向量，多边形法向量由两条边的叉积求得，顶点法向量取相邻面法向量的平均以获得平滑着色。着色方式中，平面着色每个面一个颜色，光滑但计算简单；高洛德着色在顶点算颜色并插值；冯着色在片元上插值法向后再算光照，效果最好。

纹理映射把二维图像贴到三维表面，需要处理纹理坐标、重复方式与走样问题。走样指离散采样导致的锯齿与细节丢失，反走样手段包括提高采样率与区域平均，后者用像素覆盖率决定颜色权重。

### 计算机动画

动画的基本原理是快速连续显示略有差别的画面产生运动错觉。动画分二维与三维，三维动画的流程是建模、材质、骨骼绑定、动画、渲染与合成。

关键帧动画只需给出关键姿态，中间帧由插值生成。插值方式决定运动质感：线性插值速度恒定但起停生硬；样条插值能给出平滑的加减速。物体运动还应遵循物理规律，例如自由落体应符合重力加速度，抛体运动轨迹应是抛物线，这类基于物理的动画通过求解运动方程产生更自然的结果。

粒子系统适合表现火焰、烟雾、水流等不规则形体，它不描述整体形状，而是模拟大量粒子的生成、运动与消亡。过程式动画用规则或程序生成动作，例如用噪声函数生成自然抖动。

实时渲染要求每帧在极短时间内完成绘制，因此通过层次细节、遮挡剔除、批处理与着色器优化来平衡画质与帧率。`;

const EMBEDDED = `## 嵌入式系统

### 嵌入式系统概述与处理器架构

嵌入式系统是嵌入到对象体系中、执行特定功能的专用计算机系统，特点是专用性强、资源受限、实时性要求高、可靠性要求高、功耗敏感。它由硬件层、驱动层、操作系统层与应用层构成。

嵌入式处理器分几类：微控制器把 CPU、存储器与多种外设集成在一块芯片上，成本低、功耗小，适合控制类应用；微处理器性能更强但需要外扩存储与外设，适合计算密集的应用；数字信号处理器针对乘加运算优化，适合音视频编解码；可编程逻辑器件通过硬件描述语言配置逻辑，适合并行度高或需要快速迭代的场合。

处理器架构中，精简指令集指令定长、流水线规整、能耗较低，在嵌入式领域占主导。冯·诺依曼结构与哈佛结构的区别在于指令与数据是否共用存储通路，后者取指与取数可并行，更适合实时处理。

### 嵌入式操作系统与实时系统

嵌入式操作系统的职责是任务调度、内存管理、中断处理、设备驱动与文件系统支持。与通用操作系统相比，它更强调可裁剪、可移植、实时性与可靠性。

任务调度分为抢占式与非抢占式。优先级反转是实时系统中必须解决的问题：低优先级任务持有高优先级任务所需的锁，导致中等优先级任务先运行，高优先级任务被间接阻塞。解决办法是优先级继承（持有锁的低优先级任务临时提升到等待者的优先级）或优先级天花板（进入临界区即提升到可能访问该资源任务的最高优先级）。

实时系统的正确性不仅取决于计算结果，还取决于产生结果的时间。硬实时必须在截止时间前完成，否则视为失败，如刹车控制与飞行控制；软实时允许偶尔超时，只是体验下降，如视频播放。实时性的度量指标包括中断延迟、任务切换时间与最坏执行时间。

常见的嵌入式操作系统有 FreeRTOS、RT-Thread、Zephyr 以及嵌入式 Linux。选型时要综合考虑实时性、内核体积、生态与授权成本。

### 接口技术与驱动开发

嵌入式系统通过多种接口连接外设。通用输入输出接口是最简单的形式，通过配置寄存器方向位控制引脚的电平读写。串行通信接口中，UART 异步收发不需要时钟线，双方约定波特率即可；SPI 用四根线实现高速全双工通信，主从结构且片选决定通信对象；I2C 只用两根线，支持多主多从并通过地址寻址，适合连接传感器。

模拟量处理依赖模数转换与数模转换，采样率与精度是关键参数，采样必须先经过抗混叠滤波，否则高频信号会混叠成低频假信号。定时器用于产生精确延时、测量脉宽与生成脉宽调制波形，后者广泛用于电机调速与亮度控制。

驱动开发的层次是：硬件抽象层屏蔽芯片差异，驱动层实现设备的具体操作，接口层向应用提供统一调用。寄存器操作是驱动开发的基础，通常需要读写特定地址的内存映射寄存器来配置外设，位操作要避免影响同一寄存器中其他功能位。

中断服务程序应尽可能短，只做必要处理并把耗时工作交给任务或下半部机制，否则会阻塞其他中断并恶化实时性。中断与主流程共享数据时必须使用原子操作或临界区保护。

### 物联网应用

物联网的架构分感知层、网络层与应用层。感知层负责采集数据，网络层负责传输，应用层负责数据处理与业务呈现。

物联网通信协议中，MQTT 采用发布订阅模型，通过主题路由消息，开销小、支持长连接与服务质量等级，适合低带宽 unreliable 网络；CoAP 基于 UDP，采用请求响应模型，适合极受限设备。

嵌入式设备的功耗管理手段包括休眠模式切换、动态电压频率调节、外设按需使能以及通信占空比控制。电池供电设备的续航往往由平均功耗而非峰值功耗决定，因此让设备尽可能长时间处于低功耗状态比优化运算时间更有效。

安全是物联网的关键短板：设备算力有限导致难以使用重量级加密，固件更新通道若不带签名校验会成为攻击入口，默认密码与开放调试接口是常见的失守点。因此需要安全启动、固件签名、设备身份认证与最小权限设计。`;

/** 计算机图形学与嵌入式系统：渲染管线、光照、实时任务与驱动。 */
export function buildGraphicsNotes(): SeedLectureInput[] {
  return [
    makeLecture('19', '计算机图形学', '专业方向', GRAPHICS),
    makeLecture('20', '嵌入式系统', '专业方向', EMBEDDED),
  ];
}
