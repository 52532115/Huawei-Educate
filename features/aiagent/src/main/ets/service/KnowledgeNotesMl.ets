/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const MACHINE_LEARNING = `## 机器学习

### 机器学习基础

机器学习的核心思想是从数据中自动发现规律，并用学到的规律对未见数据作出预测。按学习方式分三类：监督学习从带标签数据学习输入到输出的映射；无监督学习在无标签数据中发现结构；强化学习通过与环境交互获得奖励信号来优化策略。

模型在训练集上表现好而在测试集上表现差称为过拟合，本质是模型把训练数据的噪声也当成规律学到了。与之相对的是欠拟合，即模型过于简单无法捕捉数据规律。判断方法是比较训练误差与验证误差：两者都高是欠拟合，训练低而验证高是过拟合。

偏差方差分解把泛化误差拆为偏差、方差与不可约噪声：偏差衡量模型的系统性偏离，方差衡量模型对训练集扰动的敏感度。简单模型偏差大方差小，复杂模型偏差小方差大，因此存在最优复杂度。

应对过拟合的手段有：增加训练数据、降低模型复杂度、正则化（在损失函数中加入参数范数惩罚）、早停（验证误差不再下降时停止训练）、集成方法、以及丢弃法（训练时随机失活部分神经元）。

### 监督学习

线性回归用一条直线（或超平面）拟合连续目标，通过最小化均方误差求解，有解析解也可用梯度下降迭代求解。逻辑回归在线性组合外接 Sigmoid 函数把输出压缩到 0 到 1 之间，用于二分类，其损失函数是交叉熵，可由最大似然估计导出。

决策树递归地选择最优特征进行划分，划分准则有信息增益（偏向取值多的特征）、信息增益率（对取值多的特征作惩罚）与基尼指数。决策树可解释性强但容易过拟合，因此需要预剪枝（边建边判断是否继续分裂）或后剪枝（建完再回剪）。

支持向量机的思想是寻找能最大化类别间隔的分隔超平面，只有位于间隔边界上的支持向量决定最终模型。对于线性不可分的数据，通过核函数把样本映射到高维空间使问题变得线性可分，这就是核技巧，它避免了显式计算高维坐标。

朴素贝叶斯基于贝叶斯公式与特征条件独立假设，计算高效且对小数据表现稳健，常用于文本分类。K 近邻不训练模型，预测时找最近的 k 个样本投票或取平均，因此预测代价随数据量线性增长。

### 无监督学习

聚类把样本分成若干组，使组内相似度高、组间相似度低。K 均值算法交替执行“分配样本到最近中心”与“更新中心为组内均值”两步，需预先指定簇数且对初始中心敏感；层次聚类生成聚类树，可通过切割树得到不同粒度的划分；密度聚类按邻域密度扩张簇，能发现任意形状的簇并识别噪声点。

降维在保留主要信息的前提下减少特征数量。主成分分析寻找数据方差最大的正交方向，把数据投影到这些方向上；奇异值分解可用于矩阵降维与潜在语义分析。降维的价值在于缓解维度灾难、压缩存储并便于可视化。

异常检测识别与多数样本显著不同的点，方法包括基于统计分布、基于距离、基于密度与基于重构误差。由于异常样本稀少，这类问题常按无监督方式处理。

### 特征工程与模型评估

特征工程往往比模型选择更能决定最终效果。常见处理包括：缺失值填充（均值、中位数、模型预测或标记为独立类别）；类别特征编码（独热编码适合低基数，目标编码与频率编码适合高基数）；数值特征标准化（零均值单位方差适合需要距离计算的模型，归一化到固定区间适合有界输入要求的模型）；对数变换处理长尾分布。

特征选择有过滤法（按统计量排序后筛选）、包裹法（用模型效果评价特征子集）与嵌入法（模型训练中自动完成，如 L1 正则把无用特征系数压到零）。特征交叉可以构造出原始特征无法表达的交互关系。

分类任务的评估指标中，准确率在类别不平衡时具有误导性。精确率衡量预测为正的样本中真正为正的比例，召回率衡量真正为正的样本中被找出的比例，二者往往此消彼长，F1 值是它们的调和平均。

混淆矩阵给出真阳、假阳、真阴、假阴四个计数，是计算上述指标的基础。受试者工作特征曲线以假阳率为横轴、真阳率为纵轴，曲线下面积衡量模型排序能力，且不受分类阈值影响。回归任务用均方误差、平均绝对误差与决定系数评估。

交叉验证把数据分成 k 份，轮流用其中一份验证、其余训练，取平均结果，比单次划分更能反映泛化能力，也便于选择超参数。数据划分必须保证训练集与测试集不重叠，且预处理参数只能从训练集学习，否则会造成数据泄露——即测试信息提前进入训练过程，使评估结果虚高。

### 深度学习入门

神经网络由多层神经元组成，每个神经元对输入做加权求和后经激活函数输出。没有激活函数时多层网络等价于单层线性变换，因此激活函数是引入非线性的关键。常见激活函数有 Sigmoid（易饱和导致梯度消失）、Tanh 与 ReLU（计算简单且缓解梯度消失，但存在神经元失活问题）。

反向传播用链式法则从输出层向前逐层计算损失对各参数的梯度，再沿负梯度方向更新参数。梯度消失与梯度爆炸是深层网络的固有问题：前者使浅层参数几乎不更新，后者使训练发散；缓解手段包括使用 ReLU、批归一化、残差连接与梯度裁剪。

卷积神经网络利用局部连接与权值共享大幅减少参数量，适合处理具有网格结构的数据。卷积层提取局部特征，池化层降低空间分辨率并增强平移不变性，全连接层完成分类。循环神经网络引入状态传递以处理序列，其变体通过门控机制缓解长程依赖问题。注意力机制让模型直接关注序列中的相关位置，避免了逐级传递信息造成的衰减。

训练技巧中，批归一化加速收敛并起到一定正则作用；丢弃法在训练时随机失活神经元以增强泛化；学习率调度让学习率随训练进程衰减；迁移学习复用在大规模数据上预训练的模型参数，在小数据集上微调，显著降低了对标注数据量的要求。

### 常用框架

深度学习框架提供自动求导、张量运算与硬件加速能力。计算图分静态与动态两种：静态图先定义后执行，便于整体优化；动态图边定义边执行，便于调试。多数框架已支持两种模式的结合。

数据处理管道负责批量加载、打乱与增强，其吞吐量常成为训练瓶颈，因此需要并行预取与缓存优化。模型部署时常用推理优化手段包括算子融合、量化（把浮点参数压缩为低精度整数以减少内存与计算量）、剪枝（去掉冗余连接）与蒸馏（用大模型指导小模型训练）。`;

/** 机器学习：监督与无监督学习、特征工程、评估指标与深度学习。 */
export function buildMachineLearningNotes(): SeedLectureInput[] {
  return [
    makeLecture('23', '机器学习', '专业方向', MACHINE_LEARNING),
  ];
}
