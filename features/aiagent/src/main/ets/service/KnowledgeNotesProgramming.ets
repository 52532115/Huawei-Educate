/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const C_LANGUAGE = `## 程序设计基础（C语言）

### 数据类型与运算符

C 语言的基本数据类型包括整型 int、字符型 char、浮点型 float 与双精度型 double，以及 short、long、unsigned 等修饰符构成的变体。每种类型占用的字节数依赖具体平台，可以用 sizeof 运算符查询。

整型除法会截断小数部分，因此两个整数相除要得到小数结果必须至少一方为浮点型。取余运算符只能用于整数。自增自减运算符在表达式中参与运算时，前置与后置的行为不同：前置先自增再取值，后置先取值再自增。

类型转换分自动转换与强制转换。自动转换遵循由低精度向高精度提升的规则；强制转换在表达式前加括号写目标类型，可能丢失精度，需要明确知道自己在做什么。

### 控制结构

顺序、选择、循环是程序的三类基本结构。选择结构用 if-else 与 switch 实现，switch 的每个分支末尾必须写 break，否则会发生“贯穿”执行下一个分支。

循环结构有 for、while、do-while 三种。for 适合循环次数已知的情形，while 适合条件驱动的循环，do-while 至少执行一次循环体。break 结束整个循环，continue 只结束本次迭代。

编写循环时要特别注意边界条件与死循环，例如判断条件写成赋值表达式而非比较表达式时，编译器通常只给警告，运行时会产生难以排查的错误。

### 函数与作用域

函数把一段可复用的逻辑封装起来，由返回类型、函数名、参数列表和函数体组成。C 语言的参数传递默认是值传递，函数内修改形参不会影响实参。要修改调用方的变量必须传递地址，即用指针作参数。

数组作为参数传递时会退化为指向首元素的指针，因此函数内部无法用 sizeof 求得数组长度，必须额外传递长度参数。

变量的作用域与生存期要区分清楚：局部变量在函数内有效，全局变量在整个程序内有效；用 static 修饰的局部变量只初始化一次且生存期延续到程序结束，用 static 修饰的全局变量只在当前源文件可见。

递归是函数直接或间接调用自身，必须包含终止条件，否则会栈溢出。递归适合处理树形结构、分治与回溯类问题。

### 数组与指针

数组是一段连续内存中同类型元素的集合，下标从零开始。数组名在多数场合会被转换为指向首元素的指针，因此不能对数组名赋值。

指针保存的是内存地址，通过取地址运算符取得变量地址，通过解引用运算符访问指针指向的值。指针与数组关系密切：数组名即首元素地址，指针加一表示跳过一整个元素而非一个字节。

指针的常见用途包括：在函数间共享数据、动态内存分配、构建链表与树等动态数据结构、以及作为函数指针实现回调。多级指针保存指针的地址。

字符串在 C 语言中是以空字符结尾的字符数组，标准库提供长度计算、复制、拼接、比较等函数，使用时必须确保目标缓冲区足够大，否则会造成缓冲区溢出。动态内存用分配与释放函数管理，申请后必须释放，且不能重复释放或释放后继续使用。

### 结构体与文件操作

结构体把不同类型的数据组合成一个整体，用点运算符访问成员，用指针访问时用箭头运算符。结构体可以嵌套、可以作为函数参数与返回值、可以构成数组。共用体让多个成员共享同一段内存空间，枚举为一组整型常量命名。

typedef 可以为已有类型取别名，常用于简化结构体类型的书写。

文件操作通过文件指针完成，基本流程是打开文件、读写、关闭。打开时要指定模式：只读、只写、追加等。读写函数分按字符、按字符串、按格式化、按数据块几种粒度。文件使用完毕必须关闭，否则缓冲区数据可能未写入磁盘。判断文件是否成功打开是必做的检查，忽略这一步会让后续操作直接崩溃。`;

const JAVA = `## 面向对象程序设计（Java）

### 类与对象

类是对一类事物的抽象，包含属性（成员变量）与行为（方法）。对象是类的实例，通过 new 关键字创建。构造方法与类同名且无返回类型，用于初始化对象状态。若未显式定义构造方法，编译器会提供默认无参构造方法；一旦定义了带参构造方法，默认构造方法就不再自动生成。

封装要求把成员变量声明为私有，通过公有的取值与赋值方法访问，从而在方法中加入校验逻辑。this 关键字指代当前对象，用于区分成员变量与同名的局部变量或参数。

static 修饰的成员属于类而不属于对象，在类加载时初始化，可以通过类名直接访问。类变量被所有对象共享，类方法中不能直接访问实例成员。

### 继承与多态

继承用关键字 extends 表示，子类自动获得父类的非私有成员。Java 只支持单继承，一个类只能有一个直接父类，但可以实现多个接口。父类引用指向子类对象称为向上转型，这是多态的基础；向下转型需要显式强制转换，并且应该在转换前用类型判断避免运行时异常。

方法重载发生在同一个类中，要求方法名相同而参数列表不同，与返回值无关。方法重写发生在子类中，要求方法签名相同，用于改变父类的行为。重写方法的访问权限不能比父类更严格。

多态的含义是同一方法调用在不同对象上产生不同行为。运行时根据对象的实际类型决定调用哪个重写方法，这一机制称为动态绑定。抽象类用 abstract 修饰，不能实例化，可以包含抽象方法与具体方法，适合表达“是什么”的继承关系。

### 接口与内部类

接口定义一组行为规范，其中的方法默认是公有的抽象方法，字段默认是公有静态常量。一个类实现接口必须实现其全部抽象方法。接口可以多继承接口，从 Java 8 起接口还可以包含默认方法与静态方法，用于在不破坏既有实现的前提下扩展接口。

抽象类与接口的选择原则是：需要共享状态或代码复用时用抽象类，只需要约定能力时用接口。

内部类定义在另一个类的内部，可以访问外部类的成员。匿名内部类常用于一次性实现接口或继承类的场景，Lambda 表达式在函数式接口上可以进一步简化这种写法。

### 异常处理

异常是可被捕获处理的运行期问题，错误是程序无法处理的严重问题。异常体系以 Throwable 为根，分为受检异常与运行时异常：受检异常必须显式处理或声明抛出，运行时异常通常来自逻辑缺陷，不强制处理。

异常处理用 try-catch-finally 结构：try 中放可能抛异常的代码，catch 按类型匹配并处理，finally 中的代码无论是否发生异常都会执行，常用于释放资源。多个 catch 块要按由具体到宽泛的顺序排列，否则子类异常的捕获块会被父类异常提前拦截而变成不可达代码。

抛出异常用 throw，在方法签名上声明用 throws。自定义异常通常继承 Exception 或 RuntimeException，使业务错误可以被精确区分。

### 集合框架

集合框架分两大接口：Collection 与 Map。Collection 下有 List、Set、Queue 三条主线。

List 有序且允许重复。ArrayList 基于动态数组，随机访问快而中间插入删除慢；LinkedList 基于双向链表，插入删除快而随机访问慢，也可用作队列和栈。

Set 不允许重复元素。HashSet 基于哈希表，元素无序，要求元素正确重写哈希码与相等判断方法；TreeSet 基于红黑树，元素按自然顺序或比较器排序。

Map 存储键值对。HashMap 允许一个空键与多个空值，不保证顺序；TreeMap 按键排序；LinkedHashMap 保留插入顺序。哈希类集合的性能取决于哈希函数是否均匀，重写相等判断时必须同时重写哈希码计算，否则会出现“逻辑上相等却存了两份”的问题。

### 泛型与 IO 流

泛型的意义是把类型检查从运行时提前到编译期，并避免强制类型转换。泛型类与泛型方法的类型参数在编译后被擦除，因此不能对泛型类型使用 instanceof，也不能创建泛型数组。通配符用于放宽类型约束。

IO 流按数据单位分为字节流与字符流，按方向分为输入流与输出流。字节流以 Stream 结尾，适合处理二进制数据；字符流以 Reader 与 Writer 结尾，适合处理文本并处理编码问题。缓冲流通过减少实际读写次数显著提升性能。

文件读写应使用自动关闭语法或在 finally 中关闭，避免资源泄漏。对象序列化把对象状态转换为字节序列，要求类实现序列化接口。

### 多线程与网络编程

线程是程序执行的最小单位，创建线程有两种方式：继承线程类重写运行方法，或实现可运行接口再交给线程。推荐后者，因为 Java 是单继承，实现接口保留了继承其他类的余地。

线程的生命周期包括新建、就绪、运行、阻塞、终止等状态。线程同步用同步代码块或同步方法保证同一时刻只有一个线程访问共享资源，等待与唤醒机制用于线程间协作。死锁的产生需要四个条件同时成立，避免死锁的常见做法是按固定顺序获取锁、减小锁的粒度、使用带超时的尝试加锁。

网络编程基于套接字。TCP 通信的服务器端流程是创建服务端套接字、绑定端口、循环接受连接、通过输入输出流读写数据；客户端流程是创建套接字连接指定地址与端口、读写数据、关闭连接。UDP 通信使用数据报套接字，无需建立连接，但需自行处理丢包与顺序问题。`;

/** 编程基础：C 语言与 Java，覆盖指针、面向对象、集合与并发。 */
export function buildProgrammingNotes(): SeedLectureInput[] {
  return [
    makeLecture('7', '程序设计基础（C语言）', '编程基础', C_LANGUAGE),
    makeLecture('8', '面向对象程序设计（Java）', '编程基础', JAVA),
  ];
}
