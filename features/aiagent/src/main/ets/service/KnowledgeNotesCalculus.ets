/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const CALCULUS_1 = `## 高等数学（上）

### 函数与极限

函数是微积分研究的对象。设数集 D 是实数集的子集，若对 D 中每个 x 按对应法则 f 都有唯一确定的 y 与之对应，则称 f 为定义在 D 上的函数。定义域、对应法则、值域构成函数的三要素。

数列极限的描述性定义是：当 n 无限增大时，数列项 a(n) 无限接近于常数 A，则称 A 为该数列的极限，记作 lim(n→∞) a(n) = A。严格定义（ε-N 语言）为：任给 ε>0，总存在正整数 N，使当 n>N 时恒有 |a(n)-A|<ε。函数极限 lim(x→x0) f(x)=A 用 ε-δ 语言表述：任给 ε>0，存在 δ>0，使当 0<|x-x0|<δ 时恒有 |f(x)-A|<ε。

极限存在的充要条件是左极限与右极限存在且相等。极限的四则运算法则要求各部分极限都存在，且在商的法则中分母极限不为零。夹逼准则用于放缩后求极限，单调有界准则用于证明递推数列收敛。

#### 等价无穷小与两个重要极限

两个重要极限是：lim(x→0) sin(x)/x = 1，以及 lim(x→∞) (1+1/x)^x = e（也可写作 lim(x→0)(1+x)^(1/x)=e）。

常见等价无穷小（当 x→0 时）：sin(x)~x，tan(x)~x，arcsin(x)~x，arctan(x)~x，1-cos(x)~x²/2，ln(1+x)~x，e^x-1~x，(1+x)^a-1~a·x。

使用等价无穷小替换有一条铁律：只能替换乘除因子，不能直接替换加减项中的一项。例如求 lim(x→0) sin(2x)/x 时，sin(2x) 与 2x 是等价无穷小，替换后得 2x/x = 2，系数必须保留，漏掉系数是最常见的错误。

### 导数与微分

#### 导数的定义与几何意义

导数定义为函数改变量与自变量改变量之比的极限：f'(x0) = lim(Δx→0) [f(x0+Δx)-f(x0)]/Δx。导数存在也称函数在该点可导，可导必连续，连续不一定可导（例如 y=|x| 在 x=0 处连续但不可导）。

几何上，f'(x0) 是曲线 y=f(x) 在点 (x0, f(x0)) 处切线的斜率。物理上，位移对时间的导数是速度。微分 dy = f'(x)dx 描述函数的局部线性近似，可微与可导在一元函数中等价。

#### 求导法则与高阶导数

基本导数公式要熟记：(x^a)' = a·x^(a-1)，(sin x)' = cos x，(cos x)' = -sin x，(ln x)' = 1/x，(e^x)' = e^x。

四则运算：和差直接求导；乘法 (uv)' = u'v + uv'；除法 (u/v)' = (u'v - uv')/v²。

复合函数求导用链式法则：若 y=f(u)，u=g(x)，则 dy/dx = f'(u)·g'(x)，即由外向内逐层求导再相乘。隐函数求导是对等式两边同时关于 x 求导，把 y 看作 x 的函数，再解出 y'。参数方程确定的函数用 dy/dx = (dy/dt)/(dx/dt)。

高阶导数是导数的导数，记作 f''(x)。二阶导数用于判断函数图像的凹凸性：f''(x)>0 时曲线凹（下凸），f''(x)<0 时曲线凸（上凸），凹凸性发生改变的点称为拐点。

#### 微分中值定理与洛必达法则

罗尔定理：函数在闭区间连续、开区间可导，且两端点函数值相等，则区间内至少存在一点使导数为零。拉格朗日中值定理把该结论推广为 f(b)-f(a) = f'(ξ)(b-a)，它是用导数研究函数整体性质的基础。柯西中值定理则给出两个函数增量之比与导数之比的联系。

洛必达法则用于处理 0/0 型与 ∞/∞ 型未定式：在满足可导条件时，函数之比的极限等于导数之比的极限。使用前必须先验证类型，其他未定式（0·∞、∞-∞、1^∞ 等）要通过变形或取对数化成这两种类型再用。

#### 导数的应用

判断单调性的方法是：在区间内 f'(x)>0 则函数单调递增，f'(x)<0 则单调递减。极值点必是驻点（f'(x)=0 的点）或不可导点，但驻点不一定是极值点，还需判断导数在该点两侧是否变号。

求闭区间上最值的步骤是：求出区间内所有驻点和不可导点处的函数值，再与两端点函数值比较，最大者即最大值，最小者即最小值。开区间或全体实数上求最值则借助单调性与极限行为判断，例如求二次函数最值可以用配方，也可以求导后找驻点。

### 不定积分

若 F'(x) = f(x)，则称 F(x) 是 f(x) 的一个原函数，f(x) 的全体原函数 F(x)+C 称为不定积分，记作 ∫f(x)dx。原函数族中任意两个只相差一个常数，因此不定积分的答案必须带积分常数 C，漏写 C 是计算不定积分时最常见的失分点。

基本积分公式由基本导数公式反推，例如 ∫x^a dx = x^(a+1)/(a+1)+C（a≠-1），∫(1/x)dx = ln|x|+C，∫e^x dx = e^x+C，∫cos x dx = sin x+C。

主要积分方法有两种。第一换元法（凑微分）通过把积分改写成 ∫g(u)du 的形式再套用基本公式；第二换元法通过变量代换去掉根号。分部积分法由乘积求导公式导出：∫u dv = uv - ∫v du，适用于被积函数是“幂函数与指数函数”“幂函数与三角函数”“幂函数与对数函数”乘积的情形，选取 u 的原则是求导后变简单。

### 定积分

定积分是黎曼和的极限：把区间分成 n 份，作和式再令最大子区间长度趋于零。几何上它表示曲线与 x 轴围成的有向面积。

牛顿-莱布尼茨公式把定积分与不定积分联系起来：若 F(x) 是 f(x) 在区间上的一个原函数，则 ∫(a到b) f(x)dx = F(b)-F(a)。这是定积分计算的基本方法，步骤可归纳为“求原函数 — 代上限 — 减下限”。例如 ∫(0到2) x dx，原函数为 x²/2，代入得 2²/2 - 0 = 2。

变上限积分 Φ(x) = ∫(a到x) f(t)dt 是可导的，且 Φ'(x) = f(x)，这一结论把积分学与微分学真正贯通。定积分还有线性性质、区间可加性、以及比较性质与估值定理。应用方面：平面图形面积由上下曲线之差积分得到，旋转体体积由圆盘法或柱壳法求出。

### 无穷级数

常数项级数是把无穷多个数相加的形式，其收敛的定义是部分和数列有极限。

基本判别法包括：比较判别法（与已知敛散性的级数比较）、比值判别法（看相邻项比值极限）、根值判别法。p 级数 ∑1/n^p 的结论必须牢记：p>1 时收敛，p≤1 时发散。因此 ∑1/n² 收敛（p=2>1），而调和级数 ∑1/n 发散（p=1）。几何级数在公比绝对值小于 1 时收敛。

交错级数用莱布尼茨判别法：若各项绝对值单调递减且趋于零，则交错级数收敛。若级数取绝对值后收敛，称绝对收敛，绝对收敛必然收敛；若本身收敛但取绝对值后发散，称条件收敛。

幂级数 ∑a(n)x^n 存在收敛半径 R，在 |x|<R 内绝对收敛。把函数展开成幂级数称为泰勒展开，x0=0 时也称麦克劳林展开，是近似计算与求极限的重要工具。`;

/** 高等数学（上）：极限、导数、积分、级数，全部题库数学标签都在这里。 */
export function buildCalculusOneNotes(): SeedLectureInput[] {
  return [
    makeLecture('1', '高等数学（上）', '数学基础', CALCULUS_1),
  ];
}
