/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const DATABASE = `## 数据库系统原理

### 数据库基础与关系模型

数据库系统由数据库、数据库管理系统、应用程序与数据库管理员组成。数据库管理系统负责数据定义、数据操纵、数据库运行管理与建立维护，其核心价值在于把数据从应用程序中独立出来，实现数据共享并减少冗余。

数据模型是对现实世界数据特征的抽象，常见的有层次模型、网状模型与关系模型。关系模型用二维表表示实体与联系，概念清晰、有严格数学基础，因而成为主流。

关系模型的基本概念包括：关系即二维表；元组即表中的一行；属性即表中的一列；域是属性的取值范围；码是能唯一标识元组的属性组。候选码是能作主码的最小属性组，主码从候选码中选定；包含在某个候选码中的属性称主属性，其余称非主属性。外码是本关系中引用另一个关系主码的属性。

关系的三类完整性约束是：实体完整性要求主码非空且唯一；参照完整性要求外码的取值必须是被引用关系中存在的主码值或为空；用户定义完整性是具体业务对属性取值的约束，如年龄范围与成绩区间。

关系代数用集合运算表达查询，基本运算有选择、投影、并、差与笛卡尔积，组合运算有交、连接与除。选择是取满足条件的行，投影是取指定的列（会消除重复行），自然连接按同名属性自动匹配。

### 结构化查询语言

SQL 是关系数据库的标准语言，分四个部分：数据定义语言建表改表；数据操纵语言增删改查；数据控制语言授权回收；事务控制语言提交回滚。

数据查询的基本结构由选择子句、来源子句、条件子句、分组子句、分组筛选子句、排序子句与限制子句组成，书写顺序与执行顺序不同：实际执行为先确定数据来源，再过滤行，再分组，再筛选分组，再投影列，最后排序与限制。

连接查询包括内连接、左外连接、右外连接与全外连接。内连接只保留两表匹配的行；左外连接保留左表全部行，右表不匹配处补空值。自身连接是把同一张表取两个别名后连接，用于处理表中行之间的层级关系。

嵌套查询把子查询放在条件中，返回单值的称标量子查询，返回一列多行的用谓词匹配。相关子查询引用外层查询的列，因而对外层的每一行都要重新求值，效率通常低于连接。存在量词谓词只关心子查询是否返回行，不求具体值，因此往返常快于计数比较。

分组统计用聚合函数求计数、求和、平均值、最大值与最小值，分组后对聚合结果的筛选必须写在分组筛选子句中，不能写在条件子句中，因为后者在分组之前执行。

视图是基于查询定义的虚表，不存储数据，能简化查询、提供逻辑独立性并限制数据访问。索引是为加速查找建立的数据结构，能加速查询但会降低插入删除性能，因此不宜盲目建立。

### 数据库设计与规范化

数据库设计分需求分析、概念结构设计、逻辑结构设计、物理结构设计、数据库实施与运行维护六个阶段。概念结构设计用实体联系模型描述现实世界，用矩形表示实体、椭圆表示属性、菱形表示联系，联系有一对一、一对多与多对多三种。

实体联系模型向关系模型转换时，每个实体转为一张表，多对多联系必须单独转为一张表且主码为双方主码的组合，一对多联系可把一方的码作为属性加入多方。

规范化理论用范式衡量关系模式的优劣。第一范式要求每个属性不可再分；第二范式在满足第一范式的基础上要求非主属性完全依赖于码，消除部分函数依赖；第三范式进一步要求非主属性不传递依赖于码。

函数依赖中，完全函数依赖指决定因素不可再缩小；部分依赖指可由码的一部分决定；传递依赖指通过中间属性间接决定。多值依赖与第四范式处理属性之间相互独立的情形。规范化的过程是逐步分解，通过投影把关系拆开以消除不合适的数据依赖。

规范化程度越高，冗余越少但连接操作越多，查询性能可能下降。实际工程中常在第三范式的基础上根据访问模式适度反规范化，用少量冗余换取查询效率。

### 事务管理与并发控制

事务是用户定义的一组数据库操作序列，这些操作要么全部执行要么全部不执行，是不可分割的工作单位。

#### 事务的 ACID 特性

事务具有四个特性，简称 ACID：

- 原子性（Atomicity）：事务中的所有操作要么全部完成，要么全部不生效，通过日志的回滚机制实现。
- 一致性（Consistency）：事务执行前后数据库都处于合法状态，完整性约束不被破坏，这是事务的最终目标。
- 隔离性（Isolation）：并发执行的多个事务之间互不干扰，一个事务的中间状态对其他事务不可见，通过锁与多版本控制实现。
- 持久性（Durability）：事务一旦提交，对数据库的改变就是永久的，即使随后发生故障也不丢失，通过日志的写入与恢复机制实现。

并发操作不加控制会带来三类问题：丢失修改指两个事务同时更新同一数据，后写入的结果覆盖前者的结果；不可重复读指同一事务内两次读取同一数据结果不同；读脏数据指读到另一个事务尚未提交且随后被回滚的数据。

并发控制技术中，封锁是最常用的方法。共享锁允许其他事务读但不允许写，排他锁不允许其他事务读或写。封锁协议在何时申请与释放锁上有不同约定，两段锁协议要求事务分为扩展阶段（只加锁不解锁）与收缩阶段（只解锁不加锁），它能保证调度的可串行化，是并发正确性的重要基础。

死锁在数据库中是两个或多个事务相互等待对方持有的锁。检测通过事务等待图找环，解除靠选择代价最小的事务回滚。预防手段包括一次封锁法（事务开始即申请全部锁）与顺序封锁法（按固定顺序申请资源）。

隔离级别从低到高依次为读未提交、读已提交、可重复读与可串行化。级别越高一致性越好但并发度越低，读已提交能避免读脏数据，可重复读进一步避免不可重复读，可串行化还避免幻读。

### 数据库恢复

故障分三类：事务内部故障（单个事务出错，撤销该事务即可）、系统故障（断电或系统崩溃，内存信息丢失但磁盘完好，需要重做已提交事务、撤销未提交事务）、介质故障（磁盘损坏，需要装入备份并重做日志）。

恢复技术的基础是日志文件。日志记录事务标识、数据项、旧值与新值，采用先写日志后写数据库的原则，否则故障时无法恢复。检查点技术定期把缓冲区内容写入磁盘并记录检查点，使恢复时不必扫描全部日志。

有检查点的恢复策略是：从检查点开始正向扫描日志，把已提交事务加入重做队列，未提交事务加入撤销队列，然后反向扫描撤销未提交事务，正向扫描重做已提交事务。备份分完全备份与增量备份，两者结合可兼顾恢复速度与存储成本。`;

/** 数据库系统原理：关系模型、SQL、范式、事务 ACID 与恢复。 */
export function buildDatabaseNotes(): SeedLectureInput[] {
  return [
    makeLecture('14', '数据库系统原理', '专业核心', DATABASE),
  ];
}
