/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const COMPUTER_ORGANIZATION = `## 计算机组成原理

### 计算机系统概述

计算机系统由硬件与软件共同构成。硬件五大部件是运算器、控制器、存储器、输入设备与输出设备，其中运算器与控制器合称中央处理器。冯·诺依曼结构把程序与数据同样以二进制形式存放在存储器中，按地址顺序取指执行，这一“存储程序”思想是现代计算机的基础。

评价计算机性能的主要指标有主频、字长、运算速度、存储容量与存取周期。主频是时钟周期的倒数，反映机器节奏；字长是 CPU 一次能处理的二进制位数；存储器容量与存取速度共同决定整体性能，实际系统通过层次化存储缓解速度与容量的矛盾。

### 数据表示与运算

计算机内部用二进制表示数据。带符号整数有原码、反码、补码三种表示法：原码符号位与数值位分开，运算复杂；补码把减法统一为加法，零的表示唯一，因此被普遍采用。补码的求法是取反加一。移码常用于浮点数的阶码，便于比较大小。

定点数的小数点位置固定，浮点数由阶码与尾数组成，小数点位置随阶码浮动，因此表示范围大得多。浮点数遵循规格化表示，运算步骤为对阶、尾数运算、规格化、舍入与溢出判断，其中对阶时小阶向大阶对齐。

字符编码中，ASCII 用一个字节表示英文字符，最高位为校验位或扩展位；汉字编码有区位码、国标码与机内码等形式。数据校验常用奇偶校验、海明校验与循环冗余校验，海明码能定位并纠正一位错，循环冗余校验多用于存储与通信。

### 存储系统

存储器按与 CPU 的距离分为寄存器、高速缓存、主存与外存，速度递减而容量递增，这一层次结构依靠程序的局部性原理发挥作用：时间局部性指刚访问过的数据很可能再次被访问，空间局部性指相邻位置的数据很可能被连续访问。

主存由存储单元构成，每个单元有唯一地址。静态存储单元用触发器保存信息，速度快但集成度低；动态存储单元用电容保存信息，需要定期刷新，集成度高因而用于主存。

高速缓存的作用是缓解 CPU 与主存的速度差距，其地址映射方式有直接映射、全相联映射与组相联映射三种。直接映射实现简单但冲突多；全相联映射最灵活但比较电路复杂；组相联是二者的折中，实际系统普遍采用。替换算法常用最近最少使用与先进先出。

虚拟存储器把主存与外存统一编址，程序可以使用远大于物理内存的地址空间。页式管理把地址空间分成固定大小的页，段式管理按逻辑单位分段，段页式结合两者优点。地址变换由页表与快表共同完成，快表命中时可避免访问主存中的页表。

### 指令系统

指令由操作码与地址码组成。操作码指明做什么，地址码指明对谁做。指令字长有定长与变长两种，变长指令可以节省空间但译码复杂。

寻址方式决定操作数的来源，常见的有立即寻址（操作数在指令中）、直接寻址（指令给出主存地址）、间接寻址（指令给出地址的地址）、寄存器寻址、寄存器间接寻址、变址寻址与相对寻址。相对寻址以程序计数器为基准，便于程序浮动。

指令类型包括数据传送、算术逻辑运算、移位、控制转移、输入输出等。CISC 追求指令功能强、种类多，RISC 追求指令精简、定长、便于流水线实现。指令的执行过程通常分为取指、译码、执行、访存、写回等阶段。

### 中央处理器

CPU 由运算器与控制器构成。运算器包含算术逻辑单元、累加器与通用寄存器，完成算术与逻辑运算；控制器产生控制信号，协调各部件按时序工作。

指令周期是取出并执行一条指令所需的时间，可细分为机器周期与时钟周期。数据通路是数据在部件之间传送的路径，单总线结构简单但一次只能传送一个数据，多总线结构能提高并行度。

流水线把指令执行划分为若干阶段并重叠进行，理想情况下每个时钟周期完成一条指令，从而大幅提高吞吐率。流水线的冒险有三类：结构冒险来自硬件资源冲突；数据冒险来自后续指令需要前面指令的结果，可用转发或插入气泡解决；控制冒险来自转移指令改变了取指顺序，可用分支预测或延迟槽缓解。

中断是 CPU 处理外部事件的机制，过程包括中断请求、中断判优、中断响应、保护现场、执行中断服务程序、恢复现场与返回。中断使 CPU 能及时响应外设，是输入输出与实时处理的基础。

### 总线与输入输出系统

总线是连接各部件的公共信息通道，按传送信息类型分为数据总线、地址总线与控制总线。总线带宽等于总线宽度乘以工作频率。总线仲裁解决多个部件同时请求使用总线的冲突，分集中式与分布式两类。

输入输出方式有四种：程序查询方式由 CPU 反复查询外设状态，效率最低；中断方式在外设就绪时主动通知 CPU，提高了效率但仍需 CPU 逐字传送；DMA 方式由专用控制器直接在内存与外设之间传送数据块，只在开始与结束时需要 CPU 干预；通道方式进一步把控制权交给专用处理器，适合大量外设的情形。

磁盘是主要的外存设备，其存取时间由寻道时间、旋转延迟与传输时间组成，其中寻道时间占比最大，因此调度算法以减少寻道为目标。常见磁盘调度算法有先来先服务、最短寻道时间优先、扫描算法与循环扫描算法。`;

/** 计算机组成原理：数据表示、存储层次、指令与流水线。 */
export function buildComputerOrganizationNotes(): SeedLectureInput[] {
  return [
    makeLecture('11', '计算机组成原理', '专业核心', COMPUTER_ORGANIZATION),
  ];
}
