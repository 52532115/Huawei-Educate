/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const COMPILER = `## 编译原理

### 编译过程概述

编译程序把高级语言源程序翻译成等价的低级语言目标程序。整个过程分六个阶段，各阶段职责明确：

1. **词法分析**：从左到右扫描字符流，识别出一个个单词（Token），输出 Token 序列。Token 通常表示为二元组（种别码，属性值），例如标识符与常数需要保留具体值供后续阶段使用。词法分析还要完成过滤空白与注释、识别关键字、处理非法字符等工作。实现方式有手工构造与用词法分析器生成工具自动生成两种。
2. **语法分析**：把 Token 序列组织成语法树或分析树，判断程序是否符合文法。它能发现结构类错误，如括号不匹配、缺少分号。
3. **语义分析**：检查静态语义，包括类型匹配、变量是否声明、作用域是否正确、运算是否合法。这一阶段通常借助符号表完成，并产生带注解的中间表示。
4. **中间代码生成**：把源程序翻译成与具体机器无关的中间表示，常用四元式、三元式、逆波兰式与抽象语法树。中间代码便于优化与移植。
5. **代码优化**：在保持语义不变的前提下改进中间代码，减少运行时间或空间占用。优化分局部优化与全局优化。
6. **目标代码生成**：把中间代码转换成目标机器的汇编或机器指令，这一阶段需要分配寄存器并确定指令选择与指令调度。

此外还有贯穿全程的两项工作：符号表管理用于记录标识符的类型、作用域与地址等信息，是各阶段共享的数据结构；出错处理要求编译器在发现错误后能继续分析，尽量一次报出多个错误而不是遇到第一个就停止。

### 文法与语言

文法是描述语言语法结构的工具，形式文法由四元组组成：非终结符集合、终结符集合、产生式集合与开始符号。文法分四类：0 型（短语结构文法）、1 型（上下文有关文法）、2 型（上下文无关文法）与 3 型（正规文法），描述能力依次减弱。

上下文无关文法足以描述程序设计语言的语法结构；正规文法与正规式等价，用于描述单词的结构，这正是词法分析的理论基础。推导是从开始符号出发逐步替换非终结符得到句子的过程，最左推导与最右推导分别对应自顶向下与自底向上分析策略。

语法树是推导过程的图形表示，叶结点为终结符。二义性文法指同一个句子存在两棵不同语法树，这类文法会导致语义歧义，需要通过改写文法或约定优先级与结合性来消除。短语、直接短语与句柄是自底向上分析的核心概念，句柄是规范归约中每次被归约的直接短语。

### 词法分析与有穷自动机

正规式用来描述单词的模式，正规式与有穷自动机等价，因此可以先用正规式定义各类单词，再构造自动机识别。正规式到有穷自动机的转换经过两个步骤：先构造不确定的有穷自动机（NFA），再把 NFA 确定化为确定的有穷自动机（DFA），最后对 DFA 做最小化以减少状态数。

NFA 允许一个状态对同一输入有多个后继，也可以有 ε 转移；DFA 则对每个输入符号最多有一个后继，因此更便于程序实现。确定化的核心是子集构造法，把 NFA 的状态集合作为 DFA 的一个状态。最小化的思想是把等价状态合并，通常用划分法逐步细分状态集。

词法分析器的实现可以手工编写状态转移逻辑，也可以用工具从正规式自动生成，后者的典型流程是写出正规式、转换成自动机、生成识别程序。

### 语法分析

语法分析分自顶向下与自底向上两大类。

自顶向下分析从开始符号出发试图推导出输入串。递归下降分析为每个非终结符写一个递归子程序，直观易读。预测分析用分析表与栈实现非递归过程，要求文法是 LL(1) 文法，即只需向前看一个符号就能唯一确定产生式。

LL(1) 的判定涉及两个集合：FIRST 集是某符号串可能推导出的首终结符集合，FOLLOW 集是某非终结符后面可能紧跟的终结符集合。当同一非终结符的两条产生式的 FIRST 集相交，或某产生式可推出空串而 FIRST 与 FOLLOW 相交时，出现冲突，需要提取左公因子或消除左递归。消除左递归是必要的，因为直接或间接左递归会让自顶向下分析陷入无限循环。

自底向上分析从输入串出发逐步归约到开始符号。算符优先分析按运算符优先级决定归约时机，实现简单但适用范围有限。LR 分析法能力最强，能处理绝大多数程序设计语言文法，分 LR(0)、SLR(1)、LALR(1) 与规范 LR(1) 四类，分析能力依次增强而分析表规模也依次增大。

LR 分析的核心是构造项目集规范族：项目是产生式加点号的形式，点号表示已识别的部分；通过闭包运算与转换函数构造状态，再根据项目类型填写动作表与转移表。移进归约冲突与归约归约冲突是构造分析表时的常见问题，通过向前看符号可以化解大部分冲突。实际编译器常用 LALR(1)，因为它在分析能力与表规模之间取得了良好平衡。

### 语法制导翻译与语义分析

语法制导翻译把语义动作附加到产生式上，在语法分析过程中同步完成语义处理。属性分两类：综合属性由子结点的属性计算得到，自下而上传递；继承属性由父结点或兄弟结点的属性计算得到，自上而下传递。只含综合属性的称 S 属性定义，可以在自底向上分析中自然计算；既有综合又有继承属性的称 L 属性定义，适合自顶向下分析。

语义分析的主要任务是类型检查与作用域处理。类型检查验证运算的操作数类型是否相容，包括隐式类型转换与类型推导；作用域处理维护符号表的多层结构，进入块时压入新表，退出时弹出，从而保证内层定义可以屏蔽外层同名定义。

中间代码的常见形式中，四元式包含运算符、两个操作数与一个结果，便于优化与重排；三元式用编号引用前面结果以避免临时变量；逆波兰式是后缀表示，适合栈式求值；抽象语法树保留层次结构，便于多次遍历。控制语句的翻译要生成标号与无条件跳转，布尔表达式常用短路计算以减少求值。

### 代码优化与目标代码生成

优化的目标是提高运行速度或降低空间占用，前提是结果必须与优化前完全等价。优化的层次分局部优化、循环优化与全局优化。

常见优化技术包括：常量折叠与常量传播在编译期算出结果；公共子表达式消除把重复计算的表达式只算一次；复写传播与死代码消除去掉无用赋值；强度削弱把乘法替换为移位或加法；循环不变代码外提把循环内不变的计算移到循环外；循环展开减少循环控制开销；归纳变量删除减少循环内的自增运算。

优化的基础是控制流分析与数据流分析。基本块是顺序执行且只有一个入口一个出口的指令序列，控制流图用有向边表示基本块之间的转移关系。活跃变量分析与到达定义分析为寄存器分配与优化提供依据。

目标代码生成要解决三个问题：指令选择（用哪些机器指令实现中间代码）、寄存器分配（哪些变量放在寄存器中，寄存器不够时如何溢出到内存）、指令调度（调整指令顺序以掩盖访存延迟）。寄存器分配常用图着色法，把冲突的变量分到不同寄存器。生成的代码还要考虑指令集特点，如利用寻址方式减少指令条数。`;

/** 编译原理：词法到目标代码的六阶段、文法与自动机、LR 分析与优化。 */
export function buildCompilerNotes(): SeedLectureInput[] {
  return [
    makeLecture('15', '编译原理', '专业核心', COMPILER),
  ];
}
