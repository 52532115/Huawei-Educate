/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const WEB_FRONTEND = `## Web前端开发

### HTML 与 CSS 基础

HTML 用标签描述文档结构，语义化标签让结构本身携带含义，既利于无障碍访问也有利于搜索引擎理解。文档对象模型把页面表示为树，脚本通过它读写节点、修改样式与绑定事件。

盒模型是布局的基础，元素占据的空间由内容、内边距、边框与外边距组成。标准盒模型中宽度只含内容，替代盒模型把内边距与边框算入宽度，后者更符合直觉因此常被全局启用。外边距折叠指相邻块级元素的上下外边距合并为较大者，这是布局中常见的意外来源。

定位方式决定元素如何脱离正常流：静态定位是默认；相对定位相对自身原位偏移但保留占位；绝对定位相对最近的已定位祖先偏移且不保留占位；固定定位相对视口；粘性定位在滚动到阈值前表现为相对定位、之后表现为固定定位。

层叠与继承决定样式冲突时的胜出规则，优先级由选择器特异性、来源与声明顺序共同决定。行内样式高于标识选择器，标识选择器高于类与属性选择器，类高于标签选择器，通配与继承的优先级最低。

弹性布局适合一维排列，通过主轴方向、对齐方式与伸缩比例控制子项分配剩余空间。网格布局适合二维布局，能同时定义行与列并支持区域命名，表达复杂版面比嵌套弹性容器更清晰。

### JavaScript 语言核心

JavaScript 是动态类型语言，变量声明有函数作用域的 var、块作用域的 let 与常量 const。使用 let 与 const 能避免变量提升带来的意外行为。

原始类型包括数值、字符串、布尔、空、未定义与符号，其余都是对象。相等比较中，宽松等于会做类型转换，严格等于要求类型也相同，因此推荐始终使用严格等于。

函数是一等对象，可以作为参数传递、作为返回值返回、赋给变量。闭包指函数与其定义时所处词法环境的组合，它让函数可以访问定义时的变量，即使外层函数已经返回。闭包常用于实现数据私有与保存状态，但也会延长变量生命周期，使用不当会造成内存泄漏。

原型链实现继承：对象访问属性时若自身没有，会沿原型链向上查找直到找到或到达链尾。类语法是原型继承的语法糖。this 的指向取决于调用方式而非定义位置，箭头函数不绑定自己的 this，而是沿用外层作用域的值，因此在回调中常用来避免 this 丢失。

异步编程从回调发展为 Promise 再到 async/await。回调嵌套过深会形成难以维护的回调地狱；Promise 用链式调用表达顺序并统一错误处理；async/await 让异步代码读起来像同步代码。事件循环是理解异步执行顺序的关键：同步任务在当前调用栈执行完毕后处理微任务队列，再进入下一轮宏任务。

### 响应式设计

响应式设计让同一套代码适配不同屏幕。核心技术是流式布局（用相对单位代替固定像素）、弹性图片（限制最大宽度为容器宽度）与媒体查询（按视口条件应用不同样式）。移动优先的写法先定义小屏样式，再用媒体查询逐步增强大屏表现，这样能保证核心内容在受限环境下依然可用。

视口元标签决定移动设备如何缩放页面，缺少它会让桌面页面被整体缩小显示。相对单位中，相对根字号的单位便于统一调整比例，相对视口宽度的单位适合随屏幕变化的装饰性尺寸。容器查询让组件按自身可用宽度而非视口宽度响应，使组件的可复用性不再依赖它被放在页面何处。

### 前端框架与工程化

组件化把界面拆成可复用、可组合、状态自洽的单元。框架的核心职责是数据驱动视图：状态变化后自动更新对应的界面部分，开发者只需描述状态与界面的对应关系，而不必手工操作节点。

虚拟节点与差异比对是常见实现方式：先在内存中描述目标结构，与上一次结构比较后只把差异应用到真实节点，从而减少昂贵的布局与重绘。响应式系统通过拦截数据读写建立依赖关系，在数据变化时精确通知相关视图更新。

组件通信方式包括父传子属性、子通知父事件、跨层级共享状态与全局状态管理。状态提升指把多个组件共同依赖的状态移到最近的共同祖先，避免数据在兄弟组件之间迂回传递。

工程化解决的是协作与交付问题：模块打包把分散文件组织成可部署产物，同时处理依赖解析与代码转换；开发服务器提供热更新以缩短反馈循环；代码检查与格式化统一团队风格；类型系统在编译期发现类型错误；单元测试与端到端测试保证重构安全。构建优化的常见手段包括按需加载、代码分割、依赖预打包与产物压缩。

### 性能与可访问性

前端性能的核心指标围绕加载与交互：首次内容绘制衡量用户多快看到内容，最大内容绘制衡量主要内容的呈现时间，累计布局偏移衡量视觉稳定性，首次输入延迟衡量交互响应速度。

优化手段分几类：资源层面压缩图片、使用现代格式、启用缓存与内容分发网络；加载层面减少关键资源体积、延迟非关键脚本、预加载即将用到的资源；渲染层面避免布局抖动（批量读写样式而非交替进行）、减少重排重绘、对长列表虚拟化；脚本层面拆分长任务、把耗时计算交给工作线程以免阻塞主线程。

可访问性要求界面能被辅助技术使用：为图片提供替代文本、保证文本与背景的对比度、让所有交互都能用键盘完成、使用语义化标签并正确关联表单标签与输入框、为动态区域声明实时通知。可访问性不是额外负担，它同时提升了代码的语义清晰度与在极端环境下的可用性。`;

const LINUX = `## Linux系统管理

### 基础命令与文件系统

Linux 文件系统是树形结构，根目录在最顶层，一切皆文件——设备、管道、套接字都以文件形式暴露。常见目录中，系统配置放在 etc，用户可执行程序放在 bin 与 usr/bin，可变数据放在 var，临时文件放在 tmp，用户主目录放在 home。

常用文件操作命令包括列出目录内容、切换目录、查看当前路径、创建与删除目录、复制与移动文件。查看文件内容时，短文件可直接输出，长文件应分页查看或只看首尾若干行。查找文件可按名称、类型、大小与修改时间组合条件，也可按内容进行查找。

管道把前一个命令的输出作为后一个命令的输入，这是组合小工具完成复杂任务的基础。重定向把输出写入文件，覆盖与追加是两种模式，标准错误可以单独重定向。通配符按模式匹配文件名，注意它由 shell 展开而非由命令处理。

文件权限分属主、属组与其他用户三组，每组有读、写、执行三种权限。权限可用符号表示法修改，也可用八进制数字表示法设置。目录的执行权限表示可以进入该目录，目录的读权限表示可以列出内容，因此只有读权限的目录无法访问其中文件。特殊权限中，粘滞位让目录内的文件只能被属主删除，常用于共享临时目录。修改属主与属组需要管理员权限。

### Shell 脚本编程

脚本以指定解释器的首行开始，赋予执行权限后可直接运行。变量赋值不加空格，引用时加前缀符号。命令替换把命令输出嵌入字符串，算术运算需用专门语法。

条件判断用测试命令或括号语法，返回状态码为零表示真。逻辑运算符连接多个条件，短路求值可以避免不必要的检查。分支结构支持多路条件匹配，循环有遍历列表与按条件两种形式。

脚本中要关注退出状态：最后一条命令的退出码决定脚本的退出码，因此关键步骤后应显式检查，必要时立即退出并给出非零状态，这样脚本才能被上层流程可靠地判断成败。错误处理中，未定义变量应视为错误、管道中任一环节失败都算失败，这两条能避免脚本静默地做错事。

位置参数用于接收调用时传入的参数，参数个数与全部参数都有对应变量。函数把重复逻辑封装，参数通过位置参数传递，返回值同样靠退出码而非输出文本。定时任务用于周期性执行脚本，但其运行环境没有用户交互式登录时的环境变量，因此脚本中应使用绝对路径并显式设置所需环境。

### 用户与权限管理

用户分三类：超级用户拥有全部权限；系统用户供服务与守护进程使用，通常不允许登录；普通用户供人使用。用户组用于批量授权，一个用户可以属于多个附加组。

创建与修改用户时要合理设置主目录、登录 shell 与初始组。删除用户时应同时处理其主目录与邮箱文件。修改密码与设置密码过期策略是安全运维的常规动作。切换用户与以其他身份执行命令是运维中常用的提权方式，后者可精确到以指定用户执行单条命令，比切换整个会话更安全。

提权配置文件的语法是把“谁在哪些主机上以谁的身份执行哪些命令”写成规则，规则应尽量精确，避免使用全部命令的宽泛授权，否则一个普通用户就能获得完整的管理员能力。

### 进程管理

进程是运行中的程序实例，每个进程有唯一标识、父进程标识、状态与资源占用。查看进程可以列出全部或筛选特定条件，也可以动态观察资源占用。前台进程占据终端，后台进程与之并行，把前台任务转到后台或把后台任务调回前台是常用操作。

信号是进程间通信的简单形式，常用信号中，终止信号可由进程捕获后做清理再退出，强制终止信号不可捕获，因此应优先使用前者。挂起信号让进程暂停，继续信号恢复执行。

进程优先级影响调度权重，普通用户只能降低优先级而不能提高。杀死进程时应先按标识精确终止，必要时才批量按名称终止，避免误杀同名的关键进程。查找占用某端口的进程是排查端口冲突的标准动作。

### 网络配置与服务管理

网络配置包括查看与设置地址、网关与域名解析。现代发行版常用统一命令管理网络接口与连接配置。排查连通性时按层次推进：先确认本机地址配置，再测试网关可达性，再测试外部地址可达性，最后测试域名解析。域名解析失败与网络不通是两类不同的问题，要能区分。

端口与连接状态可用专门工具查看，监听端口表示本机服务入口，已建立连接表示当前会话。抓包工具用于在无法定位问题时观察真实报文。

服务管理由初始化系统负责，常见操作包括启动、停止、重启、查看状态与设置开机自启。查看服务日志是排查服务启动失败的首要动作。日志系统按优先级与来源分类，支持按时间与单元筛选。

定时任务有两个层次：用户级任务与系统级任务，后者适合与系统运行状态相关的调度。日志轮转避免日志文件无限增长，通过配置保留份数与压缩策略控制磁盘占用。

### 系统运维实践

磁盘管理包括查看使用率、定位大文件与目录、检查文件系统并修复错误。磁盘空间耗尽的常见原因除了数据增长还有已删除但被进程持有的文件，这类文件仍占用空间直到进程释放句柄。

性能排查按资源维度展开：CPU 看负载与上下文切换，内存看可用量与交换分区使用，磁盘看吞吐与等待时间，网络看带宽与重传率。负载高不等于 CPU 忙，也可能是大量进程在等待磁盘，因此必须结合多种指标判断。

系统安全加固的基本动作包括：及时安装安全更新、关闭不必要的服务与端口、禁用空口令与弱口令、限制提权范围、开启审计日志、配置防火墙规则并遵循默认拒绝原则。备份要验证可恢复性，未验证的备份在需要时往往不可用。`;

/** Web 前端与 Linux 运维：布局、异步、工程化，以及命令、脚本与服务管理。 */
export function buildWebLinuxNotes(): SeedLectureInput[] {
  return [
    makeLecture('24', 'Web前端开发', '专业方向', WEB_FRONTEND),
    makeLecture('25', 'Linux系统管理', '专业方向', LINUX),
  ];
}
