/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const COMPUTER_NETWORK = `## 计算机网络

### 网络体系结构

计算机网络是把分布在不同地理位置的计算设备通过通信线路连接起来、实现资源共享与信息传递的系统。按覆盖范围可分为局域网、城域网与广域网；按拓扑结构可分为总线型、星型、环型与网状。

协议是通信双方必须遵守的规则集合，三要素是语法（数据与控制信息的格式）、语义（需要发出何种控制信息、完成何种动作）与同步（事件实现的顺序）。协议是水平的，即同层之间通信；服务是垂直的，即下层向上层提供能力。

OSI 参考模型分七层，自下而上为物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。TCP/IP 模型则简化为四层：网络接口层、网际层、传输层与应用层。学习中常用五层结构折中描述：物理层、数据链路层、网络层、传输层、应用层。

分层的好处是各层独立、灵活性好、结构上可分割、易于实现与维护。数据在发送时逐层封装（加首部），接收时逐层解封装（去首部），这一过程是理解各层协议的基础。

### 物理层与数据链路层

物理层解决如何在传输介质上传输比特流，关注接口特性、传输介质与信号编码，不关心比特的含义。传输介质分导向介质（双绞线、同轴电缆、光纤）与非导向介质（无线电、微波）。信道复用技术有时分复用、频分复用、波分复用与码分复用。奈氏准则给出理想低通信道下的极限码元传输率，香农定理给出有噪声信道的极限信息传输速率，二者说明带宽与信噪比共同决定传输上限。

数据链路层的功能是把物理层的比特流组织成帧，并进行差错控制与流量控制。三个基本问题是封装成帧、透明传输与差错检测。透明传输通过字节填充或比特填充解决数据中出现与控制字符相同内容的问题。差错检测用循环冗余校验，它能检错但不能纠错，因此可靠传输还要靠确认与重传机制。

流量控制与可靠传输机制经历了停等协议、后退 N 帧协议与选择重传协议三个阶段，滑动窗口是它们的共同基础。窗口越大，信道利用率越高，但需要的序号空间也越大。

介质访问控制解决共享信道的争用问题，分为静态划分信道与动态接入两类。动态接入方法中，CSMA/CD 用于有线以太网：先听后发、边听边发、冲突停发、随机重发。CSMA/CA 用于无线局域网，因为无线环境难以检测冲突，只能尽量避免。以太网的帧格式包含目的地址、源地址、类型与数据字段。

### 网络层

网络层的核心任务是分组转发与路由选择。IP 协议提供无连接、尽最大努力交付的服务，不保证可靠性与顺序。

IPv4 地址长 32 位，用点分十进制表示，分为 A、B、C、D、E 五类。子网划分借用主机号的高位作为子网号，子网掩码用于区分网络号与主机号。无分类编址（CIDR）用斜线记法表示前缀长度，使地址分配更灵活，并支持路由聚合以减少路由表规模。

IP 数据报的首部包含版本、首部长度、总长度、标识、标志、片偏移、生存时间、协议、首部校验和、源地址与目的地址等字段。生存时间每经过一个路由器减一，减到零即丢弃，用于防止分组在网络中无限循环。

IP 分片发生在数据报长度超过链路最大传输单元时，分片后的数据报在目的主机重组。

地址解析协议（ARP）把 IP 地址解析为物理地址，工作在同一局域网内，通过广播请求、单播应答完成。逆地址解析协议用于反向查询。网际控制报文协议（ICMP）用于差错报告与网络探测，常用的 ping 命令就是基于它。动态主机配置协议（DHCP）自动分配 IP 地址，过程为发现、提供、请求、确认。网络地址转换（NAT）让多台内网主机共享一个公网地址。

路由选择协议分内部网关协议与外部网关协议。内部网关协议中，RIP 基于距离向量算法，以跳数为度量，最大跳数 15，实现简单但收敛慢且有坏消息传播慢的问题；OSPF 基于链路状态算法，每个路由器掌握全网拓扑后用迪杰斯特拉算法计算最短路径，收敛快且支持负载均衡。外部网关协议 BGP 用于自治系统之间交换可达性信息，考虑的是策略而非单纯的最短路径。

### 传输层

传输层为两台主机的应用进程提供端到端的逻辑通信，复用与分用是它向上层提供的核心能力：复用指多个应用进程共用同一条传输层连接，分用指接收方根据端口号把数据交付给正确的进程。端口号长 16 位，熟知端口为 0 到 1023。

用户数据报协议（UDP）是无连接的，不保证可靠交付，没有拥塞控制，首部只有 8 个字节，开销小、时延低，适合实时音视频、DNS 查询、广播与多播等场景。

传输控制协议（TCP）是面向连接的，提供可靠的全双工字节流服务。其可靠性依靠序号、确认、超时重传与校验和实现。首部最小 20 字节，包含源端口、目的端口、序号、确认号、数据偏移、控制位、窗口、校验和与紧急指针。

#### TCP 三次握手

建立连接需要三次握手，目的是让双方都确认自己的发送能力与对方的接收能力正常，并同步各自的初始序号。过程如下：

1. 客户端发送连接请求报文，把同步位 SYN 置为 1，同时选择一个初始序号 seq = x，然后进入 SYN_SENT 状态，等待服务器确认。
2. 服务器收到请求后，若同意连接则发回确认报文，把 SYN 与 ACK 都置为 1，确认号 ack = x + 1，同时选择自己的初始序号 seq = y，随后进入 SYN_RCVD 状态。
3. 客户端收到后，向服务器发送确认报文，ACK 置为 1，确认号 ack = y + 1，序号 seq = x + 1。这个报文可以携带数据。此后双方进入 ESTABLISHED 状态，连接建立完成。

为什么必须三次而不能只有两次？因为两次握手只能确认客户端的发送能力与服务器的接收能力，服务器无法确认自己的发送能力是否被客户端正确接收。若只有两次，一个早已失效的重复连接请求到达服务器后，服务器会直接建立连接并分配资源，而客户端并不认账，导致服务器资源被长期空占。

#### TCP 四次挥手

释放连接需要四次挥手，因为 TCP 是全双工的，每个方向必须单独关闭：

1. 客户端发送连接释放报文，终止位 FIN 置为 1，序号 seq = u，进入 FIN_WAIT_1，此后不再发送数据。
2. 服务器收到后发回确认，ack = u + 1，进入 CLOSE_WAIT。此时连接处于半关闭状态，服务器仍可继续发送尚未传完的数据。
3. 服务器把剩余数据发完后，发送自己的连接释放报文，FIN 置为 1，序号 seq = w，进入 LAST_ACK。
4. 客户端收到后发回确认，ack = w + 1，然后进入 TIME_WAIT 状态，等待 2 倍报文最大生存时间后才真正关闭。

TIME_WAIT 的必要性有两点：一是保证最后一个确认报文能到达服务器，若该报文丢失，服务器会重传释放报文，此时客户端仍能响应；二是让本次连接的旧报文在网络中彻底消失，避免它们干扰使用相同端口的新连接。这也解释了为什么服务器端出现大量 TIME_WAIT 时需要关注连接管理。

#### TCP 可靠传输与拥塞控制

超时重传时间必须动态计算，依据往返时间的加权平均值与偏差设定，否则过短会造成不必要的重传，过长会降低效率。快速重传在收到三个重复确认时立即重传，而不必等到超时。

流量控制通过滑动窗口实现，接收方在确认报文中通告自己的接收窗口，发送方据此调整发送速率，防止淹没接收方。

拥塞控制关注的是网络整体负载，包含四个算法：慢开始让拥塞窗口从小值开始按指数增长；拥塞避免在窗口超过阈值后改为线性增长；快重传在收到三个重复确认时立即重传丢失报文；快恢复在快重传后把阈值与窗口调整到中间值再进入拥塞避免。发送窗口取拥塞窗口与接收窗口中的较小者。

### 应用层

应用层协议直接面向用户需求。域名系统（DNS）把域名解析为 IP 地址，采用分布式层次数据库，查询方式分递归与迭代，通常用 UDP 传输。

万维网基于超文本传输协议（HTTP），采用请求响应模型。HTTP 是无状态协议，服务器不保存客户端状态，因此需要 Cookie 与 Session 来维持会话。请求方法有 GET、POST、PUT、DELETE 等，其中 GET 把参数放在 URL 中且可被缓存，POST 把数据放在请求体中。状态码中，2 开头表示成功，3 开头表示重定向，4 开头表示客户端错误，5 开头表示服务器错误。HTTP 的演进从短连接到持久连接再到多路复用，逐步减少了建立连接的开销。

文件传输协议（FTP）用两条连接分别传输控制信息与数据。电子邮件系统中，发送用 SMTP，接收用 POP3 或 IMAP。远程登录有 Telnet 与 SSH，其中 SSH 提供加密通道。

### 网络安全

网络安全的目标是保密性、完整性与可用性。被动攻击是截获与流量分析，难以检测但可以预防；主动攻击包括篡改、伪造、重放与拒绝服务，难以预防但可以检测。

对称加密的加解密使用同一密钥，速度快但密钥分发困难，代表算法有 DES 与 AES。非对称加密使用公钥与私钥，解决了密钥分发问题但速度慢，代表算法有 RSA 与椭圆曲线。实际系统用非对称加密协商会话密钥，再用对称加密传输数据，兼顾安全与效率。

报文鉴别通过消息摘要与数字签名实现。消息摘要把任意长度输入压缩成定长输出且不可逆，用于验证完整性；数字签名用私钥签名、公钥验证，同时保证不可否认性。数字证书由认证中心签发，把公钥与身份绑定，解决公钥被冒名替换的问题。

传输层安全协议（TLS）在传输层之上提供加密与鉴别，是 HTTPS 的基础。防火墙按规则过滤进出流量，分网络层防火墙与应用层防火墙；入侵检测系统监控异常行为并告警。拒绝服务攻击通过耗尽目标资源使其无法提供服务，分布式版本从大量主机同时发起，防御手段包括流量清洗与限速。`;

/** 计算机网络：分层体系、TCP 三次握手与四次挥手、可靠传输与安全。 */
export function buildComputerNetworkNotes(): SeedLectureInput[] {
  return [
    makeLecture('13', '计算机网络', '专业核心', COMPUTER_NETWORK),
  ];
}
