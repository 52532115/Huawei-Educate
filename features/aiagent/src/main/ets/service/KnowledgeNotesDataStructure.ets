/*
 * Copyright (c) 2025 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 */

import { SeedLectureInput } from '../viewmodel/KnowledgeModels';
import { makeLecture } from './KnowledgeNotesSupport';

const DATA_STRUCTURE = `## 数据结构

### 线性表

线性表是 n 个数据元素的有限序列，元素之间存在一对一的线性关系。顺序存储用一段连续空间存放元素，支持随机访问，按下标取值的时间复杂度是常量级，但插入删除平均需要移动一半元素。链式存储用结点加指针表示，插入删除只需改动指针，但查找必须从头遍历。

单链表每个结点含数据域与指向后继的指针；循环链表的尾结点指回头结点；双向链表每个结点含前驱与后继两个指针，便于反向遍历与删除操作。带头结点的链表可以让空表与非空表的处理逻辑统一，减少边界特判。

链表的常见操作有头插法建表（结果与输入逆序）、尾插法建表（保持顺序）、按值删除、就地逆置、合并两个有序链表。写链表代码时最容易出错的是指针丢失与空指针访问，必须先保存后继再改动指针。

### 栈与队列

栈是后进先出的线性表，只在栈顶进行插入与删除。顺序栈用数组加栈顶指针实现，链栈用链表实现。栈的典型应用包括：括号匹配、表达式求值、函数调用与递归的实现、深度优先搜索、浏览器前进后退。

队列是先进先出的线性表，队尾入队、队头出队。顺序队列会出现假溢出（队尾到界但队首之前仍有空位），因此常用循环队列解决，通过取模运算让存储空间首尾相接。循环队列需要区分队空与队满，常用做法是牺牲一个存储单元或额外维护元素个数。队列用于广度优先搜索、任务调度、打印缓冲区。

双端队列允许两端插入删除；优先队列按优先级出队，通常用堆实现。

### 串与数组

串是字符的有限序列，模式匹配是核心问题。朴素匹配算法在每个位置都可能回退，最坏效率低。改进算法利用已匹配部分的信息避免主串指针回退，把匹配复杂度降到线性级别。字符串的存储可以用定长顺序存储，也可以用堆分配存储以便动态扩展。

数组是下标的集合到值的映射，多维数组按行优先或列优先展平为一维存储。特殊矩阵（对称矩阵、三角矩阵、对角矩阵）可以通过下标映射压缩存储。稀疏矩阵用三元组表或十字链表保存非零元，避免为大量零元素浪费空间。

### 树与二叉树

树是 n 个结点的有限集合，有且仅有一个根结点，其余结点分成若干互不相交的子树。结点的度是子结点个数，树的度是各结点度的最大值。结点的层次从根开始计，树的高度是最大层次。

二叉树每个结点至多两棵子树且左右有序。重要性质：第 i 层至多有 2^(i-1) 个结点；深度为 k 的二叉树至多有 2^k - 1 个结点；对任意二叉树，叶子结点数等于度为 2 的结点数加一。满二叉树的每层都达到最大结点数，完全二叉树只有最后一层可以不满且结点集中在左侧，因此适合用数组顺序存储。

遍历是二叉树最基本的操作，分四种：先序遍历按“根—左—右”访问，中序遍历按“左—根—右”，后序遍历按“左—右—根”，层次遍历按层从上到下、从左到右依次访问。中序遍历有一个重要性质：在二叉搜索树上进行中序遍历，得到的序列是递增有序的。

#### 二叉搜索树与平衡树

二叉搜索树满足左子树所有结点的键小于根、右子树所有结点的键大于根。查找、插入、删除的平均复杂度是对数级，但退化成单支链表时会退化到线性级。删除结点分三种情形：叶子直接删除；只有一个子结点时用子结点替代；有两个子结点时用中序前驱或后继替代再删除那个替代结点。

平衡二叉树通过限制左右子树高度差不超过一来保证对数级高度，插入或删除后通过旋转（左旋、右旋、先左后右、先右后左）恢复平衡。红黑树用颜色约束近似平衡，插入删除的旋转次数更少，因此被标准库的映射与集合广泛采用。

哈夫曼树按字符频率构造带权路径长度最小的二叉树，用于数据压缩编码，其构造方法是反复合并权值最小的两棵子树。

### 图

图由顶点集与边集构成，分为有向图与无向图。顶点的度、入度、出度是基本概念，所有顶点度数之和等于边数的两倍。稠密图用邻接矩阵存储，便于判断两点是否相邻；稀疏图用邻接表存储，节省空间且便于遍历邻接点。

图的遍历有两种：深度优先搜索沿一条路径走到底再回溯，通常借助栈或递归实现；广度优先搜索按层扩展，必须借助队列实现。

最小生成树在带权连通无向图中寻找总权值最小的生成树，普里姆算法从一个顶点开始逐步加入最近顶点，适合稠密图；克鲁斯卡尔算法按边权从小到大选边并用并查集判环，适合稀疏图。

最短路径问题中，迪杰斯特拉算法求单源最短路径，要求边权非负；弗洛伊德算法求任意两点间最短路径，适合顶点数不大的情形。拓扑排序对有向无环图给出线性序列，使每条边都从前指向后，可用于任务调度与依赖分析。关键路径用于估算工程最短完成时间。

### 查找

顺序查找逐个比较，适用于无序表。折半查找要求表有序且顺序存储，每次比较把范围缩小一半，复杂度为对数级，但插入删除需要移动元素。分块查找把表分成若干块，块间有序而块内无序。

二叉搜索树查找的过程与折半查找的判定树一致。散列表通过哈希函数把键映射到存储位置，理想情况下查找是常量级。冲突处理方法有开放地址法（线性探测、二次探测）与链地址法。装填因子越大冲突越多，需要适时扩容。

### 排序

插入类排序中，直接插入排序把每个元素插入到已排好的前缀中，基本有序时效率很高；希尔排序按增量分组后插入排序，逐步缩小增量。

交换类排序中，冒泡排序反复比较相邻元素并交换；快速排序选一个基准把序列划分成两部分再递归处理，平均性能最好但最坏情况会退化。

选择类排序中，简单选择排序每趟选出最小元素放到已排好位置；堆排序用完全二叉树维护堆性质，建堆与调整都是对数级操作，适合求前 k 个最值。

归并排序把序列分成两半分别排序再合并，稳定且最坏复杂度也是对数级，代价是需要额外空间。基数排序按位分配收集，适合整数与定长字符串。

排序算法的稳定性指相等元素的相对次序在排序后是否保持不变。插入排序、冒泡排序、归并排序、基数排序稳定；快速排序、希尔排序、简单选择排序、堆排序不稳定。选择算法时要综合考虑数据规模、初始有序程度、是否要求稳定以及空间限制。`;

const ALGORITHM = `## 算法设计与分析

### 算法分析基础

衡量算法优劣的主要指标是时间复杂度与空间复杂度，用大 O 记号表示增长的数量级。分析时关注基本操作的执行次数随问题规模的变化趋势，忽略常数因子与低阶项。

常见复杂度由低到高依次为：常数级、对数级、线性级、线性对数级、平方级、立方级、指数级、阶乘级。渐进下界与紧确界分别描述算法的最好情况与确界。

递归算法的时间复杂度常用递推式表示，主定理给出了分治类递推式解的通用判定方法。

### 分治策略

分治把问题分解为规模更小的同类子问题，分别求解后再合并结果，三个步骤是分解、求解、合并。

二分查找每次把查找范围缩小一半。归并排序与快速排序都是分治在排序上的应用。大整数乘法通过把乘数拆分降低复杂度；矩阵乘法有分治实现与更快的优化算法。分治适合子问题相互独立的情形，若子问题重叠会导致大量重复计算，应改用动态规划。

### 动态规划

动态规划最核心的两个特征是**最优子结构**与**重叠子问题**：最优子结构指问题的最优解包含子问题的最优解；重叠子问题指递归求解过程中同一个子问题被反复计算，因此可以把结果存表避免重复。

动态规划的设计步骤是：刻画最优解的结构、递归地定义最优值（写出状态转移方程）、自底向上计算并保存中间结果、由保存的信息构造最优解。

经典模型包括：最长公共子序列、最长递增子序列、编辑距离、背包问题（0-1 背包与完全背包的转移差别在于容量循环的方向）、矩阵连乘的最优加括号方式、最短路径的逐段决策。

记忆化搜索是动态规划的自顶向下写法，用递归加缓存实现，思路更接近问题定义；自底向上的迭代写法没有递归开销，但要正确定义计算顺序。两者的复杂度相同，选择取决于状态依赖是否便于排序。

### 贪心算法

贪心算法在每一步都做出当前看来最好的选择，并且希望由此得到全局最优解。它比动态规划更简单高效，但只对满足贪心选择性质与最优子结构的问题有效，必须给出正确性证明（常用交换论证或归纳法）才能放心使用。

经典应用包括：活动安排问题（按结束时间排序后依次选取）、哈夫曼编码、单源最短路径的迪杰斯特拉算法、最小生成树的普里姆与克鲁斯卡尔算法、部分背包问题。注意 0-1 背包不能用贪心求解，因为物品不可分割，贪心选择可能导致次优解，必须用动态规划。

### 回溯法与分支限界法

回溯法系统地搜索解空间树，从根出发深度优先扩展，发现当前部分解不可能导致可行解或最优解时立即剪枝回退。它适合求解组合优化与约束满足问题，如八皇后、图的着色、旅行商问题、子集和问题。

回溯法的效率取决于剪枝函数的强弱。约束函数剪掉违反约束的分支，限界函数剪掉不可能优于当前最优解的分支。解空间树有子集树与排列树两种典型形态。

分支限界法采用广度优先或最小耗费优先的方式搜索，用优先队列维护活结点，按限界函数估计的界值决定扩展顺序，适合求最优化问题。它与回溯法的主要区别是搜索顺序与活结点的存储结构。

### 随机化算法与 NP 完全性

随机化算法在计算过程中使用随机数。数值随机化算法用随机采样逼近结果；蒙特卡洛算法总能给出答案，但答案有概率不正确；拉斯维加斯算法给出的答案一定正确，但运行时间不确定。随机化可以打破最坏情况，但必须分析错误概率或期望时间。

问题按难度分成若干类。P 类问题是可以在多项式时间内求解的判定问题；NP 类问题是解可以在多项式时间内被验证的判定问题。若一个问题属于 NP 且所有 NP 问题都能在多项式时间内归约到它，则称它为 NP 完全问题，旅行商判定版、可满足性问题、图着色问题都是典型例子。目前尚无人证明 P 是否等于 NP，对 NP 完全问题通常只能求近似解或采用启发式算法。`;

/** 数据结构与算法：树、图、排序、动态规划与 NP 完全性。 */
export function buildDataStructureNotes(): SeedLectureInput[] {
  return [
    makeLecture('9', '数据结构', '专业核心', DATA_STRUCTURE),
    makeLecture('10', '算法设计与分析', '专业核心', ALGORITHM),
  ];
}
