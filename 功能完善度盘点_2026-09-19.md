# 功能完善度盘点（2026-09-19）

## 一句话结论

核心链路**已跑通且有验证**，但还不能叫"完善"。差的是三件事，按重要性排序：

1. **向量检索整条支路"建好了但没接线"** —— 这是唯一一个"功能缺口"，不是"没做"。
2. **自建后端从未被 App 真正调用过** —— P0 安全改善在**代码层成立**，在**运行层还没兑现**。
3. **文档与真机复验积压** —— 交付材料层面的事。

证据来源：逐符号扫描 174 个 `.ets` 文件（区分"定义点"和"调用点"），
以及 10 套离线测试 + 服务端测试 + 真机编译的实际运行结果。

---

## 一、已经在跑的能力（都有测试与编译证据）

| 能力 | 证据 |
|---|---|
| AI 助教对话 + Agent 工具链（8 个工具） | `AgentTools.buildToolDefinitions()` 返回 8 项，含唯一带参工具 `search_course_knowledge` |
| 流式输出 / 取消 / 会话历史管理 / 轨迹日志 | p3 24/24、p3.5 13/13 |
| 自适应练习 + 错题本闭环 | p5 69/69 |
| 掌握度累积模型 + 练习历史 | p6 51/51 |
| 学习者档案（跨会话记忆） | p2 24/24 |
| 课程知识库检索（BM25） | `ChatViewModel.runKnowledgeChat` → `KnowledgeStore.search()`，另有 agent 工具通路 |
| 索引持久化 + 启动预热 | `KnowledgeIndexStore` + `KnowledgeStore.scheduleWarmUp()`（`EntryAbility` 调用） |
| 客户端零厂商密钥的双模式路由 | `AiBackend` + `AiService` 按请求惰性解析 |
| 自建后端 `server/` | 86/86（48 单元 + 38 端到端，真起服务 + 桩上游） |
| 编译 | `assembleHap --no-incremental` → BUILD SUCCESSFUL，0 error |

合计：离线 **515/515**（10 套件）+ 服务端 **86/86**。

---

## 二、还差什么

### P0-1 · 把向量侧接上线（唯一的"功能缺口"）

**现状**：语义检索的**所有零件都在**，但**一个都没接进调用链**。扫描结果明确：

| 符号 | 定义位置 | 生产调用点 |
|---|---|---|
| `searchHybrid` | `KnowledgeStore.ets:274` | **无** |
| `prepareVectors` | `KnowledgeStore.ets:293` | **无** |
| `KnowledgeProxyEmbedder` | `KnowledgeEmbeddingProxy.ets:235` | **无** |
| `localEmbedder` | — | **零使用**（连本地哈希嵌入器都没被实例化） |

⇒ **当前跑的确定是纯词法 BM25**。RAG 真正在工作的只有两条通路：`ChatIntent.KNOWLEDGE`
快路径和 `search_course_knowledge` 工具（两条都已在代码里确认接通）。

这**不是遗漏，是刻意的**——本地哈希嵌入器没有语义能力，实测等权融合会让
Recall@3 从 **0.8485 掉到 0.7879**。但对外讲"已支持语义检索"会是假的。

**要做的事**（顺序不能反）：

1. **部署后端并配好 `EMBEDDING_API_KEY`**（见 P0-2）。
2. **离线预算 241 个块的向量，随包发布**。语料是静态的（48 文档 / 241 块 / 6.2 万字符），
   运行时只需给 query 嵌一次。成本约 **3 分钱**，一次性。
3. **先换向量存储格式**：1024 维按现在的 JSON 存法是 **1.88 MB**，随包发布不可接受；
   换 Base64(float32) 是 1.26 MB，**int8 量化约 241 KB**。这一步必须做在打包之前。
4. 运行时用 `KnowledgeProxyEmbedder` 嵌 query → `prepareVectors` 灌向量 → 切到 `searchHybrid`。
5. **改完必须复跑评测集**：`KnowledgeEvalSet`（33 例）+ `KnowledgeRetrievalMetrics`。
   **Recall@3 必须 ≥ 0.8485**，掉了就是退化，不能因为"接上了真模型"就放过。

### P0-2 · 后端部署 + App 端到端联调（P0 安全建议的运行层兑现）

**现状**：`server/.env` **不存在**（只有 `.env.example`）⇒ App 运行时**必然处于直连模式**，
`server/` 至今**没被这个 App 调用过一次**。也没有部署产物。

代码完备度是够的（86/86），但"**写完了**"和"**跑起来过**"是两件事。

**要做的事**：

1. 部署到一台公网可达的机器，配 HTTPS（端子/反代），设 `APP_TOKEN`。
2. ⚠️ **`APP_TOKEN` 为空即关闭鉴权**，仅供本机演示。对外提供服务前必须设，
   否则等于把一个能用你模型额度的代理挂在公网上。
3. App ⚙️ 里填 Base URL + 令牌，保存（后端模式）。
4. **实测三种情形**：正常对话、令牌错误（应 401 且文案可读）、后端不可达（应有明确提示，不静默回退到直连）。

> 顺带说明 P0 的准确状态：`AI-SEC-001` 的建议是"客户端改调自有后端，由后端保存密钥
> 并执行认证、限流、审计"。**代码层这一半已经补完了**（`server/` + 客户端双模式），
> 但**运行层还没兑现**（后端没部署、App 没指向它）。所以现在讲"已经安全了"仍然是不准确的。

### P1-1 · 文档更新（若作为交付/答辩材料，这是硬伤）

- **根 `README.md` 仍是华为原始样例**：通篇没提 AI 助教，工程目录里
  **没有 `features/aiagent`，也没有 `server/`**。
- `AI助教Agent化进度_2026-09-07.md` **停在 09-16**，P7（RAG 全链路）与后端两期都没记进去
  —— 而它自称"最权威"。
- `项目进度报告.md` 已过期（2026-05-24，早于 aiagent），建议直接标注废弃或删除。

### P1-2 · 真机复验积压

这些是**代码完成、但从未在真机上确认过**的：

- **P4**（API Key 运行时化 / Markdown / 图片 MIME）
- **P5 六项**：长回复气泡不溢出、错题重做标记、答错不增条数而是原地更新、
  答对沉底、错题本清空后回退硬编码题库、📋 一次点击即弹出
- **P6** 掌握度模型整体
- **后端双模式**（依赖 P0-2）

### P2 · 长期遗留（不阻塞，知道即可）

- 图片搜题依赖**本机 HTTPS 后端**，未解决（历史遗留）。
- 错题本**已产生的重复记录**需要一次性去重逻辑，或手工清理。
- `MineLocalData` 演示题答案**没有自动护栏**（逐题核对过正确，但没人守着）。
- 评测集 paraphrase 分桶 **0.75 偏低**（direct 0.9048）—— 这是 BM25 的固有短板，
  也正是向量侧要解决的。**接线后应重点看这个数字**。

---

## 三、建议的下一步顺序

```
① 部署后端 + 配 .env + HTTPS        → 让 P0 在运行层兑现（最紧急，也最快）
② App 指向后端，真机跑通三种情形      → 双模式才算真的成立
③ 向量存储换 int8/二进制格式         → 打包前的必要前置
④ 离线预算 241 块向量随包发布         → 一次性成本约 3 分钱
⑤ 接 searchHybrid，复跑评测集        → Recall@3 ≥ 0.8485 才算过
⑥ 补 README + 进度文档               → 补齐交付材料
⑦ 清理 P4/P5/P6 真机复验积压          → 收尾
```

①②可以今天就做完；③④⑤是一整条链，属于"下一个 phase"；⑥⑦是收尾。

---

## 四、明确不建议做的事

- **不要自训嵌入模型**。没有成对的 (query, positive) 标注，33 例评测集只够评测、
  不够训练。自训在本项目是纯负债。
- **不要把本地哈希嵌入器等权融合进主检索**。已实测掉分（0.8485 → 0.7879），
  p7 已把这个结论固化成断言。
- **不要为了嵌入再给 App 塞第二把厂商密钥**。那等于把 P0 的洞再捅一次 ——
  这也正是 `server/` 存在的理由。
